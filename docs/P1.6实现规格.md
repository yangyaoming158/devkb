# 《P1.6 仓库级证据 + 有界工具调用 实现规格》

> 状态：**草案**（2026-08-10 起草，待用户批准；与《ADR-0011》《P1.6任务清单》同批冻结后方可开工）
> 前置 Gate：P1.5 已于 2026-08-09 以「**Gate 未通过**」状态冻结（§15 十条 5 达成 / 5 未达成，`all_hard_gates_passed = False`）。**该结论不变、不补勾、不重跑**；P1.5 未达成的第 1/2 条恰是本阶段的题目。
> 依赖：《ADR-0011》（D6 窄修订）《ADR-0010》§3《P1后真实仓库可用性测试问题记录》RT-17/RT-21《范围冻结与架构决策》D6/D7/D8《P1.5实现规格》《Evaluation-v1.5》《Evaluation-v1.6》（待 U4.1 冻结）
> 执行状态见《P1.6任务清单》。

## 1. 目标与范围

P1.6 只做**一件事**：给系统一种它现在完全没有的能力——**回答关于仓库整体的确定性事实**（这个符号存不存在、这类东西一共有几个、这个范围里有没有）。P1.5 修的是「说出口的话要诚实」，P1.6 补的是「有些话现在根本没有依据可说」。

P1.6 冻结交付（全部在 ADR-0011 修订后的 D6 与**零新增运行依赖**之内）：

1. **仓库级 inventory**（manifest / symbol / annotation）随摄取确定性构建，与 documents 同源同 commit（RT-21）；
2. **符号存在性校验**接管 `not_found` 的 refs 绑定，关闭 U3.2 实测的编造类名与绑错目标（`U32-P16-01/02`）；
3. **有界只读工具子循环**：模型在证据不足时按需索取仓库级事实，轮次与预算双硬顶（ADR-0011）；
4. **refine 的符号约束**：改写查询中的路径/符号只能来自用户问题、已检证据或 inventory（RT-17）；
5. **全局否定/穷举的确定性证明**：`confirmed_undocumented` 首次可产出，每条绑定 commit + 扫描范围 + 匹配规则 + 覆盖计数（RT-21）。

**明确不做**（见 §10）：多语言摄取（`.vue/.ts/.tsx/.sql`，RT-13）、检索机制改进（RT-06 及 RT-05 检索层 metadata 权重）、`claims` 未进正文（`U32-P16-03`）、在线 L2/NLI、LangFuse、MCP、UI/SSE、云部署。**上述三项经 2026-08-10 用户裁决移出 P1.6**（ADR-0011 §修订/被取代内容已登记该收窄）。

## 2. 继承与兼容性

- **D6 按 ADR-0011 窄修订**：在线调用点由「恰好四类」扩为「四类 + 一个有界只读工具子循环」；单 run 总请求硬上限 6 → **11**，其中四个既有调用点的逻辑上限**仍为 6 不变**、工具子循环请求上限 **5**、工具执行轮次硬上限 **4**。检索轮次硬上限仍为 **2**。除此之外 D6 全部保留——尤其「**路由由确定性代码做决定**」一节完全不修订。
- **D7 数据隔离不变**：inventory 的全部查询构造只允许出现在 `src/devkb/repositories.py`；Repository 以 project_id 实例化；**`project_id` 不出现在任何工具的参数 schema 里**，由确定性代码注入，模型无法指定或覆盖。
- **ADR-0002 embedding 冻结不变**：`Qwen/Qwen3-Embedding-0.6B` 与 `vector(1024)` 不动。inventory 是**结构化事实表**，不产生新向量、不参与向量检索。
- **默认检索机制不变**：在线仍是 vector-HNSW 多查询，不启用 Hybrid lexical（README 已记录 T15.4 的负结果）。RT-06 不在本阶段，**不得借 inventory 之名顺手改检索默认值或权重**。
- **摄取后缀不变**：仍为 `.md/.txt/.java/.yml/.yaml/.properties`。这条对本阶段有一个**关键的诚实性后果**，见 §4 的覆盖边界。
- **P1.5 的响应契约全部保留并只增不删**：四态 `mode`（含 `policy_refusal`）、`not_found_details` 平行字段、`warning_details`/`resolved_warnings`、覆盖披露、能力边界声明、partial 范围句。
- **一处有意的契约变更（须显式声明，不得静默）**：`not_found` 的 `confirmed_undocumented` 分类由「永不产出」改为「有 inventory 支撑时可产出」，P1.5 锁定该行为的冻结用例必须改写。见 §8。
- `PROMPT_VERSION` 因本阶段递增；P1.5 的 Prompt 冻结在本阶段失效。这**不回改** P0/P1/P1.5 任何验收结论，也不重跑或据此调优任何已使用的 holdout。
- CLI 与 `POST /ask`、`GET /runs/{id}`、`GET /healthz` 只增字段、不删既有字段。

## 3. 先冻结 Evaluation-v1.6（前置纪律）

动任何 `src/`、Prompt、schema 或迁移之前，先按《Evaluation-v1.6》完成回归集冻结（任务 U4.1）。最低要求：

- **Evaluation-v1.5 全量作回归网**：13 条契约题 + 7 条反例 + 探针继续跑，指标口径不变。**P1.6 的 Gate 不得以新指标替代 P1.5 既有指标**，也不得因新能力使既有指标退化而不记录。
- **新增 v1.6 题集**，至少覆盖五类，每类 ≥2 题：① 符号存在性正例（点名真实类/方法）；② **符号存在性负例（点名不存在的类，直接复现 `U32-P16-01`）**；③ 穷举题（如「一共有几个 Controller / 哪些方法带某注解」）；④ 全局否定题（如「`src/main` 里有没有 X」）；⑤ **不在摄取范围的语言的否定题**（如「有没有 Vue 组件」——正确行为是**披露 inventory 无发言权**，而不是回答"没有"）。
- **反例必须含「工具不该被调用」的题**：证据已充分时不得进入子循环（防止为用工具而用工具）。
- 每题按两栏预期预登记：**P1.6 契约预期绑定 Gate、P1.7/P2 目标预期仅记录**（沿用 P1.5 的两栏纪律）。
- 另建 v1.6 dev / holdout；**v1.5 holdout 至今封存（`holdout_accessed=false`），P1.6 期间继续封存**，动它仍须单独授权。

## 4. 仓库级 inventory（RT-21）

### 4.1 三类事实

| 事实 | 内容 | 来源 |
|---|---|---|
| **manifest** | 已摄取文件清单：`rel_path`、`content_hash`、`suffix`、active 状态 | 摄取管线，与 `documents` 同一次写入 |
| **symbol** | 类 / 接口 / 枚举 / 注解类型 / 方法：名称、kind、所属包与文件、起止行 | 现有 tree-sitter Java 解析器（`src/devkb/ingest/java.py` 已识别 `class_declaration` / `method_declaration` / `annotation_type_declaration`） |
| **annotation** | 注解**使用**：注解名、被注解的目标符号、文件与行 | 同上 |

### 4.2 构建纪律

- **与 documents 同源、同一次摄取、同一 commit**。inventory 不单独构建、不异步补建——不同源就会产出"确定性"的错误否定。
- 落 PostgreSQL 现有库（D1 单库多职责不变），走 Alembic 迁移，`project_id NOT NULL + FK`（D7）。
- **fail-closed**：inventory 与 `documents` 的 commit / content_hash 集合不一致时，**不产出任何否定结论**，退回 P1.5 的条件式措辞并记 warning。宁可不答，不可答错。
- 解析失败的文件计入**未覆盖清单**并可查询——「解析失败」与「不存在」必须可分。

### 4.3 覆盖边界（本节是本阶段最容易出错的地方）

**inventory 的论域 = 已摄取后缀的文件，仅此而已。**

摄取范围仍是 `.md/.txt/.java/.yml/.yaml/.properties`。因此：

- 对 `.vue/.ts/.tsx/.sql` 等**未摄取格式，inventory 没有任何发言权**。问「有没有 Vue 组件」时，正确回答是**「该格式不在摄取范围、未纳入索引，本系统无法确认仓库是否包含此类文件」**（沿用 P1.5 的 `unsupported_or_not_ingested` 条件式措辞 + 覆盖披露），**绝不能回答"不存在"**。
- 任何否定结论必须**同时**声明扫描范围与该范围的后缀集合。**否定结论的论域声明是硬性内容，不是修辞**。
- 动态注册、反射、注解处理器生成等**静态不可见**的边界，单独披露、不计入否定证明的论域（RT-21 原判据）。

> **为什么单列这一节**：inventory 让系统第一次有能力说"没有"。而"没有"是本项目最危险的一句话——P1 后真实测试的 RT-02/RT-15 就是「把已索引内容说成不存在」，P1.5 花了一整个阶段把它压住。**新能力必须带着旧纪律上线**，否则 P1.6 会以更权威的口气重犯 P1 的错。

## 5. 符号存在性校验与 `not_found` refs 修复（U32-P16-01/02）

### 5.1 要修的实测缺陷

U3.2 人工复核（2026-08-09，`evalsets/reports/p1.5-manual-check.md`）实测：

- `not_found` 出现 `KnowledgeBaseService`、`ConversationService`（c07）、`AsyncConfig`（e06）——**三者在语料源仓库全仓零命中，属编造**；
- c07 把「会话删除的生产实现」绑到 `domain/Conversation.java`，实际在 `web/ConversationController.java` 与 `rag/RagService.java`；
- 连带把 `basis` 标为 `corpus_index`，输出「该路径已在当前项目索引中（状态 active）」——**一句关于错误文件的、听起来已核验的断言**。

根因：`NotFoundDetail.refs` 由 `iter_path_tokens(text)` + `path_matches_token`（`src/devkb/agent/not_found.py:379` / `:393`）从模型自写的缺口句子抽路径 token 再匹配索引，而 `path_matches_token` **只判文件身份、不判命题**。

### 5.2 冻结要求

缺口句子中点名的符号/路径，**先经 inventory 判定，再决定措辞与 refs**，四种结果各有确定性文案：

| inventory 判定 | refs | 措辞 |
|---|---|---|
| 符号存在，唯一定位 | 绑定 inventory 给出的**真实路径与行段** | 「该符号位于 X，本轮未召回，属当前证据缺口」 |
| 符号存在，多处同名 | 绑定**全部**命中位置（有上限，超限截断并声明） | 同上，并声明命中多处 |
| 符号**不存在**于已摄取范围 | 空 | 「本次索引的语料中不存在名为 X 的符号；索引范围见覆盖披露」——**可判定的负结论，须绑定扫描范围** |
| 不在摄取范围（按后缀/技术词判定） | 空 | 沿用 P1.5 `unsupported_or_not_ingested` 条件式措辞，**不断言存在与否** |

- **`path_matches_token` 不再单独决定 refs**；它可保留为候选生成，但**必须经 inventory 确认**才能落 refs。
- **`basis` 取值须如实反映校验来源**：经 inventory 确认的记新增来源（如 `symbol_inventory`），未经确认的**不得**标为 `corpus_index`。
- **反过拟合反例（必须有单测）**：同名类分布在多个包下、内部类 / 嵌套类、泛型与数组类型、方法重载、大小写差异、`com.example` 之类的包名片段（P1.5 已有的词法防御不得回退）。

## 6. 有界只读工具子循环（ADR-0011）

### 6.1 位置与触发

- **新节点，位于 evaluate 之后、generate 之前**。理由：模型必须**先看到证据**才知道自己缺什么；plan 阶段（retrieve 之前）无法预知缺口——P1.5 的 `required_evidence` 解析已证明那条路径只能解析用户显式说出的东西。
- **是否进入由确定性代码判定**（模型不能要求进入）。进入条件在实现前冻结，至少包含：证据不足或存在未覆盖方面、且全局预算足以完成子循环最小一轮并保住 `generate`。
- 结束条件三选一，全部由确定性代码判定：① 达轮次硬上限 4；② 本轮模型未请求任何工具；③ 预算不足以再发一次请求。**模型不能声明「我还要继续」来延长循环。**

### 6.2 工具面（冻结清单）

全部只读、全部经 Repository 层（D7）、参数经严格 Pydantic 校验、**`project_id` 不在任何 schema 里**：

| 工具 | 参数 | 返回 |
|---|---|---|
| `lookup_symbol` | `kind`（枚举：class/interface/enum/annotation/method）、`name` | 是否存在 + 命中位置列表（`rel_path`、行段、所属包），有上限 |
| `list_annotated` | `annotation`、`scope`（目录前缀，枚举或校验后的相对路径） | 命中符号列表 + 计数 + 扫描范围声明 |
| `count_matching` | `scope`、`pattern_kind`（枚举）、`pattern` | 计数 + 扫描范围 + 匹配规则声明 |
| `check_path` | `rel_path` | 是否在索引内 + active 状态 + 后缀是否在摄取范围 |

- **返回结构化事实，不回传文档正文**。正文仍只经 retrieve 与引用校验路径。
- `pattern` 类参数**不得**被当作可执行文本；匹配规则限定为确定性字符串/前缀/枚举匹配，**不引入正则回溯风险**（实现须声明所用匹配算法并有上界）。
- **绝对禁止**（ADR-0011 §3）：任何 shell / 进程执行工具（含 `grep`/`find`/`cat` 的"安全包装"）、任何写工具、任何跨 project 查询、任何返回正文全文的工具、开放式无界 ReAct。
- **工具执行失败**：确定性降级——记 warning、该轮结果标记不可用、按上限判定继续或结束，**不重试、不无界**。工具永不执行被查询内容。

### 6.3 预算守卫（确定性，冻结）

- 两层硬顶：总请求 ≤ **11**、子循环请求 ≤ **5**、工具执行轮次 ≤ **4**。
- **裁剪顺序优先保 `generate`**：若继续子循环会使 `generate` 无请求可用，则立即结束子循环并携已获得的工具结果继续。**宁可少查一轮，不可答不出来。**
- 子循环内解析/传输失败最多重问 1 次，重问计入全局预算，且**不重置轮次计数**。
- 达轮次上限而未收敛时，**携已有结果继续，不因未收敛而拒答**。

### 6.4 可断言性（本阶段的测试硬要求）

必须能用 FakeLLM 精确断言：节点序列、**每个 run 的供应商请求总数**、**子循环轮次**、每轮请求的工具名与参数、终态。新增的不确定性**只允许**存在于「子循环内每轮选了哪个工具」这一点。

## 7. refine 的符号约束（RT-17）

- refine 改写查询时，其中的**路径 / 符号 / 框架名 / 状态词只能来自**：用户问题原文、已检证据、或 inventory 查询结果。三者之外的符号**不得进入改写后的查询**。
- 模型若写出遍历/执行类动作（`grep`、`find`、`cat` 等），确定性代码**拒绝执行并记 warning**，必要时转为对应的结构化工具调用；**绝不实现 shell 执行**（RT-17 的原判据是「区分检索查询与真实可用的只读工具动作」，不是「把 shell 包一层」）。
- 用户限定的源码范围（如"只看生产实现"）在 refine 后必须保持，不得扩到测试/生成代码。

## 8. 全局否定 / 穷举的确定性证明与 `confirmed_undocumented` 解锁

- **`confirmed_undocumented` 首次可产出**，且**只在有 inventory 支撑时**。每条必须绑定四要素：**commit + 扫描范围（含后缀集合）+ 匹配规则 + 覆盖计数**。缺任一要素即不得产出，退回条件式措辞。
- **这是一次有意的契约变更**：P1.5 对该分类有「永不产出」的单测锁定，本阶段必须**改写那条冻结用例**并在任务清单显式登记为契约变更。**不得静默放开**——改写的用例必须变成「无 inventory 支撑时仍永不产出」的更强断言，而不是删掉。
- **语义证据只负责解释成员**，不负责证明覆盖（RT-21 原判据）。「一共 26 个 endpoint」的数字来自 inventory 计数，不来自模型数数。
- 静态不可见边界（动态注册、反射、注解处理器生成）单独披露，不计入论域。
- **未摄取格式一律不进否定证明的论域**（§4.3）。

## 9. 安全边界与注入风险增量（D8）

- D8 结论「只读工具面消除执行类风险」不变；工具面仍全部只读。
- **新增一条风险，如实声明不宣称消除**：工具返回值进入模型上下文，被检索文档中的指令载荷因此有了一条**新的间接影响路径**（模型可能因文档内容而选择某个工具或参数）。缓解：参数经严格 schema 校验且无自由文本可执行字段；返回结构化事实不含正文；子循环有硬上限，最坏情形是浪费预算而非越权。**写入 README「已知限制」，不得省略。**
- P1.5 的 policy-refusal 终态与三张词法闭表继续有效；**policy 命中时不进入工具子循环**（零 LLM 调用、零检索、零工具的拓扑事实必须保持）。
- 日志/轨迹不打印文档正文与 secret（D8 不变）；工具调用轨迹记录工具名、参数与结果摘要，**不记录问题正文以外的自由文本载荷**。

## 10. 明确不做（推 P1.7 / P2）

经 2026-08-10 用户裁决收窄，以下**不在 P1.6**：

- **RT-13 多语言摄取**（`.vue/.ts/.tsx/.sql`）——需新增 tree-sitter grammar，触碰依赖门禁，须另立 ADR。ADR-0010 §3 原本就标注其为「独立可裁量」。
- **RT-06 检索改进**（path/symbol/method-aware 通道或 boost）与 **RT-05 检索层 metadata 权重/排序**——必须先做同题对照实验才能定方案，是独立回滚点。**P1.6 不得未经实验改动检索默认值。**
- **`U32-P16-03`**（结构化 `claims` 未进入正文）——独立小项，且修法有两条方向不同的路（确定性一致性扩展 vs Prompt 变更），需单独裁决。
- 在线 L2/NLI（D9 不变）、LangFuse（P2-A）、MCP（P2-B）、轻量 UI/SSE、云部署、reranker、GraphRAG、Multi-Agent。

## 11. 测试与 Evaluation v1.6

- CI 与测试**不得调用真实模型/真实 API**（FakeEmbedder / FakeLLM）；业务代码禁止 `if testing` 分支；每项任务完成时 ruff、pyright、pytest 零错误全绿。
- **工具子循环必须可用 FakeLLM 脚本化断言**（§6.4）：节点序列、请求总数、轮次、每轮工具名与参数、终态。
- **Evaluation-v1.5 全量并入回归**，与 v1.6 一同进 `make eval-ci`；P0/P1/P1.5 兼容回归继续全绿。
- inventory 构建须有**确定性单测**（给定小语料 → 期望的 symbol/annotation 集合），不依赖真实大仓库。
- 不引入在线 NLI；L2 仍属离线（D9 不变）。

## 12. P1.6 验收标准（退出 Gate）

以下全部满足才可勾选 P1.6 完成：

1. **符号存在性**：Evaluation-v1.6 的符号正例题给出**真实定位**（路径 + 行段经 inventory 确认）；负例题（点名不存在的类）产出**可判定的负结论并绑定扫描范围**，且 `not_found` 中**零编造符号**（直接复现 `U32-P16-01` 的三个类名作硬用例）。
2. **refs 绑定正确**：`not_found` 的 refs 全部经 inventory 确认；`basis` 如实反映校验来源，**未经确认者不得标为 `corpus_index`**（直接复现 `U32-P16-02` 的 c07 场景作硬用例）。
3. **穷举/否定证明**：穷举题的计数来自 inventory 且绑定 commit + 扫描范围 + 匹配规则 + 覆盖计数；`confirmed_undocumented` 仅在有支撑时产出，无支撑时**仍永不产出**（改写后的冻结用例守住）。
4. **未摄取格式不被误判**：`.vue/.ts/.tsx/.sql` 类问题一律走「不在摄取范围、无法确认仓库是否包含」的条件式措辞 + 覆盖披露，**零"不存在"断言**。
5. **工具子循环有界且可断言**：FakeLLM 精确断言节点序列、请求总数 ≤ 11、子循环请求 ≤ 5、轮次 ≤ 4、每轮工具名与参数、终态；证据充分时**不进入**子循环；`policy_refusal` 路径**零工具调用**（拓扑事实）。
6. **预算守卫**：存在一条冻结用例证明「继续子循环会挤掉 `generate` 时立即结束子循环」，且该场景下仍产出可用回答而非拒答。
7. **安全边界**：零 shell / 写 / 跨 project / 正文回传工具（**静态扫描 + 用例双重把守**）；工具执行失败走确定性降级不重试；工具返回值注入风险已写入 README。
8. **RT-17**：refine 改写后的查询中零未经核验符号；遍历/执行类动作被拒绝执行并记 warning；用户限定的源码范围保持。
9. **P0/P1/P1.5 兼容回归**：Evaluation-v1.5 全量复跑，**既有指标无退化**（有退化必须如实记录并说明，不得掩盖）；本地 `make ci && make eval-ci` 与当前头提交 GitHub Actions 全绿；`PROMPT_VERSION` 递增已记录。
10. **v1.6 holdout** 只运行一次并留档，不据结果调参。

### 12.1 判据纪律（自 P1.5 的教训写入，本阶段起生效）

P1.5 的退出 Gate 有两条在实际运行中失去判别力，本阶段起以下三条纪律具约束力：

- **「本轮无样本触发」不得记为通过。** P1.5 的 `zero_full_without_required_evidence = True` 是在 20 题**零 full** 的情况下取得的——一个从未被触发的守卫返回 True，不构成守卫有效的证据。本阶段每条 Gate 必须同时报告**施压样本数**；施压样本数为 0 时一律记为「**未施压**」，既不记通过也不记失败。
- **「判不了」不得记为通过（fail-closed 继续有效）。** 沿用 T31.2R-c 的三态判定：`True` / `False` / `None`，`None` 不计入通过。
- **机器可判定子集全绿 ≠ 判据达成。** P1.5 的 `not_found_decidable_ok = 104/104` 全绿，而人工在同一批数据里找出编造类名。凡判据含**命题级**内容（某物在仓库里存不存在、某断言是否被证据支持），必须显式声明「机器覆盖到哪、剩下的归谁判」，并把未覆盖部分路由到人工复核任务，**不得用可判定子集的绿色代表整条判据**。

**明确不作为 P1.6 出口条件**（属 P1.7/P2）：`.vue/.ts/.sql` 已摄取、检索指标提升、`claims` 与正文一致、在线 L2。**P1.6 通过只证明「仓库级确定性事实可查、可证、可界」，不得对外宣称检索质量或语义正确性有提升。**
