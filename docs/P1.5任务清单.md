# P1.5 任务清单（真实仓库可靠性/诚实加固）

> 开始日期：2026-07-23
> 前置 Gate：P1 已于 2026-07-22 验收关闭（结论不变）
> 唯一实现依据：《P1.5实现规格》；评测依据：《Evaluation-v1.5》；治理依据：《ADR-0010》。
> 规则：完成判据全部满足后才勾选，并追加 `✅ YYYY-MM-DD 结果与证据 commit`；规格冲突或缺失写入文末偏差记录，等待用户裁决。
> 2026-07-24 规划复审修订：判据与《P1.5实现规格》《Evaluation-v1.5》同日修订同步（两栏预期、not_found 静态判定、policy-refusal 机制、13+5 拆分、诊断轨迹迁移/上限）；P1.5 各任务在从 `p1.5` 派生的 task 分支上进行，验收后快进合回 `p1.5`（2026-07-28 按 WF-001 修订，原文"P1.5 工作在 `p1.5` 分支进行"见文末偏差记录）。
> 边界：全程在 D6 与现有依赖栈之内；不新增运行依赖、不改检索机制、不重跑 P1 holdout。多语言摄取/inventory/工具调用属 P1.6，禁止在本清单提前实现。

## 执行顺序

```text
U3.1 冻结 Evaluation-v1.5（预登记 + 反例）——先于一切 src/Prompt 改动
 ├─→ T22 证据类型硬约束与权威性分层（RT-01/05）
 ├─→ T23 not_found 四分类 + 全轮历史事实校验（RT-02/15）
 ├─→ T24 跨轮覆盖单调性 + partial 保留（RT-16/20）
 ├─→ T25 节点一致性 + finalize 覆盖差分（RT-03/08）
 ├─→ T26 partial 认知口径 + 安全/配置覆盖矩阵（RT-18/19）
 ├─→ T27 policy-refusal 终态（RT-22）
 ├─→ T28 摄取覆盖披露（案例三/十廉价版）
 ├─→ T29 unknown project fail-fast 顺序（RT-23）
 └─→ T30 诊断轨迹最小版 + warning 归属（RT-07/10）
T31 Evaluation-v1.5 harness 扩展与 dev Gate（T22 后可并行，最终依赖 T22–T30）
全部完成 → T32 P1.5 收尾与 Gate（依赖 U3.2 人工复核、U3.3 v1.5 holdout 授权）
```

同一时间只推进一个主任务；允许先写失败测试或最小 spike，但不得提前实现 P1.6/P2。`PROMPT_VERSION` 因本阶段递增，须显式记录并使旧 dev 报告不被覆盖。

## U3 用户/人工任务

- [x] U3.1 在首次 P1.5 dev 运行前冻结 Evaluation-v1.5
  - 判据：按《Evaluation-v1.5》§2 把 18 条 post-P1 复现路径转为正式回归题（13 条真实问答 + 5 条机制检查分开落盘，文件名与 JSONL schema 依 §2），补自然问法/合法安全咨询/真正无资料/格式未摄取反例；每题按**两栏预期**预登记（P1.5 契约预期绑定 Gate、P1.6 目标预期仅记录；必需证据、允许补充类型、禁止替代类型、预期 mode、关键路径/符号，"必须引用 X"登记为条件式）；建立独立 v1.5 dev/holdout，不复用、不重跑、不据此调优已使用的 P1 holdout；冻结后运行任何 P1.5 dev 即锁标签
  - ✅ 2026-07-24 冻结：`evalsets/v1.5/` 落 4 文件——`contract_dev.jsonl`（13 条真实问答，字段 source_case/run_id/aspects/required_evidence(type+path/symbol)/allowed_supplement/forbidden_substitute/expected_mode_p15/p15_expectation/p16_target/key_paths/key_symbols）、`contract_dev_extended.jsonl`（8 条反例：误杀负例 e02/e03、自然问法 e01、真无资料 e04、格式未摄取 e05、全局否定 e06、注入改写 e07、跨 project fail-fast e08）、`contract_holdout.jsonl`（6 条，已封存、实现期 T22–T31 不查阅）、`mechanism_checks.md`（14–18 走 §5.2 确定性层）。用户 2026-07-24 逐题确认全部 13 条 P1.5 契约预期（c06/c07/c09/c11/c12/c13 承诺实质修复、c04 正向基线、接受 RT-20 partial 优先取舍、c01/c02/c05/c08/c10 诚实天花板）。三份 JSONL 逐行 JSON 校验通过、`confirmed_at=2026-07-24`；未运行任何 P1.5 dev、未改 src/Prompt、未触碰 P1 holdout（证据：本提交）
- [ ] U3.2 P1.5 验收时人工复核代表性回答与完整轨迹
  - 判据：至少覆盖 full、跨轮补证后 partial、假拒答被修正为 partial、policy-refusal、fail-fast 404 各一种；逐引用打开真实路径/行号；核对 mode/正文/not_found/limitations 一致；结果写入 `evalsets/reports/p1.5-manual-check.md`
- [ ] U3.3 P1.5 holdout 运行授权
  - 判据：确认实现/Prompt/阈值已冻结、被测系统自 dev Gate 起零变更后，用户明确同意才运行一次 v1.5 holdout；不据结果调参

## T22 证据类型硬约束与权威性分层（RT-01/05）

- [x] T22.1 required-evidence 结构化模型
  - 确定性代码从用户问题解析被明确要求的证据类型/路径（生产源码/配置/迁移/引用某类/测试等），构造 required-evidence 清单；plan/evaluate 结构化输出增加逐项覆盖状态字段（不新增在线调用点）
  - 判据：schema 严格 Pydantic、`PROMPT_VERSION` 递增并快照；解析以确定性规则为主，plan 只可回显确认且每个 required 项须锚定问题原文片段，LLM 不能覆盖或伪造 required 清单；解析对"请引用 X 与 Y""不要用测试代替实现"等表述有确定性单测；解析规则不得只针对回归集单句特判，须过"自然问法"反例
  - ⚠️ 2026-07-24 第二轮复审再次撤销：首版整改仍有 4 处可复现缺陷（子句切 `.` 截断显式路径→错误 full、裸文件名子串误配、否定整句级删除正确生产要求、type-only anchor 非原文 + 无序 set 决定 item_id + 类名误判 migration）。已在本提交二次整改（见偏差记录 2026-07-24 T22 二审）并用原始场景 + 跨 PYTHONHASHSEED 自验，**待第三轮独立复审确认后再勾选**
  - ⚠️ 2026-07-25 第三/四轮复审：二次整改（fc16b66）经第三轮又发现 5 处组合错误 full（三审），三次整改（a6583f8）经第四轮再发现窄修正则治标不治本——改采**结构性 fail-closed**（用户裁决）
  - ⚠️ 2026-07-25 第五轮复审撤销：三态方向确认正确，但"解析漏检必进 unresolved"未成立——`_extract` 按整段判定，一个目标命中即吞掉同义务其余目标（`请引用 Foo.java 和关键实现`→complete、c08"相关 Java 配置"被吞、c02 整题 none）；裸 README 退化 type-only 被任意 current_doc 顶替；"OrderTest 的生产实现"被判 test 再补泛化生产项；否定约束无消费者（死信号）；unresolved 说明只在单一 finalize 分支产生；required 可达 11 项却仍报 complete
  - ✅ 2026-07-25 **约束跨度账本**重写（本提交，按五审 7 条路线裁决）：义务跨度独立检测（强指令/弱指令/**核验比较谓词**，后者对象在标记左侧）→ 义务对象按连接词逐段结算为 resolved / unresolved / 明确非目标尾语，**不存在整段 produced**；裸 `README/PROGRESS/CHANGELOG` 保身份、`相关 Java 配置` 直接 unresolved；点名类的显式生产语境优先于 `*Test` 命名约定；`MAX_REQUIRED_ITEMS=10` 与 plan/evaluate schema 同源、超限记 `partial_enumeration`（有防漂移单测）。自查另补：否定列表跨顿号不得截断、`引用` 名词用法不得当祈使指令
  - ✅ 2026-07-25 第六轮独立复审裁定"T22.1 可以验收"，据此勾选：解析层六处发现全部收敛，独立复跑解析/schema/state 单测 129 passed、Prompt/契约/answer 单测 9 passed、Ruff+Pyright 通过；`PROMPT_VERSION`=p1.5-agent-v3（快照 SHA `6c838462…`）、严格 Pydantic、anchor 全为原文子串、自然问法反例通过。证据 commit：3576bf0（约束跨度账本）
  - ✅ 2026-07-25 第四轮结构性重写（历史记录）：解析产出**确定性三态** `RequiredEvidence.status`（none/complete/ambiguous）；漏检/部分解析/未知语法目标进入 `unresolved_constraints`（原文 anchor + reason 枚举 unparsed_target/partial_enumeration/unsupported_syntax），永不进 coverage、不伪造 type=other item。路径改 ASCII 边界 file-token 遮蔽（无空格中文不吞、`.txt/.sql/.vue/.ts/.tsx` 齐全、未知 ext→unsupported_syntax）；逗号/顿号列表按同指令延续解析；单词类名/`X 类`/URLParser 可解析、泛化角色词(Repository/DTO)→unresolved；强/弱指令作用域（弱指令须邻接证据词）；匹配大小写敏感（ext4）。对抗单测 + §五.A 图级 8 反例 + 跨 5 PYTHONHASHSEED 不变量
- [x] T22.2 full/sufficient 逐类硬约束
  - 判据：每个必需证据类型都有直接匹配引用才允许 `full`；缺任一必需类型时确定性最高判 `partial`；测试/设计/dev-log 不占用必需生产证据预算；Fake 矩阵覆盖"仅测试代替生产→不得 full""必需类型齐全→可 full"
  - ✅ 2026-07-25 收尾勾选（用户 2026-07-25 指示"先把 T22 完全收尾"）：六审唯一阻塞项（双引用账本）已在 b5de64c 修复，本次收尾按判据逐条自查复核——①先在 b5de64c 上复跑六审原场景（"不要引用设计文档，只引用生产源码" + 正文 `[E1][E2]` + claim 仅 E1）确认已判 `partial` + `finalize: 引用了被禁止的证据类型` + 用户可见说明；②另跑 10 条 finalize 硬门对抗探针（仅测试代替生产 / dev-log 冒充生产 / 两点名类只覆盖一个 / 配置类点名给 .md / 越界 `[E9]` / 禁引类型只在 claim / 只在正文 / 必需齐全应 full）全部符合判据，无错误 full；③补齐判据字面要求的 Fake 矩阵两条替代表述用例（"不要用测试代替生产源码"+仅测试证据→partial、"请引用生产源码…不要用测试代替"+生产证据→full）。`make ci`（ruff/pyright 0、pytest 440）、`make eval-ci`（65+19）全绿。**诚实标注：本轮为我方自查复核，非第七轮独立复审**；证据 commit：b5de64c + 本提交
  - ⚠️ 2026-07-24 第二轮复审再次撤销：finalize full 门虽逐项，但 `route_after_evaluate` 未读确定性覆盖——必需项未覆盖却 LLM 判 sufficient 时会跳过补检直奔 generate（发现4）；已在本提交补齐（待第三轮复审）
  - ✅ 二次整改后（本提交）：`route_after_evaluate` 增加确定性覆盖缺口判定——有 required 缺口且预算允许时优先 refine（即便 sufficient），full 门/refine 缺口/finalize 全部只读确定性覆盖。图级测试：覆盖缺口先补检再降级 partial、LLM 漏报 required/coverage 记诊断但确定性 full 不受影响
  - ⚠️ 2026-07-25 第六轮复审：解析层已合格，但 full 门仍有 1 处可稳定复现的错误 full——`finalize` 丢弃 `apply_l0` 返回的正文有效引用编号，禁引检查只看保留 claim 的 `evidence_ids`；而 L0/L1 不要求正文 `[E#]` 出现在 claim 中。复现：问"不要引用设计文档，只引用生产源码"，正文写 `[E1][E2]`、claim 只申报 E1 → verification 通过、正文保留 `[E2]`、`final_mode=full`、无禁引告警。**T22.2 暂不验收**
  - ✅ 2026-07-25 双引用账本修复（本提交，最小整改、未动解析器）：`required_evidence_tail(required, support_citations, visible_citations)` 明确拆两套账本——必需证据覆盖只认保留 claim 的引用（正文裸标记不得反过来满足点名要求），禁止引用检查认"claim 引用 ∪ 正文过 L0 后仍保留的 `[E#]`"；`generate_failed` 分支的正文也补跑 `apply_l0`，越界标记不随降级路径漏出。新增 2 条图级测试（正文含 E2/claim 仅 E1 → partial + 告警；可见标记不得覆盖必需项）与 2 条 tail 单元测试，前者在 3576bf0 上确认失败
  - ⚠️ 2026-07-25 第五轮复审撤销：硬门本身成立，但输入不可信——解析漏检使门真空化（c02 整题 none、c08 只剩 3 项）；`forbidden_substitute_types` 无任何消费者，"不要引用设计文档"同时引用生产源码与设计文档仍判满足
  - ✅ 2026-07-25 两类否定真正执行（本提交）：`forbidden_citation_types` 与 `forbidden_substitute_types` 分开落库并分开执行——`forbidden_citation_hits` 进入 `required_satisfied`（被禁类型被直接引用即不得 full）并写入 generate Prompt；"只能作为补充"/"不要用 X 代替 Y"/"不要只引用 X" 仍只禁替代、允许补充引用。`finalize` 抽出公共收尾 `required_evidence_tail`：覆盖缺口/unresolved/禁止引用三类说明对 draft is None、generate_failed、claim 被移除、正常四条分支统一生成
  - ✅ 2026-07-25 第四轮结构性 fail-closed（历史记录）：`full` 必要条件 = `required_satisfied`（resolved 全覆盖 **且** 无 unresolved）；`route_after_evaluate` 把 unresolved 也当覆盖缺口优先 refine（anchor 进补检查询）；finalize 有 unresolved 时最高 partial（保留 claims + 确定性限制说明，不静默降级）。§五.A 8 条图级反例（否定跨句/逗号枚举/单词类名/`.txt`/大小写伪 test/大小写匹配/resolved+unresolved）全绿
- [x] T22.3 权威性分层
  - 判据：区分 current docs / production source·config / tests / historical plans·audits / dev-log；规划材料只支撑"规划过/范围状态"、不支撑"已实现"；分层规则有单测；具体权重变更须经 Evaluation-v1.5 对照，不凭个案
  - ⚠️ 2026-07-25 第三轮复审撤销：`classify_path` 仍用 `base.startswith("application.")`——`application.md/.java/.txt` 被误判 production_config，与"只匹配配置扩展名"声明矛盾，测试证据可凑 full（三审发现3）；已在本提交删除裸前缀（仅按 `.yml/.yaml/.properties` 扩展名），补 3 个负例，待第四轮复审
  - ⚠️ 2026-07-25 第四轮复审撤销：`base.endswith("test.java")` 在 lower() 后伪命中——`src/main/.../Contest.java`、`Latest.java` 被判 test，只引生产 Contest.java 满足"对应测试"→错误 full（四审发现5）
  - ⚠️ 2026-07-25 第五轮复审撤销：文件名启发式仍先于目录判断——`src/main/java/AuditService.java`→historical_plan、`ChangelogService.java`→dev_log、非 src/main 的 `Testimony.java`/`Testing.java`→test，生产文件可满足对应非生产 type-only 要求并放行 full
  - ✅ 2026-07-25 第六轮独立复审裁定"T22.3 可以验收"，据此勾选：分层规则（可信目录 > 生产目录 > 文档专属文件名约定 > 词边界 Test 约定）有 40+ 条参数化单测，生产文件不再冒充 test/historical_plan/dev_log；权重未凭个案变更（检索层 metadata 权重仍属 P1.6）。证据 commit：3576bf0
  - ✅ 2026-07-25 先目录/扩展名后文件名（3576bf0）：audit/changelog/plan/review/roadmap 等**文件名**约定只作用于文档扩展名（`.md/.txt/.rst/.adoc`）；`Test*` 改词边界规则 `^Test(?![a-z])` / `[a-z0-9]Tests?$` / `(?<![A-Z])ITs?$`——Testing/Testimony/SPLIT/Contest/Latest 不是测试，`TestOrderService`/`FooSpec`/`AuditIT`/`OrderTest`(非生产目录) 仍是；13 条参数化负例
  - ✅ 2026-07-25 第四轮结构性重写（历史记录）：`classify_path` 明确目录优先级——可信 test 目录 > 生产目录(src/main) > `*Test/*Tests/*IT/*Spec` **大小写敏感**命名约定；`Contest/Latest`(main)→production_source、`OrderTest.java`(main)→production_source、`src/test/Order.java`→test、根级 `OrderTest.java`→test；后缀 lexer 与 `SUPPORTED_SUFFIXES` 对齐并有防漂移单测
  - ✅ 2026-07-24 `classify_path` 可信目录优先（test/design/plans/review·audit/dev-log 先判，再在生产目录内识别 source/config/migration）：`src/test/**.sql`→test、`docs/**flyway*.md`→historical_plan。冲突矩阵单测；`PRODUCTION_TYPES` 保证测试/设计/规划不替代生产

## T23 not_found 四分类与事实校验（RT-02/15）

- [x] T23.1 四分类枚举与三类实现
  - 判据：枚举 `missing_from_current_evidence`/`unsupported_or_not_ingested`/`confirmed_undocumented`/`forbidden_or_unavailable_by_policy`，以 Answer JSON 平行字段 `not_found_details` 承载（`not_found` 保持 `list[str]`，见规格 §2）；P1.5 实现前二类 + 策略类（后者见 T27）；`unsupported_or_not_ingested` 由 `SUPPORTED_SUFFIXES` + documents 表确定性判定（含有限且单测覆盖的技术词→后缀映射表），措辞用条件式、不得断言仓库有无该文件（仓库级普查属 P1.6）并有单测；`confirmed_undocumented`/全局不存在需 inventory，P1.5 不得断言，降级为"当前证据不足以穷举确认"
  - ✅ 2026-07-25 新增 `src/devkb/agent/not_found.py`（确定性、零 LLM 调用）：四类枚举齐备，`NotFoundDetail` 严格 Pydantic（text/category/source/**basis**+**refs** 为事实校验来源/original_text 留档），经 `AgentState.final_not_found_details` 落 Answer JSON 平行字段 `not_found_details`；`not_found` 仍为 `list[str]` 且与 details 逐条一一对应（有单测）。P1.5 只产出前二类：`confirmed_undocumented` 有"永不产出"单测，策略类留 T27。`unsupported_or_not_ingested` = `SUPPORTED_SUFFIXES` + documents 表快照（`CorpusProfile` ← `DocumentRepo.list_active_rel_paths`，一次只读查询、硬上限 `MAX_CORPUS_PATHS=5000`、未取得快照即 fail-closed、"未命中"永不作结论）+ **9 条封闭技术词→后缀表**（表外词如 Python/Kotlin/Rust 不推断，有负例）；另加"隐藏文件恒不在摄取范围"与"包名 `com.example` 不得被当作 `.example` 后缀"两条词法防御。措辞固定条件式（"不在当前摄取范围、未纳入本项目索引…无法据此确认仓库是否包含此类文件"），有"不得出现仓库中没有/仓库无源码"的单测；全局否定/穷举断言降级为"当前证据不足以穷举确认"。`PROMPT_VERSION`→p1.5-agent-v4（generate 增 not_found 条件式措辞指令，快照 SHA `96242198…`）、`AGENT_STATE_SCHEMA_VERSION`→v5、`ANSWER_SCHEMA_VERSION`→p1.5-answer-v2。取舍见偏差记录 2026-07-25 T23 分类锚点（用户 2026-07-25 已裁决采用目标级锚定）
  - ⚠️ 2026-07-25 复审未通过（拆分逻辑两处阻断点）：①同原因多目标只保留 `members[0].refs`——"数据库迁移、OrderService 与 PaymentService"里 `PaymentService.java` 从 refs 与告警中消失；②单字连接词在任意位置匹配，"涉及/或者/与此同时/参与"被误切并把无信号残段挪到错误位置（"数据库迁移的字段定义未找到涉…"）。已在本提交修复（见下条）
  - ✅ 2026-07-25 **一条一原因**（用户裁决后补强）：`_assess` 逐目标段结算，单条明细的 `category`/`basis`/`refs` 恒自洽；LLM 把两类缺口写成一句时按连接词确定性拆分（括号内不拆、确定性说明不拆、同类不拆）。复审两处阻断点已修：同原因成员的 refs **全量合并**（文案最多列 3 条 + "等 N 条"，refs 与告警不截断）、单字连接词（及/与/或/和）加左右封闭屏蔽表且句首/句尾不算连接处、无信号残段并入**紧邻前一段**（保序，不再挪到首条）。测试补至 55 条：拆分正确性 + `category`↔`basis`↔`refs` 自洽 + **文本中每个目标都有对应事实来源** + 自然连接词不切 + 真连接词仍切 + 保序与残段归位 + 不过度拆分
  - ⚠️ 2026-07-25 第三轮复审仍未通过（屏蔽表路线被否）：①"或者"被当成不可切复合词，`未找到数据库迁移或者 DocumentService 的删除实现` 只产出一条 `corpus_index`，`.sql` 的 `unsupported_or_not_ingested` 整条消失 = 复现 c10 型分类混淆；②屏蔽表覆盖不到自然词汇，`提及/与否/与此同时/同时` 仍被削首字或错位（`提 DocumentService 的说明`、`此同时 DocumentService`、`实现否`、`数据库迁移同时`）。裁决：改为**基于两侧目标信号确认连接位置**，不得继续扩充单字屏蔽表。已在本提交修复（见下条）
  - ✅ 2026-07-25 **目标锚定切分**（第三轮复审后重写）：删除 `_CHAR_CONNECTORS` 屏蔽表，切点改由目标信号定位——`iter_target_spans`（复用 T22 词法：路径 token/符号 token/规范文档名）+ 隐藏文件 + 封闭技术词构成"目标"，切点**只可能出现在两个相邻目标之间**，且只认两种可证情形：括号外的标点（中文标点不可能出现在词内，取最左一个，其后的从句措辞随之留给后一个目标）、或"整个间隙就是一个连接词"（以及/或者/与/及/或/和）。其余一律不切：同条内按优先级取唯一原因、原文完整保留，被压住的未摄取信号进 warnings（`混合未摄取格式信号但无法确定性拆分`），不静默消失。副作用同步修掉：`_EDGE_TRIM` 不再把"与/及/或/和"当边界字符盲删（"与此同时 X"被削成"此同时 X"的第二成因），残段归位逻辑随之整体删除（每段必含目标，结构上不再产生无信号残段）。测试 65 条（+10 红测：`或者`混合原因、四例自然措辞不被破坏、端到端原文完整性、无法判定时保守不切且告警），另在 `test_evidence_types.py` 加 2 条"目标区间与 token 词法同源"防漂移测试。`make ci`（ruff/pyright 0、pytest 509）、`make eval-ci`（65+19）全绿
- [x] T23.2 全检索轮历史事实校验
  - 判据：出现在任一轮证据路径的 active 文件不得被写成"不存在"；前轮已支持方面不得在后轮凭空升级为缺失；用户问题未给出的路径/类名/字段名与"源码不存在"强断言必须有证据支撑，否则只陈述当前证据不足；Fake 矩阵含"前轮出现过该文件→不得报缺失"
  - ✅ 2026-07-25 `evidence_path_history`（每轮 retrieve 累积，reducer=`operator.add`，被后轮挤出也留痕）与 `supported_aspect_history`（每轮 evaluate 累积）进 AgentState；finalize 对**所有**终态分支统一跑 `calibrate_not_found`：①任一轮证据里出现过、或 documents 表中 active 的路径被写成缺失 → 整条改写为确定性事实句（主语用去缺失标记后的方面名，"未找到/不存在"字样消失，方法级缺口信息保留）+ 告警 + `original_text` 留档；②无证据支撑的仓库级否定强断言 → 降级条件式；③前轮已支持方面在后轮被报缺失 → 剔除并告警。确定性 required-evidence 说明只追加披露、不被改写。测试：36 条 unit（分类/事实校验/边界）+ 4 条图级 Fake 矩阵（判据要求的"前轮出现过该文件→不得报缺失"、c10 型"迁移 .sql 未摄取 vs 已索引 Java 未召回"分类分离、前轮已支持不得翻转、Answer JSON 平行字段）+ 2 条集成（真实 PG：`list_active_rel_paths` 只含 active/受限/跨项目不可见；service 注入语料快照后 `corpus_index` 与 `static_suffix_rule` 两类同时出现）。改动前行为已在 2716c3f 上跑出原始输出留证（"源码中不存在 RagService"原样交付、两类缺口不可分、前轮已支持方面照报）。`make ci`（ruff/pyright 0、pytest 482）、`make eval-ci`（65+19）全绿

## T24 跨轮覆盖单调性与 partial 保留（RT-16/20）

- [x] T24.1 证据方面（aspect）分解与逐方面最佳保留
  - 判据：用户要求拆为 aspects，跨轮逐 aspect 保留最佳证据并记录淘汰原因；最终充分性按单调覆盖矩阵判定；某 aspect 由"已支持"退为"缺失"必须有"原证据被证伪"理由，仅被新轮挤出不算
  - ✅ 2026-07-26 第七轮独立复审裁定「T24 可以验收」，据此勾选：方面分两轨（T22 每条 required item + 各轮 evaluate 自报），跨轮按代表证据联合求解保留（确定性轨代表绝对优先、诊断轨代表给新证据让位、只丢弃不重排），淘汰逐条记入 `AgentState.evidence_eliminations` 与 retrieve 轨迹；`supported` 只增不减、`present_now` 按「某一轮的 chunk_id 绑定集合完整保留」判定，P1.5 无证伪机制故矩阵不存在降级路径。证据 commit：0c0eb36→f243c86→bb1242c→faf43b0→18beb85→e1e42bd→**734cd2d**（六轮复审整改，见偏差记录一～六审）
  - ⚠️ 2026-07-25 第一轮复审撤销勾选（3 处发现，见偏差记录 2026-07-25 T24 一审）：①P1 保留算法把"全部 fresh 覆盖了哪些方面"当既成事实，预留旧锚点又缩短实际入选的 fresh 前缀 → 新旧两条 RagService 一起被截，容量本来够；②P1 `has_deliverable_content` 把"历史已支持"当"当前可交付"，且同一方面既告警"被挤出、非证伪"又在 not_found 里被写成"未取得"；③P2 `EliminationRecord` 只在 retrieve 内生成后丢弃，运行结束无法逐条审计，"记录淘汰原因"名不副实
  - ⚠️ 2026-07-26 第二轮复审撤销（3×P1 + 2×P2，见偏差记录 2026-07-26 T24 二审）：①两轨新证据代表被合并进同一个桶再按召回排名截断，诊断轨代表会把确定性轨代表挤掉；②evaluator 方面的 `present_now` 用「本轮是否又自报一次」代理，证据未变时 LLM 仍能撤销历史支持；③「RagService 生产源码」这类带限定词的缺失项与三态说明并存（归一化全等比较建立不了跨轨身份）；④淘汰记录落 step summary 时被 `MAX_SUMMARY_ITEMS` 截成 8 条，>8 无法回放；⑤新增 state 字段未递增 `AGENT_STATE_SCHEMA_VERSION`
  - ⚠️ 2026-07-26 第三轮复审撤销（2×P1，见偏差记录 2026-07-26 T24 三审）：①诊断轨绑定按"路径交集"判定，绑定集合只剩任意一条（哪怕是无关文档）也算仍被支撑，且同文件不同 chunk 被当成同一条证据；②跨轨吸收只数"匹配到几个 required id"，会把方法级缺口与未绑定的第二目标一起静默删除
  - ⚠️ 2026-07-26 第四轮复审撤销（1×P1 两处同源边界，见偏差记录 2026-07-26 T24 四审）：①`iter_target_spans` 不等于完整的原文目标计数——T22 还能从"测试/配置/迁移文件/设计文档"这类**类型词**解析出 type-only 目标，它们不进 spans 又恰好落在限定词白名单里，"RagService 和测试"整条被吸收；限定词与绑定 item 的类型不相容时同样被吸收（required=生产源码、缺口=测试）；②"纯身份"判定读的是被 `strip_absence_markers` 截到 60 字的文本，500 字缺口把方法级语义放在第 60 字之后即可骗过判据
  - ⚠️ 2026-07-26 第五轮复审撤销（1×P1 + 1×P2，见偏差记录 2026-07-26 T24 五审）：①**句末标点被误判为协调连接词**——`_CONNECTOR` 含 `，,；;` 且对整段 remainder 任意搜索，并未落实注释所说的"目标两侧"；`RagService 生产源码，`（ShortText 未禁止句末标点，属合法 LLM 输出）因此不再被吸收，同一方面的"缺失"与"已取得后被挤出"三态说明重新并存；②P2 清单把本轮整改误标为"三审整改（本提交）"，与下一行"三审整改（历史记录）"重号
  - ⚠️ 2026-07-26 第六轮复审撤销（1×P1，见偏差记录 2026-07-26 T24 六审）：**"背离目标一侧有实词"不等于"存在第二目标"**——范围前缀也被判成并列对象，而它们正是 T23 `_ORPHAN_PREFIXES` 明确要剥离的限定。最小等价对：`证据中 RagService 的生产源码` 吸收、`证据中，RagService 的生产源码` 不吸收；7 个 orphan 前缀全部稳定复现这种 plain=True / comma=False 差异，displaced R1 的矛盾文案重现
  - ✅ 2026-07-26 六审整改（734cd2d，先红后绿）：`_has_coordinator` 判"背离目标一侧"时改用 T23 同一个 `strip_absence_markers` 归一化——去掉可剥离的缺失措辞与范围前缀后仍有实词才算并列对象（`证据中，RagService 和测试` 仍不吸收），只剩标点/空白即句首句末标点或范围前缀（吸收）。新增**变形不变量**红测：7 个 orphan 前缀 + 11 个 absence 标记 × 7 种标点 = 126 例插入后吸收结果不得改变；另加图级断言锁定"证据中，RagService 生产源码"终态只有一条 RagService 说明。去掉本轮 src 改动后新测 74 条全红、恢复后 199 条全绿。探针补第二条不变量（可剥离前缀变形 126 种 × 已吸收基例）：翻转 0
  - ✅ 2026-07-26 五审整改（历史记录）：连接词判定改为**左右两段分别判、只看背离目标的一侧**——该侧去掉标点后仍有实词才算并列（`RagService、设计文档` 不吸收），只剩标点即句首/句末标点（`RagService 生产源码，` 吸收）；新增 `_has_coordinator` 与 `_PUNCT_EDGE`。6 条红测（`，` `,` `；` `;` 四种句末形态 + 句首标点 + 句末句号回归）在 18beb85 上确认前 5 条被判"不吸收"；探针扩到 182400 组合（新增 8 种句末标点形态）0 违反、吸收 11960 条。②按裁决改正标号
  - ✅ 2026-07-26 四审整改（历史记录）：吸收判据改为**三道 fail-closed 闸门**——①目标计数按 token 逐个核对身份（新增 `target_tokens_all_match`；只数跨度会被 `merge_spans` 的相接合并骗过，`RagService.javaOrderService` 两个符号只剩一段）；②目标两侧出现协调连接词（与/和/及/或/、/逗号/以及/或者）一律不吸收，连接词不再被当噪音抹掉；③残余限定词必须落在**按证据类型分表**的 `_TYPE_QUALIFIERS` 里（生产源码不接受测试/配置/迁移/设计文档）。`strip_absence_markers` 不再截断，60 字上限只留在用户可见的 `_subject` 渲染。7 条红测在 faf43b0 上确认被吸收；22800 组合的忠实探针（绑定类型取自真实问题解析，交叉核验"吸收 ⟹ T22 读这条文本时没有第二个目标"）0 违反、仍吸收 1375 条
  - ✅ 2026-07-26 三审整改（历史记录）：绑定改**逐轮分组 + chunk_id 身份**——`EvidenceRef` 携带稳定 chunk_id，`AspectObservation.chunk_ids` 按轮记录绑定集合，`present_now` 要求**某一轮的绑定集合完整保留**（"还剩一条"与"同文件另一 chunk"都不算）；吸收增加"纯身份缺口"判据——整句只含一个目标（按 T22 `iter_target_spans` 计数，不看它是否命中 required item）且去掉目标、复用 T23 `strip_absence_markers` 剥掉缺失措辞后只剩封闭限定词表内的词，方法级/行为语义与第二目标一律原样保留。12 条对抗探针逐条核对；`AGENT_STATE_SCHEMA_VERSION`→**v8**（AspectObservation 字段语义变更）。另修两处注释漂移（`plan_retention` 资格顺序、evaluator `present_now` 口径）
  - ✅ 2026-07-26 二审整改（历史记录）：保留改**按轨发资格再排序**（确定性轨代表 → 本轮新证据里的诊断轨代表 → 补检至少一条 → 旧轮诊断轨代表 → 其余新证据 → 其余旧证据，同来源内次序不变）；evaluator 观察记录**稳定证据绑定**（标签 token 命中则绑定命中路径，否则绑定该轮全部证据），`build_matrix` 改按「绑定证据 ∩ 当前证据集」算 `present_now`，末轮不再能凭「这轮不提了」撤销历史支持；`bound_required_ids` 建立跨轨目标身份，唯一绑定到**被挤出态**必需项的 LLM 缺口交给确定性说明；step summary 按 `MAX_ELIMINATION_RECORDS` 落盘并补持久化映射单测；`AGENT_STATE_SCHEMA_VERSION`→**v7**。两轨随机探针 4000 例（新增确定性轨绝对优先、诊断轨在本轮有代表时不被饿死两条不变量）：整改前 43 例违反、整改后 0
  - ✅ 2026-07-25 一审整改（历史记录）：保留算法改**代表证据联合求解**（逐方面先选代表：本轮新证据优先、否则旧轮最高排名；确定性轨代表**绝对优先**占位，诊断轨代表才给新证据让一个位；非代表按原排名填剩余槽位，**只丢弃不重排**）；淘汰记录进 `AgentState.evidence_eliminations`（只增账本，硬上限 `MAX_ELIMINATION_RECORDS=2×MAX_FINAL_TOP_K`）并写入 retrieve 的 step 轨迹（chunk_id/rel_path/aspect_ids/reason，可逐条回放）。随机化不变量探针 4000 例（容量/去重/两来源各自保序/确定性方面不被饿死/淘汰记录完备且 reason 正确/跨次确定性）：修复前 **1057 例违反**（含我未发现的 carried 重排），修复后 **0**
  - ✅ 2026-07-25 首版实现（历史记录）：新增 `src/devkb/agent/aspects.py`（确定性、零 LLM 调用）：方面分为**确定性轨**（T22 每条 required item = 一个方面，身份是问题原文锚点）与**诊断轨**（各轮 evaluate 自报 supported_aspects）；`AgentState` 以只增账本 `aspect_observations`（reducer=`operator.add`）替代 `supported_aspect_history`（同一事实不再存两份，T23 的"前轮已支持方面"改由矩阵折出，输入口径不变）。跨轮保留：`plan_retention` 在合并时**先为"旧轮锚定、本轮未覆盖"的方面预留槽位**再填新证据（本轮内不重排，避免改动 `E#` 编号），同方面最佳证据 = 旧集合中排名最高的一条；锚点最多占 `limit-1` 槽位，被容量截断的逐条产出 `EliminationRecord`（`capacity_limit` / `aspect_anchor_capacity`），后者带 `limit_reached` 标记使轨迹记 degraded。单调性：`supported` 只增不减，`present_now` 才反映当前证据集，二者之差在 finalize 逐条告警"被挤出，非证伪"（P1.5 无证伪机制，故矩阵不存在降级路径）。**充分性口径（请复审重点核对）**：存在确定性轨时由 `is_monotonically_sufficient`（全部方面跨轮已支持 + 末轮无"从未被支持"的 missing）接管，不再看末轮扁平 `sufficiency`；无确定性轨时仍沿用末轮 evaluate 自报（不把纯 LLM 报告升格为权威）。矩阵**只能阻止不当降级、不能放宽 full**——T22 的逐项引用硬门、unresolved、禁引三道门不变，有图级反例锁定"跨轮已支持但终稿未引用 → 仍 partial"。测试 23 条（单元 12 + 图级 11，含跨 5 个 PYTHONHASHSEED 一致）
- [x] T24.2 partial 保留优先于全量 refusal
  - 判据：≥1 aspect 有直接证据即确定性交付范围准确 `partial`；`refusal` 仅用于零可交付证据；Fake 矩阵精确复现案例九（第一轮 support=2、补 CitationParser 时勿丢 RagService；即使仍缺 RagSupport 也交付 partial 而非全空）
  - ✅ 2026-07-26 第七轮独立复审裁定「T24 可以验收」，据此勾选：`has_deliverable_aspect` 判 `supported and present_now`，≥1 方面当前仍有直接证据即交付范围准确的 partial，`refusal` 只用于零可交付证据；缺口措辞按三态分开（从未取得 / 仍在证据集但未引用 / 前轮取得后被容量挤出），能唯一绑定到被挤出必需项的纯身份缺口交给确定性说明，同一方面不再出现互相矛盾的两句话。案例九 Fake 矩阵回放为 partial + 保住 RagService。证据 commit：同 T24.1（末次 **734cd2d**）
  - ⚠️ 2026-07-25 第一轮复审撤销勾选（与 T24.1 同批发现2）：可交付判定只看"证据集非空 + 历史有任一已支持方面"，未校验当前证据是否仍支撑该方面
  - ⚠️ 2026-07-26 第二轮复审撤销：T24.1 同批发现里的「诊断轨 present_now 语义」与「跨轨目标身份」两条直接决定本项判据
  - ⚠️ 2026-07-26 第三轮复审撤销：T24.1 同批两条 P1 直接决定本项判据（可交付判定的绑定口径、缺口文案的吸收范围）
  - ⚠️ 2026-07-26 第四轮复审撤销：T24.1 同批 P1 直接决定本项判据（缺口文案的吸收范围）
  - ⚠️ 2026-07-26 第五轮复审撤销：T24.1 同批 P1 直接决定本项判据（合法句末标点使矛盾文案复现）
  - ⚠️ 2026-07-26 第六轮复审撤销：T24.1 同批 P1 直接决定本项判据（范围前缀 + 标点使矛盾文案复现）
  - ✅ 2026-07-26 六审整改（734cd2d，随 T24.1）：范围前缀与目标之间的标点不再触发 fail-closed，`证据中，RagService 的生产源码` 恢复被三态说明吸收；图级红测锁定终态只有一条 RagService 说明
  - ✅ 2026-07-26 五审整改（历史记录，随 T24.1）：句末标点不再触发 fail-closed，`RagService 生产源码，` 恢复被三态说明吸收；`和/、` 等真并列仍不吸收
  - ✅ 2026-07-26 四审整改（历史记录，随 T24.1）：吸收再收窄为"一个目标 + 无连接词 + 类型相容的限定词"，测试/配置/迁移/设计文档缺口不再被生产源码的三态说明吞掉
  - ✅ 2026-07-26 三审整改（历史记录）：可交付判定改按"绑定集合完整保留"，不再因"还剩一条无关证据"误判为仍可交付；吸收只作用于纯身份缺口
  - ✅ 2026-07-26 二审整改（历史记录）：证据未变时末轮 evaluate 撤回自报不再导致 refusal（新增端到端图级红测）；被挤出方面的矛盾缺口文案消除
  - ✅ 2026-07-25 一审整改（历史记录）：`has_deliverable_aspect` 改判 `supported and present_now`（当前仍有直接证据才算可交付；历史支持只用于"不得被改写成缺失"的单调下界）；`required_evidence_tail` 的缺口措辞按复审要求拆成**三态**——从未取得 / 仍在证据集但未被引用 / 前轮取得后被容量挤出（第三态明确写"本次无法作为引用支撑，覆盖按单调矩阵保留"，不再与"被挤出、非证伪"告警自相矛盾，仍保持 partial）；`calibrate_not_found` 的已支持标签改传**两轨**（确定性轨的单调性事实此前对 not_found 不可见）。用户 2026-07-25 裁决"诊断轨只提供 partial/refusal 的单调下界与缺口防回退、不得授予 full"已落到 `is_monotonically_sufficient`（显式只看确定性轨）并加复审给出的反例单测（无 required item 的双方面题不得因两轮各自报支持而判充分）
  - ✅ 2026-07-25 首版实现（历史记录）：`has_deliverable_content`（证据集非空 + 单调矩阵有已支持方面）成为**进入 finalize 的每条路径**的统一口径：`route_after_evaluate` 在末轮 evaluate 退化为 insufficient 时改走 generate；`route_after_refine` 同样（refine 解析/传输失败此前直奔整体拒答，是同型缺陷的另一条分支）。案例九图级复现：第一轮 support=2 → 补检 CitationParser 后 RagService/RagConstants 仍在证据集 → `partial` + 2 条 claim + 2 条 citation，`llm_calls=5` 在 D6 预算内；`NO_ANSWER`/`UNGROUNDED` 不再出现在 not_found，真正缺失的 RagSupport 如实披露。改动前在 d0f037d 上用同一脚本跑出原始输出留证：`mode=refusal`、`claims=[]`、证据集为 `[CitationParser, CitationParseResult, 架构设计.md]`（RagService 被挤出）、确定性说明把四个类全报成未取得。附带修正一处由本任务放大的自相矛盾：被跨轮保留但未被任何 claim 引用的必需证据，文案由"未取得"改为"已在本次证据集中，但未被任何断言直接引用"（与矩阵 `present_now=True` 一致）。`make ci`（ruff/pyright 0、pytest 532）、`make eval-ci`（65+19）全绿；`AGENT_STATE_SCHEMA_VERSION`→v6，Prompt 未改故 `PROMPT_VERSION` 仍 p1.5-agent-v4。**自查复核，非独立复审**（同 T22/T23 收尾口径）

## T25 节点一致性与 finalize 覆盖差分（RT-03/08）

- [x] T25.1 finalize 前确定性一致性检查
  - 判据：`mode`/正文/`claims`/`not_found`/`limitations` 相符；正文声称已证明的内容不得同时出现在 `not_found`；finalize 不得无事实校验注入 evaluator 的 missing；冲突用例有确定性断言
  - ✅ 2026-07-27 GPT 终局复核裁定「未发现阻塞问题 / PASS」，据此勾选。逐条对照（**判据第 2 句按 2026-07-26 用户裁决 A 收窄，见文末偏差记录**）：①五字段一致性由 `finalize_consistency` 在 finalize 收尾前确定性校验，`full` 不与 `not_found`/空 claim 并存、`refusal` 不残留 claim 或正文 `[E#]`，且**只降不升**（`_MODE_RANK` 取最小）；②"正文声称已证明的内容不得同时出现在 `not_found`"**只在身份级确定性消除**——`not_found` 不得把本次交付的引用来源（E0 = claim 申报 ∪ 正文过 L0 的 `[E#]`，与 Answer `citations` 同源）写成缺失；**命题级关系仅作待核验披露，未消除、也未断言其为冲突**（确定性代码做不到，需 L2/NLI，D9/§14 禁止在线；能力随 P1.6 推进）；③evaluator 的 missing 不再无校验注入——`_with_citation_scope` 按 E0/E1（整条原始条目的全部可解析目标都在 E0）/E2（残余落在 T24 封闭限定词表且与 `classify_path` 类型相容）三道闸门判定，任一不成立整条 fail-closed，同一 token 解析到任何未交付路径也 fail-closed；④冲突用例有确定性断言：案例七（I1）保留原缺口语义 + 追加"未经确定性核验"披露句，禁用词表（含"已被支撑/直接支撑/不成立/存在冲突/相矛盾"）由 U14 在文案与 warning 上机器判定零命中。冻结测试 U1–U15 / I1–I7 全部落盘（U13 十态穷举、U4c 歧义路径回归由定向修复补齐），基线上先红后绿（19 条红测）。三个契约版本均未变（`src/devkb/contracts.py` 不在 diff 中）。门禁：`make ci`（ruff/pyright 0、pytest **757**）、`make eval-ci`（**76 + 19**）、`make verify-full`（**PASS**，8/8 文件在 `allowed_paths`）全绿，跨 5 个 `PYTHONHASHSEED` agent 相关 424 条一致。合同与全过程见 `docs/tasks/T25.1-finalize-consistency.md`。证据 commit：**5170a02**（实现）→ **9746a7c**（唯一一次定向修复，关闭首审 P1 `citation_scope` 歧义路径 + P2 U13 缺失）
  - ⚠️ 2026-07-26 首审代码审查 2 处发现（1×P1 + 1×P2，已在 9746a7c 关闭）：①**P1 `citation_scope` 一对多解析未 fail-closed**——token 只要匹配到任一被引用路径就通过，`citation_scope("未找到 README", cited_paths=("docs/README.md",), known_paths=("README.md","docs/README.md"))` 返回 `('docs/README.md',)` 并输出"已按引用事实更正"，但 `README` 同时解析到未交付的根级 `README.md`，证明不了缺口说的是哪一个；②**P2 冻结测试 U13 未落盘**（`grep -c "def test_u13"` = 0），只实现了 I2/I3/I7 三条代表路径
- [x] T25.2 第二稿覆盖与一致性复检
  - 判据：regen 后除重过 L0/L1 外，重做覆盖度与一致性检查；必要时准确降级而非交付错误缺口；Fake 矩阵复现案例二（重生成后 not_found 不得由 1 增至 2 且含 active 文件）
  - ✅ 2026-07-27 GPT 限定代码审查裁定「空 ledger / 未发现 P0、P1、合同内 P2 / PASS」，首审即终审（无定向修复，故不触发冻结复审），据此勾选。逐条对照（**判据第 3 句按 2026-07-27 用户裁决 A 收窄、跨稿引用比较按裁决 C1 删除、文件预算按裁决 D1 放宽至 9，三条见文末偏差记录**）：①**重做覆盖度检查**——终稿的 `monotonic_matrix`（`nodes.py:961`）与 `required_evidence_tail`（`nodes.py:1043`）既有承担，本任务新增的是**跨稿**那一层：`AgentState.first_draft`（新增 frozen/strict 的 `DraftSnapshot`，只含 `not_found` + `evidence_ids` 两字段）在首次 `generate` 写入、此后只读，使第一稿的自述缺口不再被第二稿静默覆写；②**准确降级而非交付错误缺口**——模块级纯函数 `regen_full_block` 按七行真值表判 P1（`generate_calls == 2`）∧P2（两稿证据集逐一相同）∧P3（第一稿声明过缺口而第二稿**原始** `draft.not_found` 为空），三者全成立即在 `nodes.py:1051` 一行否决 `full`。**只否决 + 发 warning，不新增、不删除、不改写任何一条 `final_not_found`**——"第一稿那条缺口仍然存在"推不出来（第二稿可能确实解决了它、只是没再声明），把它回填进用户可见清单等于用未经证明的话冒充事实；两稿都非空且不同时只披露条数并逐字声明"两稿逐条对应关系未作判定"；`generate_calls == 2` 而快照缺失这一类型合法态单列 fail-closed，只陈述"快照缺失"，**不发**"两稿证据集不同"这个无依据的断言；③**案例二复现**——m14 断言 1–3 由 `tests/integration/test_agent_service.py` 的 I1 绑定（**真实 PG + FakeLLM**，`mechanism_checks.md:11` 的执行层要求），参数化覆盖「篡改一字」与「跨 chunk 拼接」两种构造，经 `get_run_trace` 断言恰好一次重生成、首稿 `no_verbatim_match` 未过、第二稿 `passed` 与 `l1_passed` 均真且保留一条非空逐字 quote（防零 quote 空洞满足）。**按裁决 A 的口径核对，不按判据字面宣称"条数未增长"**：新增缺口指向 active indexed 但本轮未召回的 `table.md`，`basis == "corpus_index"`（证明 `documents` 表 → `service.py:156` → `AgentRuntime.corpus` 的真实接线）、`category == "missing_from_current_evidence"`、`original_text` 留档，终态文本不含"不存在/仓库无此文件/查无此文件"；I1 **显式断言 `len(not_found) == 2`**——条数确实由 1 增至 2 且被允许，删除它才是隐藏一个真实的召回缺口。冻结测试 U1–U18 / I1–I7 全部落盘（缺陷形态在基线上先红：场景 B 探针实测终态为 `full`），并做三次变异检验证明判别力（误传 T23 校准口径→只有 I6 失败；去掉否决条件→I2+I6 失败；「逐字相同」收窄为「两稿都空」→U17+U14 失败）。`AGENT_STATE_SCHEMA_VERSION` v8→**v9**（新增状态字段），另三个契约版本未变；`answer.py`/`not_found.py`/`trace.py`/`verification.py`/`service.py`/`graph.py` 零改动；**零新增 LLM 调用**（D6 四调用点与单 run ≤6 不变）。**残留（如实记录，非已证明）**：真值表行 5（两稿 `not_found` 逐字相同且均非空）的**图级侧无独立冻结用例**，由纯函数返回值配合实现形态推出，审查者判定可接受。门禁：`make ci`（ruff/pyright 0、pytest **781**）、`make eval-ci`（**98 + 19**）、`make verify-full`（**PASS**，8/9 文件在 `allowed_paths`）全绿，跨 5 个 `PYTHONHASHSEED` `tests/unit` 各 689 条一致。合同与全过程见 `docs/tasks/T25.2-regen-coverage-diff.md`。证据 commit：**d41abcb**（实现，唯一代码提交，首审零 finding 故无定向修复）

## T26 partial 认知口径与安全/配置覆盖矩阵（RT-18/19）

- [ ] T26.1 partial 正文限定已验证范围
  - 判据：partial 正文与总结只对已验证资源/操作下结论，不对未覆盖方面作绝对断言；复现案例七（不得一边承认两路径未确认、一边断言"无法访问任何资源"）
- [ ] T26.2 安全类 resource×operation×enforcement-layer 矩阵
  - 判据：安全题输出该矩阵，区分"Repository 语句自身过滤"与"Service 前置校验后调用非 owner-scoped 语句"；不混为统一双重防线
- [ ] T26.3 配置类分层校验
  - 判据：配置题分别验证 scope/fallback/required·non-empty/length·strength/runtime-validation；错误消息或文档建议不得冒充代码强制条件；复现案例八（Compose `${VAR:?}` 非空 ≠ secret 强度/生产安全）

## T27 policy-refusal 终态（RT-22）

- [ ] T27.1 前置识别与独立终态
  - 判据：明确索取系统 Prompt/凭据/隐藏文件内容或要求越权执行命令的请求，在检索潜在敏感语料与调用任何工具之前返回独立 `policy_refusal`（mode 第四枚举值，见规格 §2）；识别 = 确定性规则前置（命中零 LLM 调用）+ plan policy 标志兜底（不新增调用点，retrieve 前短路）；照常落一个终态 `policy_refusal` 的 run（零 tool invocation、记录触发层 rule/plan）以保可审计；固定无秘密文案；不检索、不生成、不执行、不编造；受保护对象不得写入普通 `not_found`
- [ ] T27.2 召回率与误杀反例
  - 判据：用多种攻击改写、直接索密、角色覆盖、文档内注入、要求编造/执行命令验证召回；用合法安全咨询（"如何保护 API key"）与普通 `.env.example` 文档题验证不误杀；分别报告泄露率、越权工具调用、策略漏拦与误杀

## T28 摄取覆盖披露（案例三/十廉价版）

- [ ] T28.1 refusal/partial 附静态覆盖披露
  - 判据：拒答/部分回答说明本项目摄取/不摄取的后缀，使"格式未摄取"可与"仓库确实无源码"区分；为静态能力披露，不做逐题语言/意图推断（属 P1.6）；披露文案有单测

## T29 unknown project fail-fast 顺序（RT-23）

- [ ] T29.1 project 解析先于模型初始化
  - 判据：输入校验与 `_resolve_project()` 先于 `_get_embedder()`/`_get_llm()`；unknown project 在任何模型/client 初始化前返回结构化 404、统一脱敏错误体、零 run/step/tool、零外部调用
- [ ] T29.2 冷服务 loader spy 回归
  - 判据：新增冷 AppService/API 测试，以 loader/factory spy 断言 unknown project 时 embedder/LLM factory 均 0 次调用；现有注入已实例化 Fake 的测试无法覆盖，须补 spy；保持 404/脱敏/零持久化

## T30 诊断轨迹最小版与 warning 归属（RT-07/10）

- [ ] T30.1 有界逐候选轨迹
  - 判据：每轮保存有界 per-query 候选元数据（chunk_id/rel_path/line range/rank/score/融合·截断原因），使"未召回/低排名被截断/被融合淘汰/已给模型但未采用"可事后区分；候选条数有冻结为常量的硬上限，不得无界；需要建表/加列时新增可逆 Alembic 迁移 0004；refine term 记来源；finalize 注入的 missing 记来源与事实校验结果；继续不保存正文与 secret
- [ ] T30.2 warning attempt/已解决区分
  - 判据：区分 `resolved_warnings` 与 final active warnings，并带 node/attempt；复现案例二/八（第二稿已过 L0/L1 时不得再把历史 `verify:l0_l1_failed` 当作最终未通过展示）

## T31 Evaluation v1.5 harness 与 dev Gate

- [ ] T31.1 harness 扩展与分环节统计
  - 判据：新增 not_found 分类准确率、跨轮覆盖单调性、finalize 一致性、policy-refusal 召回/误杀、fail-fast 断言；分开报告 retrieval / evidence-selection / claim-support / final-consistency，不用总分掩盖失败环节；并入 `make eval-ci`
- [ ] T31.2 v1.5 dev Gate 真实运行
  - 判据：真实 DeepSeek 在 v1.5 dev 上运行一次，13 条真实问答复现达到各自 **P1.5 契约预期**（两栏预期见《Evaluation-v1.5》§2）；固定复现 14–18 的机制检查在确定性层全绿；P0/P1 兼容回归与 L0/L1 继续全绿；报告落盘不覆盖旧结果，不在 dev 上为单题过拟合调参

## T32 P1.5 收尾与 Gate

- [ ] T32.1 README/文档口径更新
  - 判据：README 增加四分类 not_found、policy-refusal、覆盖披露与"能力边界诚实声明"口径；明确 P1.5 不具备仓库级审计/工具调用（属 P1.6）；无未实现能力措辞
- [ ] T32.2 P1.5 验收总清单
  - 判据：逐条核对《P1.5实现规格》§15；所有证据链接到测试、报告、run_id 或命令；无"计划中/待补"被勾选；所有偏差有用户裁决
- [ ] T32.3 v1.5 holdout（依赖 U3.3）
  - 判据：一次性运行并留档；不据结果回改 Prompt/阈值/权重/标注
- [ ] T32.4 当前头提交远端 CI 全绿
  - 判据：GitHub Actions 当前 P1.5 头提交全绿，不以较早提交代替
- [ ] T32.5 P1.5 Gate 关闭
  - 判据：用户确认 P1.5 验收；之后才允许创建 P1.6 实现规格/任务清单与 D6 窄修订 ADR

## P1.5 验收总清单

对《P1.5实现规格》§15 逐条核对（勾选前须全部满足）：

- [ ] 1. Evaluation-v1.5 的 13 条真实问答复现全部达到各自 P1.5 契约预期（P1.6 目标预期仅记录）；14–18 机制检查全绿
- [ ] 2. 不再把 active/indexed 文件断言为不存在；missing/not_found 不含未经证据支持的路径/类名/字段
- [ ] 3. 用户明确要求的证据类型未满足时绝不判 `full`；测试/设计/dev-log 不替代必需生产证据
- [ ] 4. mode/正文/claims/not_found/limitations 一致；≥1 方面有直接证据时交付范围准确 partial，不整体清空
- [ ] 5. Injection/秘密提取/越权命令进入独立 `policy_refusal`，无泄露/执行/敏感检索；合法安全咨询不误杀
- [ ] 6. 全局否定/穷举题给出诚实能力边界声明（不假拒答、不假断言"不存在"）
- [ ] 7. 格式未摄取导致的缺失标为 `unsupported_or_not_ingested` 并附覆盖披露
- [ ] 8. unknown project 在加载 embedder/LLM 前 fail-fast 404，loader spy 证明两 factory 均 0 次
- [ ] 9. 新 v1.5 holdout 只运行一次并留档，不据结果调参
- [ ] 10. P0/P1 兼容回归 + 本地 `make ci && make eval-ci` + 当前头提交 GitHub Actions 全绿；`PROMPT_VERSION` 递增已记录

**明确不作为 P1.5 出口**（属 P1.6）：Controller 穷举 6 类/26 endpoint、`.vue/.ts/.sql` 已摄取、动态工具调用可用。

## 偏差记录

| 日期 | 任务项 | 冲突/缺失描述 | 建议 | 用户裁决 |
|---|---|---|---|---|
| 2026-07-24 | T22 复审 | 首版 T22（commit 26777fd）经代码复审确认 4 处缺陷并撤销勾选，均可稳定复现：①硬门只比类型集合、丢弃点名路径/符号——点名 3 个类却引用无关生产文件仍判 `full`（错误 full）；②evaluate 逐项覆盖未实现、`RequiredEvidence` 为 dataclass 非严格 Pydantic；③`classify_path` 先判 `.sql/flyway/application` 后判可信目录——`src/test/.../*.sql`、`flyway-audit.md` 被判为生产 migration；④解析否定反转（"不要引用设计文档"→必需 design、"计划文档…规划过"→禁止 plan）与漏识别（"对应测试"）。附带规格张力：T22.1 判据"plan/**evaluate** 结构化输出增加逐项覆盖状态字段"与 D6"控制流由确定性代码判定"——逐项覆盖本质是确定性计算 | 整改：required-evidence 改逐项严格 Pydantic 模型（type+原文 anchor+可选 path/symbol，同类型多点名不合并），finalize 逐项匹配直接引用；`classify_path` 可信目录优先（test/design/plan/audit/dev-log 先判，再在生产目录内识别 source/config/migration）；解析按子句与否定范围重写并补冲突矩阵测试；删除"under-detect 仍安全"假设。evaluate 逐项覆盖：建议确定性代码为权威门、evaluate 可选报告仅驱动 refine（并存、确定性为准） | ✅ 2026-07-24 用户裁决：采用「A 的 schema + B 的确定性裁决」——plan/evaluate 按 A 增加逐项字段，但 RequiredEvidenceItem 与 coverage 的裁决权全在确定性代码，LLM 报告仅诊断/驱动查询，不得把 missing 改成 covered、不得增删 required item。已按此整改并用复审原始场景逐条重验（本提交） |
| 2026-07-24 | T22 二审 | 第一轮整改（commit a1f3f37）经第二轮独立复审仍确认 4 处可复现缺陷，再次撤销 T22.1/T22.2（T22.3 分类修复确认有效、保留）：①`_CLAUSE_SPLIT` 含英文 `.`、分句先于路径提取——`a/b/Foo.java` 被切成 `Foo`+`java`，显式路径退化 type-only → 引用 `NotFoo.java` 仍 `full`；且路径匹配用子串，裸 `foo.java` ⊂ `notfoo.java`；②否定作用域整句级——"不要用测试代替生产源码，请引用生产源码"把 production_source 一并加入 forbidden 再全局删除，required 变空 → 引用 `FooTest.java` 仍 `full`；③type-only anchor 用规范标签非原文、用无序 `set` 决定 item_id（PYTHONHASHSEED 敏感）、同句"数据库迁移"使 `DocumentService` 被 `_infer_symbol_type` 误判 migration；④"A schema + B 裁决"只落到 finalize——`route_after_evaluate` 未读确定性 coverage，必需项未覆盖却 sufficient 时跳过 refine；evaluate/plan 只查 LLM 已返回项，全省略时不告警 | 补红测后整改：分句不切 `.`；路径按完整后缀 / 裸名 basename 全等匹配；否定改子句内"用 X 代替 Y"（X 禁止、Y 肯定必需），禁止类型不删其它子句的 required；类型检测按原文位置排序产出 (type, 原文 anchor, span)、item_id 依位置稳定、点名类默认 production_source；`route_after_evaluate` 读确定性覆盖缺口优先 refine；plan/evaluate 完整回显校验、遗漏也记诊断 mismatch | ✅ 2026-07-24 已按整改二次修复（本提交）：4 处均补对抗单测 + 图级测试并用原始场景 + 跨 5 个 PYTHONHASHSEED 逐条自验（item_id→type 完全一致）；`make ci` 327、`make eval-ci` 65+19 全绿。**因勾选纪律 + 上轮曾误勾，T22.1/T22.2 暂不勾选，待第三轮独立复审确认** |
| 2026-07-25 | T22 三审 | 第二轮整改（commit fc16b66）经第三轮复审：二审 4 个原始用例已过，但**组合场景**仍稳定错误 full，撤销 T22.1/T22.2/**T22.3**（三者全撤）：①删英文 `.` 分句致否定跨句污染——"不要引用测试. 请引用 Foo.java" 整句判禁止，`Foo.java` 未成必需，仅引 `FooTest.java` 仍 full；②`_TYPE_PATTERNS` 漏"生产配置"——"请引用生产配置"→required 空→测试证据凑 full；③`classify_path` 仍 `base.startswith("application.")`——`application.md/.java/.txt` 误判 production_config；④非否定子句无条件抽 CamelCase——"消息队列（如 Kafka/RabbitMQ）"把 RabbitMQ 当必需点名类；⑤evaluate 只查 LLM 多报 evidence、漏报（covered=true 但 evidence_ids=[]）不记 mismatch | 先补红测再整改：路径 token 先遮蔽为无点占位符再按句界（含 `.`）分句、逐子句还原（否定停在句号）；`_TYPE_PATTERNS` 加"生产配置"；删 `application.` 裸前缀只按配置扩展名；点名硬约束限定在带"引用/根据"指令的子句（替代结构 Y 侧强制提取）；evaluate 覆盖用 evidence 集合全等比较 | ✅ 2026-07-25 已按整改三次修复（本提交）：5 处 + 自查发现的"替代 Y 侧裸文件名漏必需"共补对抗单测 + 图级测试，用三审原始场景 + 跨 5 个 PYTHONHASHSEED 逐条自验；`make ci` 339、`make eval-ci` 65+19 全绿。**T22.1/T22.2/T22.3 全部暂不勾选，待第四轮独立复审确认** |
| 2026-07-25 | T22 四审 | 三次整改（commit a6583f8）经第四轮复审：二审用例仍过，但**组合场景**又 5 处稳定错误 full——①遮蔽正则用 Unicode `\w` 吞无空格中文（"不要引用测试.请引用Foo.java"→required 空→full；"Foo.java和Bar.java"被吞成一个）；②逗号枚举后续项无重复指令即丢失；③单词/缩写类名漏检（"Order 类""URLParser"→退化 type-only）；④显式路径后缀不全（`.txt` 漏）；⑤`base.endswith("test.java")` 大小写伪命中（Contest/Latest→test）；⑥path/symbol 匹配忽略大小写（Foo.java≡foo.java）。根因：窄修正则治标不治本，解析器 under-detect 即自动放行 full | 用户裁决：**改采结构性 fail-closed**——`RequiredEvidence` 增 `unresolved_constraints`（严格 Pydantic：原文 anchor + reason 枚举）与确定性三态 `status`；`full` 必要条件 = resolved 全覆盖 **且** 无 unresolved；route 把 unresolved 当缺口优先 refine；finalize 有 unresolved 最高 partial + 确定性限制说明。解析：ASCII 边界 file-token 遮蔽、逗号枚举延续、单词类名/角色词区分、后缀对齐 SUPPORTED_SUFFIXES、大小写敏感、强/弱指令作用域。控制流仍全确定性、不新增在线调用点、单 run ≤6（D6 内）；LLM 不得增删/改写/自报 resolved 或 unresolved | ✅ 2026-07-25 已按裁决实现（本提交）：先红后绿（§五.A 图级 8 反例 + 分类/parser 参数化 + 自然问法 + §五.D 结构不变量，均先在 a6583f8 失败）；`AGENT_STATE_SCHEMA_VERSION`→v3、Prompt 未改故 `PROMPT_VERSION` 不变；`make ci` 376、`make eval-ci` 65+19 全绿；跨 5 PYTHONHASHSEED items+unresolved 完全一致。**T22.1/T22.2/T22.3 仍不勾选，待下一轮独立复审** |
| 2026-07-25 | T22 五审 | 四次整改（commit 3e18503）经第五轮复审：三态硬门方向正确，但**核心性质"解析漏检必进 unresolved"尚未成立**，T22.1/T22.2/T22.3 全撤。6 处可复现缺陷：①部分解析成功后剩余目标静默消失（`_extract` 以整段 `produced` 为准）——"请引用 Foo.java 和关键实现"→complete/无 unresolved；c08"相关 Java 配置"被吞、引用三项即 full；c02 整题 status=none 使硬门真空；②点名身份与上下文类型丢失——裸 README 退化 type-only（`docs/architecture.md` 即可满足）、"OrderTest 的生产实现"被判 test 再补泛化生产项（`src/test/OrderTest.java`+任意生产文件即满足）；③文件名启发式先于目录——`src/main/**/AuditService.java`→historical_plan、`ChangelogService.java`→dev_log、`Testimony/Testing.java`→test；④否定约束是死信号（`forbidden_substitute_types` 无消费者、`required_satisfied` 忽略）；⑤unresolved 说明只在"无 claim 移除"分支产生（draft None / generate_failed / removed 全丢）；⑥required 可 11 项却报 complete，与 plan/evaluate schema 上限 10 冲突 | 路线裁决（复审给出，7 条）：保留三态 + unresolved 硬门，但解析器改**约束跨度账本**——①义务 span 独立检测、与目标解析成败解耦，覆盖 c02 类核验/比较表达；②义务对象按枚举与连接词拆目标段，每段须结算 resolved / unresolved / 明确非目标尾语，禁止整句 produced；③裸 README/PROGRESS 保身份，模糊"相关 Java 配置"直接 unresolved；④分类先精确目录与扩展名，audit/changelog 文件名规则只用于文档，`Test*` 用词边界；⑤拆分并实际执行"禁止替代"与"禁止引用"；⑥required 超 schema 上限须统一扩容或记 partial_enumeration；⑦unresolved 说明集中到 finalize 公共收尾 | ✅ 2026-07-25 已按 7 条裁决实现（本提交）：先在 3e18503 复现 6 处原始输出留证，再重写为约束跨度账本（`_detect_obligations` / `_resolve_region` / `_resolve_one` / `_Ledger`）+ `forbidden_citation_hits` + `required_evidence_tail` + `MAX_REQUIRED_ITEMS` 同源上限。§五.B 10 条图级反例在 3e18503 上 10/10 失败、修复后全绿；另补分类/解析/账本不变量约 30 条与 5 条自然问法反例。自查再补 2 洞（否定列表跨顿号被截断、`引用` 名词用法误当祈使指令）。`PROMPT_VERSION`→p1.5-agent-v3、`AGENT_STATE_SCHEMA_VERSION`→v4、快照 SHA→`6c838462…`；`make ci`（ruff/pyright 0、pytest 434）、`make eval-ci`（65+19）全绿；跨 5 PYTHONHASHSEED 完全一致。**T22.1/T22.2/T22.3 仍不勾选，待第六轮独立复审** |
| 2026-07-25 | T22 六审 | 五审整改（commit 3576bf0）经第六轮复审：六处原始问题与七条整改路线基本合格，**T22.1/T22.3 可以验收**，但 full 门仍剩 1 处可稳定复现的错误 full（P1，**T22.2 暂不验收**）：`finalize` 丢弃 `apply_l0` 返回的正文有效引用编号，禁引检查只用保留 claim 的 `evidence_ids`；`verification.l0_errors` 只校验正文 `[E#]` 存在性、不要求它同时出现在 claim 中。复现：问"不要引用设计文档，只引用生产源码"，E1=生产源码、E2=设计文档，正文 `[E1][E2]`、claim 只申报 E1 → verification=True、正文保留 `[E2]`、`final_mode=full`、无禁引告警。既有测试把 E2 同时写进了 claim，故未覆盖"正文与 claim 账本不一致" | 复审给出最小整改（不再动解析器，只修收尾层）：①必需证据覆盖仍只认保留 claim 的引用；②禁止引用检查改认"claim 引用 ∪ 最终正文实际保留的 `[E#]`"；③补正文含 E2、claim 仅含 E1 的图级红测，必须降 partial 并告警；④可把 `required_evidence_tail` 拆成 `support_citations` 与 `visible_citations`，避免正文标记反过来满足必需证据 | ✅ 2026-07-25 已按最小整改实现（本提交）：先复现原始输出（full + 正文 `[E2]` + 无告警），再把 `required_evidence_tail` 拆为 `support_citations`（覆盖判定）/`visible_citations`（禁引判定）两套账本，`finalize` 各分支保留 `apply_l0` 的有效编号并合入可见账本，`generate_failed` 分支补跑 `apply_l0`。新增 2 图级 + 2 单元测试（在 3576bf0 上确认失败）；解析器与 `PROMPT_VERSION`/`AGENT_STATE_SCHEMA_VERSION` 未动。`make ci`（ruff/pyright 0、pytest 438）、`make eval-ci`（65+19）全绿。T22.1/T22.3 按复审裁定勾选；**T22.2 待复审确认本修复后再勾选** |
| 2026-07-25 | T22 收尾 | 六审只给出 1 处 P1（已在 b5de64c 修复并附红测），但"第七轮独立复审"并未发生；按前六轮的自我约束（自认已修不算数）T22.2 会无限期挂起，阻塞 T23 及其后全部任务 | 由用户裁决是否以"自查复核 + 全绿 CI + 对抗探针"代替第七轮独立复审来关闭 T22.2；勾选行必须显式标注复核性质，不得表述为"已通过独立复审" | ✅ 2026-07-25 用户指示"先将 T22 完全收尾"后勾选：已按判据逐条自查（六审原场景复现为 partial + 禁引告警、10 条 finalize 对抗探针无错误 full、补 2 条判据字面要求的 Fake 矩阵用例），`make ci` 440 / `make eval-ci` 65+19 全绿；勾选行已标注"自查复核，非独立复审"。若后续复审再发现错误 full，按同一纪律撤销重开 |
| 2026-07-25 | T23 分类锚点 | 规格 §5 允许用技术词→后缀映射表"覆盖用户未给路径的场景"。按字面把问题里的技术词套到每条 not_found 上，实测会污染同一题里的其他缺口：c10（"请根据数据库迁移与 DocumentService 说明"）会把已索引 Java 的召回缺口一并标成"格式未摄取"，与判据"与已索引 Java 的证据缺口分类区分"直接冲突 | 技术词只匹配**缺口条目自身文本**；"用户只在问题里点名"的场景改由**确定性 required-evidence 说明**承载——该说明由问题原文解析而来且带类型标签（如"前端源码:Vue"、"数据库迁移:数据库迁移"），分类因此仍锚定用户表述，且不会外溢到别的缺口。为此 `required_evidence_tail` 的缺口说明按证据类型拆成多条（原为合并一条） | ✅ 2026-07-25 用户裁决（在头提交实跑 c03/e05/c10 后给出）：**采用目标级锚定**——问题中的封闭技术词先确定性绑定到对应 required-evidence 缺口，再逐条分类；不得作为整题标签传播到其他 not_found；无法绑定时保守归入当前证据缺口或 unresolved，不恢复整题级兜底。依据：规格 §5 只要求覆盖"未给路径"场景、未要求题级传播，而 Evaluation-v1.5 §2 明确 .sql 必须是 unsupported_or_not_ingested、已索引 Java 必须是 missing_from_current_evidence |
| 2026-07-25 | T23 混合原因条目 | 用户在裁决同时发现的独立边界：LLM 把两类原因写成一句（"当前证据未覆盖数据库迁移与 DocumentService 的删除实现"）时，条目会得到 `category=unsupported_or_not_ingested` 却同时是 `basis=corpus_index` + Java 路径，**内部自相矛盾**；根因是"一条文本只能有一个类别"而分类却按信号叠加 | 补"单条 not_found 混合两类原因"红测，并在 finalize 保证"一条一原因"或确定性拆分；不得用恢复整题级兜底来修 | ✅ 2026-07-25 已按此实现（本提交）：先复现原始矛盾输出，再把分类改成**逐目标段结算**——`_assess` 对每段只产出一个原因（证据事实 > 索引事实 > 摄取范围 > 无从证明的强断言），一条语句出现多个原因即按连接词确定性拆成多条（括号内不拆、确定性说明不拆），同类目标不拆、原文照旧。红测 3 组（拆分正确性、`category`↔`basis`↔`refs` 自洽不变量、不得过度拆分）在修复前 4/4 失败 |
| 2026-07-25 | T23 拆分复审 | 5325ecd 修好了原始矛盾，但新增拆分逻辑本身有两处可复现阻断点：①同一原因下多个目标只记 `members[0].refs`，其余事实来源（PaymentService.java）从 refs 与告警中消失；②单字连接词在任意位置匹配，`涉及/或者/与此同时/参与` 被误切，且无信号残段被挪到错误位置（`数据库迁移的字段定义未找到涉…`）。既有自洽测试只查 refs 形状，未查"文本里每个目标都有事实来源"，也未覆盖自然连接词 | 补两组红测；同原因成员 refs 全量聚合；切分只在真正的目标连接处生效并保序 | ✅ 2026-07-25 已修（本提交）：先复现两处（含 `['未找到 OrderService ', '者 DocumentService 的实现']` 的分词证据）再改——refs 按原文顺序合并去重、文案最多列 3 条并记"等 N 条"、单字连接词加左右封闭屏蔽表且句首/句尾不算连接、残段并入紧邻前一段。红测 7 条修复前全红；另补"文本中每个目标都有对应事实来源"不变量。`make ci` 497 / `make eval-ci` 65+19 全绿 |
| 2026-07-25 | T23 切分口径 | 29311fb 的 refs 聚合已确认修好，但**单字屏蔽表路线本身**被第三轮复审否决，两处仍可复现：①`或者` 被当成不可切复合词 → `未找到数据库迁移或者 DocumentService 的删除实现` 只剩一条 `corpus_index`，`.sql` 分类整条消失（复现 c10 型混淆）；②屏蔽表无法覆盖自然词汇 → `提及/与否/与此同时/同时` 仍被削首字或错位；另 `_ANCHOR_TRIM` 把"与/及/或/和"当边界字符盲删，是"此同时 X"的第二成因 | 复审裁决：改为**基于两侧目标信号确认连接位置**，不得继续扩充单字屏蔽表；补"单条混合两类原因"的 `或者` 红测与四例自然措辞红测 | ✅ 2026-07-25 已按裁决重写（本提交）：删 `_CHAR_CONNECTORS`，切点只在**两个相邻目标之间**且只认"括号外标点"与"整个间隙就是一个连接词"两种可证情形，其余保守不切（原文完整保留、按优先级取唯一原因、被压住的未摄取信号进 warnings 不静默消失）；`_EDGE_TRIM` 去掉连接词字符；残段归位逻辑随结构消失（每段必含目标）。10 条红测修复前全红（含复审四例原句与 `或者` 混合原因），另加 2 条"目标区间与 T22 token 词法同源"防漂移测试。取舍：`…的说明与数据库迁移` 这类左侧是散文的连接处不再拆分（宁可少拆一条也不切碎原文），已用专门单测固化并告警 |
| 2026-07-25 | T24 充分性口径 | 规格 §6"最终充分性按单调覆盖矩阵判定，不看最后一轮扁平 top-k"与 D6/T22 裁决"LLM 报告只作诊断、裁决权在确定性代码"存在张力：**没有 required item 的问题**，矩阵里只剩 evaluate 自报的方面，"按矩阵判定"等于把纯 LLM 报告升格为权威信号 | 按轨分治：①存在确定性轨（required items）时，充分性由单调矩阵接管（全部方面跨轮已支持 + 末轮无"从未被支持"的 missing），末轮扁平 `sufficiency` 不再有否决权；②无确定性轨时沿用末轮 evaluate 自报（P1 行为不变）。无论哪一轨，矩阵**只能阻止不当降级、不能放宽 full**：T22 的逐项引用硬门/unresolved/禁引三道门不变 | ✅ 2026-07-25 用户/复审裁决：**同意两轨分治，不要把无 required item 的分支改成矩阵判定**。正式口径——"诊断轨矩阵只提供 partial/refusal 的单调下界和缺口防回退，不得授予 full；只有确定性轨完成终稿逐项引用闭环后，矩阵才可覆盖末轮 sufficiency"。理由不是 D6 禁止读 LLM 结构化信号，而是无 required item 时既没有权威方面全集、也没有 aspect→终稿 claim/citation 的确定性闭环（复审反例：两轮各自报支持一个方面、终稿只引用其一 → 矩阵会判两方面全 supported，空 required 硬门真空通过，partial 会升成错误 full）。已把该裁决实现为 `is_monotonically_sufficient` 只看确定性轨，并加反例单测锁定 |
| 2026-07-25 | T24 一审 | 首版 T24（commit 0c0eb36）经复审确认 3 处发现并撤销 T24.1/T24.2 勾选：①**P1 保留算法误判 fresh 已覆盖**——`anchored` 按全部 fresh 计算，但预留旧锚点又缩短实际入选的 fresh 前缀；`limit=3` / fresh=[OtherOne, OtherTwo, RagService] / carried=[RagService, CitationParser] 时实际保留 [OtherOne, OtherTwo, CitationParser]，新旧两条 RagService 都被标 `aspect_anchor_capacity`，而容量本可保留 [OtherOne, RagService, CitationParser]；②**P1 历史已支持被当成当前可交付**——`has_deliverable_content` 只看"证据集非空 + 历史有任一 supported"，构造当前只剩无关文档、RagService 当前覆盖为 false 的状态仍返回 True；同一直接复现里终态同时出现 `被挤出，非证伪` 告警与 `未取得…生产源码:RagService`（根因：缺口措辞只看终态 evidence，且 `calibrate_not_found` 只收到 evaluator 轨标签）；③**P2 `EliminationRecord` 未进状态/轨迹**——retrieve 内生成后即丢弃，只返回汇总 warning，运行结束无法逐条审计 | ①按"最终实际入选的 fresh 前缀"与 reserved anchors 联合/迭代求解并补红测；②可交付判定须校验当前证据仍支撑该方面，并把缺口措辞明确为三态（从未取得 / 仍在证据集但未引用 / 前轮取得后被挤出），第三态保持 partial 但不得写成"未取得"——这属 T24 单调性，不留给 T25；③逐条淘汰记录要么进状态与轨迹，要么把完成说明改为"运行时计算并汇总"，不得称"逐条记录" | ✅ 2026-07-25 三条全部按裁决整改（本提交，先红后绿）：4 条红测在 0c0eb36 上确认失败；保留算法改代表证据联合求解（确定性轨代表绝对优先、诊断轨代表给新证据让位、只丢弃不重排）；三态措辞落地并把两轨已支持标签都传给 `calibrate_not_found`；`evidence_eliminations` 进 `AgentState`（只增账本 + 冻结上限）与 retrieve step 轨迹（chunk_id/rel_path/aspect_ids/reason）。另用 4000 例随机化不变量探针对比：0c0eb36 违反 1057 例（含未被复审点出的 carried 重排），整改后 0 例。`make ci`（ruff/pyright 0、pytest 544）、`make eval-ci`（65+19）全绿，跨 5 个 PYTHONHASHSEED 一致。**T24.1/T24.2 仍不勾选，待下一轮独立复审** |
| 2026-07-26 | T24 二审 | 一审整改（commit f243c86）经第二轮复审：最小反例与裁决落位均已确认，但仍有 3×P1 + 2×P2 —— ①**确定性代表并非绝对优先**：两轨的新证据代表被合并进 `fresh_anchors` 后按召回排名截断，`limit=1` / required=RagService / diagnostic=CitationParser / fresh=[CitationParser, RagService] 实际保留 CitationParser；fresh 只有 CitationParser、carried 是 RagService 时确定性旧锚点同样被挤掉；②**诊断轨仍可被末轮 LLM 自报回退**：evaluator 的 `present_now` 定义成「本轮再次自报」，证据集完全不变时 evaluator 返回 supported=[]/insufficient 即令路由进 refusal，违反已裁决的诊断轨单调下界；③**限定词变化仍产生矛盾缺口**：被挤出的 RagService 若被报成「RagService 生产源码」，终态同时出现原始缺失项与三态说明（`not_found.py` 只做归一化全等比较，`nodes.py` 传两轨标签也建立不了跨轨身份）；④**淘汰记录仍不能逐条回放**：单轮账本可达 24 条，step summary 只落前 8 条，而完整 AgentState 不落库；⑤**新增 state 字段未递增契约版本**（`evidence_eliminations` 已加但仍是 v6，与《P1版本标识》「状态字段改变即递增」冲突） | ①按「确定性轨 → 诊断轨」分配保留资格再按各来源原序输出；②为 evaluator 方面保存稳定证据绑定，以当前证据交集算 `present_now`；③evaluator missing 若能由 T22 matcher 唯一绑定到 required item，交给确定性三态说明；④按 `MAX_ELIMINATION_RECORDS` 持久化并补 >8 的 replay 测试；⑤升到 v7 并同步版本断言 | ✅ 2026-07-26 五条全部按裁决整改（本提交）：5 条红测先在 f243c86 上确认失败；两轨随机探针（新增两轨竞争覆盖）整改前 43 例违反、整改后 0 例，且从未出现确定性轨被饿死或被驱逐。**两处取舍已显式记录**：③只吸收「被挤出」这一矛盾态——「从未取得」（两句同向、仅冗余）与「仍在证据集但未引用」（T23 会改写成「已出现在本次检索证据中」）都不构成矛盾，全吸收会连方法级细节与 `corpus_index` 事实来源一起丢（c10 要求两类缺口可分辨，已有 T23 测试守住）；④持久化链路按「recorder 摘要 12 条 + `_step_output_summary` 映射 12 条 + 既有 output_summary 落库回放集成测试」三段验证，未另造 >12 chunk 真实语料跑端到端 21 条淘汰的 replay。`make ci`（ruff/pyright 0、pytest 550）、`make eval-ci`（65+19）全绿，跨 5 个 PYTHONHASHSEED 一致。**T24.1/T24.2 仍不勾选，待第三轮复审** |
| 2026-07-26 | T24 三审 | 二审整改（commit bb1242c）经第三轮复审：五条原始反例均已对点修复，但**泛化后仍有 2×P1** —— ①**证据集合只剩任意交集仍被判为直接支撑**：无法精确绑定时记录该轮全部路径，`present_now` 又按任意路径交集算，绑定=[A.java（实际支持）, unrelated.md]、终态只剩 unrelated.md 时仍判 True 并路由 generate；按路径绑定还会把同一文件的不同 chunk 当成同一条证据；②**跨轨吸收静默删除第二目标与方法级缺口**：吸收只检查"匹配到几个 required id"，`RagService 中 NO_ANSWER 的触发条件` 整条删除、`RagService 和 UnknownService 生产源码` 也整条删除（UnknownService 不是 required item，`len(bound)==1` 仍成立），与代码注释声称的"多目标保守保留"相反，并回归 T23"方法级缺口信息保留"要求。另有两处非阻断注释漂移（`plan_retention` 资格顺序、evaluator `present_now` 口径） | ①保留每次 observation 的绑定集合分组、以 chunk_id 判定，只有某个历史绑定集合**完整保留**才算 `present_now`（不需改 LLM 输出契约；新增 state 字段则继续递增版本）；②只吸收"单目标 + 封闭通用限定词"形式，存在其他目标或方法/行为语义时拆分或保留残余，并补两条红测 | ✅ 2026-07-26 两条按裁决整改（本提交，先红后绿）：`EvidenceRef` 引入稳定 chunk_id，`AspectObservation.chunk_ids` 逐轮分组记录绑定，`build_matrix` 改判"某一轮绑定集合完整保留"；吸收增加 `_is_pure_identity_gap`（`iter_target_spans` 计目标数、复用 T23 `strip_absence_markers` 剥缺失措辞、残余须落在封闭限定词表），12 条对抗探针逐条核对（方法级/行为/第二目标/未绑定目标全部保留）。`AGENT_STATE_SCHEMA_VERSION`→**v8**；两处注释漂移同步修正。**取舍显式记录**：绑定粒度粗（EvaluateOutput 无 aspect→evidence 自报字段，加它要改 LLM 契约，属 D6 边界）的代价由"整组保留"这一严格口径承担——回退风险是"末轮 evaluate 未再自报 + 绑定集合缺一条"时诊断轨不再算可交付，此时终态取决于确定性轨；这是复审明确要求的最小安全口径。`make ci`（ruff/pyright 0、pytest 559）、`make eval-ci`（65+19）全绿，跨 5 个 PYTHONHASHSEED 一致。**T24.1/T24.2 仍不勾选，待第四轮复审** |
| 2026-07-26 | T24 四审 | 三审整改（commit faf43b0）经第四轮复审：chunk_id 分组绑定、`present_now` 完整集合包含、NO_ANSWER 方法缺口与第二符号目标保留、两处注释与 v8 均已确认，但**"纯身份缺口"判据还剩 1×P1（两处同源边界）** —— ①**`iter_target_spans` 不等于完整的原文目标计数**：该判据只依赖路径/符号/规范文档名，而"测试、迁移文件、设计文档"本身也是 T22 能识别的 type-only 证据目标，不进 spans 又恰好都在限定词白名单里（根因 `nodes.py` 的 `_QUALIFIER_NOISE` 把连接词当噪音抹掉），`RagService 和测试`／`与配置`／`及迁移文件`／`、设计文档` 四条稳定复现均被整条吸收；更直接的类型错配是 required R1=`RagService 的生产源码`、missing=`RagService 的测试` 也被吸收，而确定性三态说明只覆盖生产源码，替代不了测试缺口；②**同一判据使用被截断的文本**：`not_found.py` 的 `strip_absence_markers` 返回前截到 60 字才交给"是否纯身份"的安全判定，允许 500 字的 missing item 把方法级语义放在第 60 字之后，使判据只看到前面的"生产源码"并错误吸收 | ①目标后出现协调连接词（与/和/及/或/、/逗号）时 fail-closed，不吸收整句；②通用限定词必须与绑定的 `RequiredEvidenceItem.type` 相容（production source 只能接受源码/实现/代码，不能接受测试/配置/迁移/设计文档）；③分类判定读取**完整**规范化文本，60 字截断只留在用户可见 `_subject` 渲染阶段；④增加上述至少两类红测 | ✅ 2026-07-26 全部按裁决整改（本提交，先红后绿）：判据收敛为三道 fail-closed 闸门——目标身份逐 token 核对（新增 `target_tokens_all_match`）、目标两侧协调连接词一律不吸收（连接词从 `_QUALIFIER_NOISE` 移出，改由 `_CONNECTOR` 单独判）、残余限定词按 `_TYPE_QUALIFIERS` 分类型相容；`strip_absence_markers` 去掉截断，`_MAX_SUBJECT_CHARS` 只作用于 `_subject`。**红测有效性已留证**：7 条新增用例在 faf43b0 上逐条确认被吸收（含 60 字截断那条）。**另有一处探针自查发现**（复审未点出）：只数 `iter_target_spans` 会被 `merge_spans` 的相接合并骗过——`RagService.javaOrderService` 含两个符号却只剩一段跨度，故目标计数改为逐 token 核对身份而非数跨度。忠实探针 22800 组合（绑定类型取自真实问题解析，交叉核验"吸收 ⟹ 用 T22 读这条缺口文本时不存在第二个目标/别的类型目标/未解析约束"）0 违反、仍吸收 1375 条（未退化成"永不吸收"）。`make ci`（ruff/pyright 0、pytest 572）、`make eval-ci`（65+19）全绿，跨 5 个 PYTHONHASHSEED 一致；契约版本不变（无 state/Prompt 改动，仍 v8）。**T24.1/T24.2 仍不勾选，待第五轮复审** |
| 2026-07-26 | T24 五审 | 四审整改（commit 18beb85）经第五轮复审：逐 token 身份核对、类型错配、多目标连接词、长文本不截断、`RagService.javaOrderService` 边界与 v8 不变均已确认，但**新增 1×P1** —— **句末标点被误判为协调连接词**：`_CONNECTOR` 纳入了 `，,；;`，而 `_is_pure_identity_gap` 对整段 remainder 任意搜索，并没有落实注释所说的"目标两侧出现"。最小复现：required R1 = 生产源码:RagService（前轮已支持、终态被挤出）、evaluator missing = `RagService 生产源码，` → `dropped=[]`，终态同时出现"RagService 生产源码，"与"…在前几轮检索中已取得…被挤出终态证据集…"；去掉句末逗号即被正确吸收。`ShortText` 并未禁止句末标点，故这是合法 LLM 输出，会重新造成同一方面"缺失"与"已取得后被挤出"并存。另有 P2（非单独阻断）：清单把本轮写成"三审整改（本提交）"，下一行又是"三审整改（历史记录）"，应为"四审整改" | 最小修复口径是先排除句首/句末标点，再判断内部协调连接符；同时锁住 `RagService 和测试`、`RagService、设计文档` 仍不得吸收；建议把 `，` `,` `；` `;` 四种句末形态加入红测 | ✅ 2026-07-26 两条按裁决整改（本提交，先红后绿）：连接词判定改为**左右两段分别判、只看背离目标的一侧**——连接词靠目标那一侧天然有内容（就是目标本身），故只需检查背离目标的一侧去掉标点后是否还有实词：有 → 真并列（`RagService、设计文档` 不吸收）；只剩标点 → 句首/句末标点（`RagService 生产源码，` 吸收）。新增 `_has_coordinator` + `_PUNCT_EDGE`，`_CONNECTOR` 字符集不变。**红测有效性已留证**：`，` `,` `；` `;` 四种句末形态与句首标点共 5 条在 18beb85 上确认被判"不吸收"（另加句末句号一条回归）。忠实探针扩到 **182400 组合**（新增 8 种句末标点形态）0 违反、仍吸收 11960 条。P2 已改正标号（本轮为"四审整改（历史记录）"、本提交为"五审整改（本提交）"），并恢复"⚠️ 升序 → ✅ 降序"的原有排布。`make ci`（ruff/pyright 0、pytest 578）、`make eval-ci`（65+19）全绿，跨 5 个 PYTHONHASHSEED 一致；契约版本不变（无 state/Prompt 改动，仍 v8）。**T24.1/T24.2 仍不勾选，待第六轮复审** |
| 2026-07-26 | T24 六审 | 五审整改（commit e1e42bd）经第六轮复审：句末标点反例、真并列、类型错配、清单标号与排序、v8 口径均已确认，但**新增 1×P1** —— **"背离目标一侧有实词"不等于"存在第二目标"**：`_has_coordinator` 会把范围前缀也判成并列对象，而这些前缀在 `not_found.py` 的 `_ORPHAN_PREFIXES` 里明确属于可剥离限定。最小等价对：`证据中 RagService 的生产源码` → absorbed=True，`证据中，RagService 的生产源码` → absorbed=False；7 个 orphan 前缀全部稳定复现这种 plain=True / comma=False 差异，displaced R1 的终态重新出现"证据中，RagService 的生产源码"与"…已取得…被挤出终态证据集…"并存。182400 组合的探针没发现它，是因为不变量只有"吸收 ⟹ 没有第二目标"，只能抓**错误吸收**，本例是**应吸收却未吸收** | ①判"背离目标一侧"时先剥掉可剥离的 orphan/absence 前缀及其标点，再看是否还有并列对象；②补第二条**变形不变量**：对已认定的纯身份缺口，插入可剥离前缀及其标点不得改变吸收结果；③加图级断言确保终态只有一条 RagService 说明；④`RagService 和测试`、`RagService、设计文档` 仍须不吸收 | ✅ 2026-07-26 四条全部按裁决整改（本提交，先红后绿）：`_has_coordinator` 判"背离目标一侧"改用 T23 同一个 `strip_absence_markers` 归一化（与后面算限定词同口径，不另建一套），归一化后仍有实词才算并列对象。**红测有效性已留证**：去掉本轮 src 改动后新测 **74 条全红**（含图级那条），恢复后 199 条全绿。新增变形不变量红测 126 例（7 个 orphan 前缀 + 11 个 absence 标记 × 7 种标点）+ 图级断言（末轮报 `证据中，RagService 生产源码` 时终态只有一条 RagService 说明）+ 4 条守住真并列/非可剥离前缀的用例。探针同步补第二条不变量：126 种变形 × 每个已吸收基例，**翻转 0**；原不变量仍 0 违反、吸收 11960 条。`make ci`（ruff/pyright 0、pytest 710）、`make eval-ci`（65+19）全绿，跨 5 个 PYTHONHASHSEED 一致；契约版本不变（无 state/Prompt 改动，仍 v8）。**T24.1/T24.2 仍不勾选，待第七轮复审** |
| 2026-07-26 | T25.1 判据收窄 | T25.1 判据「正文声称已证明的内容不得同时出现在 `not_found`」（规格 §7、Evaluation-v1.5 §5.2 第 5 条）含**命题级**要求：案例七里正文断言「findConversation 强制 owner 校验」、`not_found` 称「无法确认是否强制 owner」，判定二者是否互斥需要语义蕴含。仓库事实证明确定性代码做不到——`path_matches_token`（`evidence_types.py:870`）只判 token 与 basename/stem 的身份关系，`l1_errors`（`verification.py:64`）只判 quote 是证据原文逐字子串且 `quotes` 允许为空（`state.py:121`）。唯一能做该判断的是 L2/NLI，而《P1.5实现规格》§14 与 D9 明确禁止其进入在线路径。计划前审两轮（PG-01、PG-01-R）据此判 `PLAN_REVISION_REQUIRED`，要求用户裁决而非由执行者自行取舍 | 二选一：**A** 把判据收窄为「身份级不符确定性消除 + 命题级关系确定性标记为待核验并披露」，命题级消除随离线/人工 L2 推 P1.6；**B** 要求完全关闭 → T25.1 `RETURN_TO_DESIGN`，需新 ADR 放开在线语义校验或先建离线 L2 回归门，范围与文件预算重估 | ✅ 2026-07-26 用户裁决 **A**：判据按以下口径收窄，作为 T25.1 的正式验收依据——①**必须确定性消除（身份级）**：`not_found` 不得把本次回答实际交付为引用来源的文件写成缺失，判定用 E0（`visible_citations` = claim 申报 ∪ 正文过 L0 的 `[E#]`，与 Answer `citations` 同源）+ E1（整条条目的全部可解析目标都在 E0 内），资格按**整条原始条目**计算后经 `_Markers` 下发（前审 PG-03：该类条目会被 T23 切成多段多原因，逐段判定必然误命中）；②**只需确定性披露（命题级）**：条目含残余命题时保留原语义并追加「该文件是本次回答的引用来源之一…未经确定性核验」披露句 + 专用 warning，供 T31 与 U3.2 度量，**不得声称该命题已被支撑，也不得声称其与正文存在冲突**（前审 PG-05：E1 成立且残余命题存在，推不出二者互斥——反例：正文仅断言「RagService 是服务类」、缺口讨论 `findConversation` 的 owner 行为，可以并存）；③不得为此引入 L2/NLI 或任何在线语义判断（D9、§14 不变）；④T25.1 勾选时按本口径逐条核对，**不得按判据字面宣称"命题级冲突已消除"**。合同与冻结测试见 `docs/tasks/T25.1-finalize-consistency.md`（该 packet 另记 PG-02/PG-04 两处修订：交付引用集须含正文独立 `[E#]`；`limitations` 按 `claims` 空否 × `generate_failed` 拆四态，因 `test_agent_graph.py:175` 证明"非生成失败但无结构化 claim"的 partial 终态可达） |
| 2026-07-27 | T25.2 案例二判据 | T25.2 判据字面要求「重生成后 `not_found` 不得由 1 增至 2 **且含 active 文件**」。但案例二里 `README.md` 确实是 active indexed、也确实没被本轮召回；T23 已把该项改写为「该路径已在当前项目索引中（README.md，状态 active），本轮未被召回，属当前证据缺口」——**这句话每一部分都为真**。按字面执行须把该项从 `final_not_found` 删除，等于**隐藏一个真实的召回缺口**，与 T23 `missing_from_current_evidence` 四分类设计直接冲突，并需规格/ADR 修订与 `not_found.py` 改动（文件预算 8→≥10）。计划前审第二轮 PG-01-R 据此判 `PLAN_REVISION_REQUIRED`，要求用户裁决而非执行者自行取舍 | 二选一：**A** 判据收窄为「重生成后不得新增**没有事实依据**的 `not_found`，且不得把 active/已索引路径断言为不存在；条数可以增长」；**B** 按字面执行——新增的、能解析到 active indexed 路径的缺口一律删除，需重新拆分并修订规格 | ✅ 2026-07-27 用户裁决 **A**：判据按下述口径收窄，作为 T25.2 的正式验收依据——①依据为 m14 断言 3 原文「不得新增没有事实依据的 `not_found`」（`evalsets/v1.5/mechanism_checks.md:23`）、规格 §7 的「**错误**缺口」、《Evaluation-v1.5》§5.2 第 2 条的「不得报'**不存在**'」，三处均指向事实性而非条数；②验收由绑定测试 I1 承担，参数化覆盖 m14 要求的两种构造（篡改一字 / 跨 chunk 拼接——仓库现有覆盖只到 `tests/unit/test_l1.py:65,70` 的 L1 原语层，无图级重生成路径），断言新增缺口经 T23 事实校验（`category`/`basis`/`original_text`）且终态不含"不存在/仓库无此文件"任一措辞；③**不得按判据字面宣称"条数未增长"**；④m14 第 4 条（warning `resolved`/attempt 归属）按本清单第 160 行仍归 T30.2，本任务只记归属不断言。合同与冻结测试见 `docs/tasks/T25.2-regen-coverage-diff.md` |
| 2026-07-27 | T25.2 跨稿引用比较 | 计划前审第二轮 PG-04-R：第二次修订新增的「第二稿有效引用集合是第一稿真子集则告警」不可按计划实现——`src/devkb/agent/nodes.py:1000-1011` 证明 finalize 在 claim 被移除时会**按保留 claim 重建正文并重新 `apply_l0`**，原正文独立 `[E#]` 标记被丢弃，只有 `removed == 0` 才保留；而首稿恰恰因 L0/L1 失败才触发重生成，两稿账本无法在不改动 `visible_citations` 既有构造（`nodes.py:1029-1042`）的前提下对齐，会产生**虚假的覆盖收缩告警** | 二选一：**C1** 删除该比较（判据要求的「重做覆盖度检查」已由既有 `monotonic_matrix`（`nodes.py:961`）与 `required_evidence_tail`（`nodes.py:1043`）对终稿承担，跨稿引用比较系执行者自加项、非判据要求）；**C2** 保留但把账本构造抽成 `verify`/`finalize` 两处共用的纯函数，代价是触碰 finalize 既有构造，与本 packet「不重构」非目标冲突 | ✅ 2026-07-27 用户裁决 **C1**：删除跨稿引用集合比较。连带后果——`DraftSnapshot` 由三字段减为 `not_found` + `evidence_ids` 两字段；快照写入点由 `verify` **回到 `generate`**（PG-04 当初逼它移到 `verify` 的唯一理由是账本需要 `verification.failed_claims`，随账本一并消失）；`visible_citations` 零改动。以 U13（首稿含 L1 失败 claim 且正文有独立 `[E#]` 标记时，快照只含两字段）作为该收窄的机器证明 |
| 2026-07-27 | T25.2 文件预算例外 | 计划前审第三轮 PG-07：`evalsets/v1.5/mechanism_checks.md:11` 把 m14 的执行层写明为「FakeLLM 脚本 + **真实测试 PG**」，而 `tests/unit/` 无数据库夹具（真实 PG 夹具只在 `tests/integration/conftest.py:36`）。第二轮计划把绑定用例 I1 放在 `tests/unit/test_agent_graph.py`，只能**手工注入 `CorpusProfile`**，证不到 `documents` 表 → `src/devkb/agent/service.py:156` → `AgentRuntime.corpus` 的真实接线。补上必须新增 `tests/integration/test_agent_service.py`，即第 9 个文件，超出 AGENTS.md 的 8 个总文件上限 | 二选一：**D1** 批 `size_exception = 9`，一个任务内做完；**D2** 拆出 T25.2b 只放这条集成测试 | ✅ 2026-07-27 用户裁决 **D1**：批准 `max_changed_files = 9`（**核心源文件仍为 3**，超的只是总数）。理由：①该文件已有 `_seeded_project`（ingest `tests/fixtures/corpus_md/` 的 5 个 md 全部成为 active indexed）与 `session` 夹具，边际成本小；②`pytest -q` 按 `pyproject.toml:80` `testpaths=["tests"]` 收集 **667 unit + 90 integration = 757**，与既有门禁报数一致，故该绑定有 `make ci` 强制；③拆分的代价是为一条测试多走整轮前审+实现+代码审查+终局复核，且会让 T25.2 在 m14 仍未绑定的情况下可勾选——正是 PG-01/PG-01-R 两次指出的问题。落点：**I1 整体移到 `tests/integration/test_agent_service.py`**（真实 PG + FakeLLM 脚本，参数化 m14 两种构造），观测走 `get_run_trace`（沿用 `tests/integration/test_agent_service.py:240` 已验证的模式），并断言 `basis == "corpus_index"` 作为真实接线的证明。例外记录同时写入 packet frontmatter 的 `size_exception`（AGENTS.md 要求的落点） |
| 2026-07-28 | T26.1 预防层与落点 | 判据「partial 正文与总结只对已验证资源/操作下结论，不对未覆盖方面作绝对断言」有两处执行者无权自决的取舍：**①预防层**——确定性代码只能在正文尾部**追加**限定句，无法阻止模型写出"用户 A 无法读取或删除用户 B 的任何资源"（基线探针实测，见 packet「复现证据」）；而《Evaluation-v1.5》§5.1 第 5 条是真实 dev/holdout run 上的人工判据，读到全称断言仍判失败。纳入 Prompt 预防层则 `PROMPT_VERSION` v4→v5，强制连带 `tests/unit/test_agent_prompts.py:17` 与 `tests/unit/test_contract_versions.py:16` 两处硬编码版本串，总文件 9 个 > AGENTS.md 的 8 个上限。**②落点**——判据点名"正文与总结"，写进 `final_answer` 会改变 `answer_text` 并须更新 `test_agent_graph.py:181`/`:235` 两处精确断言；只写 `limitations` 则正文里的全称断言旁边没有任何限定，"正文"半句落空，且要改 `answer.py`（第 4 个核心文件）并触碰 T25.1 PG-04 冻结的四态 | ①A 纳入 Prompt 预防层 + 批 `size_exception=9` / B 只做确定性追加与披露（5 文件、无例外，代价是真实 run 正文措辞不改善）；②A 写进正文尾部 / B 只进 `limitations` | ✅ 2026-07-28 用户裁决 **E1=A、E2=A**：①纳入 Prompt 预防层，`GENERATE_SYSTEM` 增 partial 范围限定指令、`PROMPT_VERSION`→`p1.5-agent-v5`，批准 `max_changed_files = 9`（**核心源仍为 3**，超的只是 Prompt 版本递增强制连带的两个测试文件，与 T25.2 裁决 D1 同形）；②范围句写进 `final_answer` 尾部——只**追加**确定性事实句，绝不删改、重写模型任何字句；`answer.py` 零改动。两处既有精确文本断言更新为"原文 + 范围句"的更强断言，不是放宽。合同与冻结测试见 `docs/tasks/T26.1-partial-verified-scope.md` |
| 2026-07-28 | 分支纪律 | 本清单第 7 行（2026-07-24 冻结）写"P1.5 工作在 `p1.5` 分支进行"，与其后引入的 WF-001 工作流门禁**直接冲突**：`scripts/workflow_guard.py:31` 的 `PROTECTED_BRANCHES = {"main","master","p0","p1","p1.5"}` 使 `make preflight` 在 `p1.5` 上必定报 `ERROR: 不得直接在受保护阶段分支 'p1.5' 开始任务；请新建 task 分支`。按清单字面执行则 Stage 0 永远无法通过，T25.1/T25.2 实际也都是在 task 分支（`workflow/ai-review-convergence`）上完成后合回 `p1.5` 的。注：`make ci` 不检查分支，故此冲突只在 Stage 0 显现，不影响已验收任务的证据有效性 | 二选一：**A** 修订第 7 行为"各任务在 task 分支进行、验收后快进合回 `p1.5`"，以 WF-001 为准；**B** 保留原文，仅在此记录其写于 WF-001 之前、现以工作流为准 | ✅ 2026-07-28 用户裁决 **A**：第 7 行已按此修订。正式口径——`p1.5` 是受保护的集成分支，只接收已验收任务的快进合并，不在其上直接开工；每项任务从 `p1.5` 派生 task 分支，走 WF-001 Stage 0–10（`make preflight` → `make verify-task` → 限定范围审查 → `make verify-full`），验收提交后 `git merge --ff-only` 合回 `p1.5`。本次修订只改分支纪律表述，不改任何任务判据、不改代码 |
