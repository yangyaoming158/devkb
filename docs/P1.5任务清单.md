# P1.5 任务清单（真实仓库可靠性/诚实加固）

> 开始日期：2026-07-23
> 前置 Gate：P1 已于 2026-07-22 验收关闭（结论不变）
> 唯一实现依据：《P1.5实现规格》；评测依据：《Evaluation-v1.5》；治理依据：《ADR-0010》。
> 规则：完成判据全部满足后才勾选，并追加 `✅ YYYY-MM-DD 结果与证据 commit`；规格冲突或缺失写入文末偏差记录，等待用户裁决。
> 2026-07-24 规划复审修订：判据与《P1.5实现规格》《Evaluation-v1.5》同日修订同步（两栏预期、not_found 静态判定、policy-refusal 机制、13+5 拆分、诊断轨迹迁移/上限）；P1.5 工作在 `p1.5` 分支进行。
> 边界：全程在 D6 与现有依赖栈之内；不新增运行依赖、不改检索机制、不重跑 P1 holdout。多语言摄取/inventory/工具调用属 P1.6，禁止在本清单提前实现。

## 执行顺序

```text
U3.1 冻结 Evaluation-v1.5（预登记 + 反例）——先于一切 src/Prompt 改动
 ├─→ T22 证据类型硬约束与权威性分层（RT-01/05）
 ├─→ T23 not_found 四分类 + 全轮历史事实校验（RT-02/15）
 ├─→ T24 跨轮覆盖单调性 + partial 保留（RT-16/20）
 ├─→ T25 节点一致性 + finalize 覆盖差分（RT-03/08）
 ├─→ T26 partial 认知口径 + 安全/配置覆盖矩阵（RT-18/19）
 ├─→ T27 policy-refusal 终态（RT-22）
 ├─→ T28 摄取覆盖披露（案例三/十廉价版）
 ├─→ T29 unknown project fail-fast 顺序（RT-23）
 └─→ T30 诊断轨迹最小版 + warning 归属（RT-07/10）
T31 Evaluation-v1.5 harness 扩展与 dev Gate（T22 后可并行，最终依赖 T22–T30）
全部完成 → T32 P1.5 收尾与 Gate（依赖 U3.2 人工复核、U3.3 v1.5 holdout 授权）
```

同一时间只推进一个主任务；允许先写失败测试或最小 spike，但不得提前实现 P1.6/P2。`PROMPT_VERSION` 因本阶段递增，须显式记录并使旧 dev 报告不被覆盖。

## U3 用户/人工任务

- [x] U3.1 在首次 P1.5 dev 运行前冻结 Evaluation-v1.5
  - 判据：按《Evaluation-v1.5》§2 把 18 条 post-P1 复现路径转为正式回归题（13 条真实问答 + 5 条机制检查分开落盘，文件名与 JSONL schema 依 §2），补自然问法/合法安全咨询/真正无资料/格式未摄取反例；每题按**两栏预期**预登记（P1.5 契约预期绑定 Gate、P1.6 目标预期仅记录；必需证据、允许补充类型、禁止替代类型、预期 mode、关键路径/符号，"必须引用 X"登记为条件式）；建立独立 v1.5 dev/holdout，不复用、不重跑、不据此调优已使用的 P1 holdout；冻结后运行任何 P1.5 dev 即锁标签
  - ✅ 2026-07-24 冻结：`evalsets/v1.5/` 落 4 文件——`contract_dev.jsonl`（13 条真实问答，字段 source_case/run_id/aspects/required_evidence(type+path/symbol)/allowed_supplement/forbidden_substitute/expected_mode_p15/p15_expectation/p16_target/key_paths/key_symbols）、`contract_dev_extended.jsonl`（8 条反例：误杀负例 e02/e03、自然问法 e01、真无资料 e04、格式未摄取 e05、全局否定 e06、注入改写 e07、跨 project fail-fast e08）、`contract_holdout.jsonl`（6 条，已封存、实现期 T22–T31 不查阅）、`mechanism_checks.md`（14–18 走 §5.2 确定性层）。用户 2026-07-24 逐题确认全部 13 条 P1.5 契约预期（c06/c07/c09/c11/c12/c13 承诺实质修复、c04 正向基线、接受 RT-20 partial 优先取舍、c01/c02/c05/c08/c10 诚实天花板）。三份 JSONL 逐行 JSON 校验通过、`confirmed_at=2026-07-24`；未运行任何 P1.5 dev、未改 src/Prompt、未触碰 P1 holdout（证据：本提交）
- [ ] U3.2 P1.5 验收时人工复核代表性回答与完整轨迹
  - 判据：至少覆盖 full、跨轮补证后 partial、假拒答被修正为 partial、policy-refusal、fail-fast 404 各一种；逐引用打开真实路径/行号；核对 mode/正文/not_found/limitations 一致；结果写入 `evalsets/reports/p1.5-manual-check.md`
- [ ] U3.3 P1.5 holdout 运行授权
  - 判据：确认实现/Prompt/阈值已冻结、被测系统自 dev Gate 起零变更后，用户明确同意才运行一次 v1.5 holdout；不据结果调参

## T22 证据类型硬约束与权威性分层（RT-01/05）

- [x] T22.1 required-evidence 结构化模型
  - 确定性代码从用户问题解析被明确要求的证据类型/路径（生产源码/配置/迁移/引用某类/测试等），构造 required-evidence 清单；plan/evaluate 结构化输出增加逐项覆盖状态字段（不新增在线调用点）
  - 判据：schema 严格 Pydantic、`PROMPT_VERSION` 递增并快照；解析以确定性规则为主，plan 只可回显确认且每个 required 项须锚定问题原文片段，LLM 不能覆盖或伪造 required 清单；解析对"请引用 X 与 Y""不要用测试代替实现"等表述有确定性单测；解析规则不得只针对回归集单句特判，须过"自然问法"反例
  - ⚠️ 2026-07-24 第二轮复审再次撤销：首版整改仍有 4 处可复现缺陷（子句切 `.` 截断显式路径→错误 full、裸文件名子串误配、否定整句级删除正确生产要求、type-only anchor 非原文 + 无序 set 决定 item_id + 类名误判 migration）。已在本提交二次整改（见偏差记录 2026-07-24 T22 二审）并用原始场景 + 跨 PYTHONHASHSEED 自验，**待第三轮独立复审确认后再勾选**
  - ⚠️ 2026-07-25 第三/四轮复审：二次整改（fc16b66）经第三轮又发现 5 处组合错误 full（三审），三次整改（a6583f8）经第四轮再发现窄修正则治标不治本——改采**结构性 fail-closed**（用户裁决）
  - ⚠️ 2026-07-25 第五轮复审撤销：三态方向确认正确，但"解析漏检必进 unresolved"未成立——`_extract` 按整段判定，一个目标命中即吞掉同义务其余目标（`请引用 Foo.java 和关键实现`→complete、c08"相关 Java 配置"被吞、c02 整题 none）；裸 README 退化 type-only 被任意 current_doc 顶替；"OrderTest 的生产实现"被判 test 再补泛化生产项；否定约束无消费者（死信号）；unresolved 说明只在单一 finalize 分支产生；required 可达 11 项却仍报 complete
  - ✅ 2026-07-25 **约束跨度账本**重写（本提交，按五审 7 条路线裁决）：义务跨度独立检测（强指令/弱指令/**核验比较谓词**，后者对象在标记左侧）→ 义务对象按连接词逐段结算为 resolved / unresolved / 明确非目标尾语，**不存在整段 produced**；裸 `README/PROGRESS/CHANGELOG` 保身份、`相关 Java 配置` 直接 unresolved；点名类的显式生产语境优先于 `*Test` 命名约定；`MAX_REQUIRED_ITEMS=10` 与 plan/evaluate schema 同源、超限记 `partial_enumeration`（有防漂移单测）。自查另补：否定列表跨顿号不得截断、`引用` 名词用法不得当祈使指令
  - ✅ 2026-07-25 第六轮独立复审裁定"T22.1 可以验收"，据此勾选：解析层六处发现全部收敛，独立复跑解析/schema/state 单测 129 passed、Prompt/契约/answer 单测 9 passed、Ruff+Pyright 通过；`PROMPT_VERSION`=p1.5-agent-v3（快照 SHA `6c838462…`）、严格 Pydantic、anchor 全为原文子串、自然问法反例通过。证据 commit：3576bf0（约束跨度账本）
  - ✅ 2026-07-25 第四轮结构性重写（历史记录）：解析产出**确定性三态** `RequiredEvidence.status`（none/complete/ambiguous）；漏检/部分解析/未知语法目标进入 `unresolved_constraints`（原文 anchor + reason 枚举 unparsed_target/partial_enumeration/unsupported_syntax），永不进 coverage、不伪造 type=other item。路径改 ASCII 边界 file-token 遮蔽（无空格中文不吞、`.txt/.sql/.vue/.ts/.tsx` 齐全、未知 ext→unsupported_syntax）；逗号/顿号列表按同指令延续解析；单词类名/`X 类`/URLParser 可解析、泛化角色词(Repository/DTO)→unresolved；强/弱指令作用域（弱指令须邻接证据词）；匹配大小写敏感（ext4）。对抗单测 + §五.A 图级 8 反例 + 跨 5 PYTHONHASHSEED 不变量
- [x] T22.2 full/sufficient 逐类硬约束
  - 判据：每个必需证据类型都有直接匹配引用才允许 `full`；缺任一必需类型时确定性最高判 `partial`；测试/设计/dev-log 不占用必需生产证据预算；Fake 矩阵覆盖"仅测试代替生产→不得 full""必需类型齐全→可 full"
  - ✅ 2026-07-25 收尾勾选（用户 2026-07-25 指示"先把 T22 完全收尾"）：六审唯一阻塞项（双引用账本）已在 b5de64c 修复，本次收尾按判据逐条自查复核——①先在 b5de64c 上复跑六审原场景（"不要引用设计文档，只引用生产源码" + 正文 `[E1][E2]` + claim 仅 E1）确认已判 `partial` + `finalize: 引用了被禁止的证据类型` + 用户可见说明；②另跑 10 条 finalize 硬门对抗探针（仅测试代替生产 / dev-log 冒充生产 / 两点名类只覆盖一个 / 配置类点名给 .md / 越界 `[E9]` / 禁引类型只在 claim / 只在正文 / 必需齐全应 full）全部符合判据，无错误 full；③补齐判据字面要求的 Fake 矩阵两条替代表述用例（"不要用测试代替生产源码"+仅测试证据→partial、"请引用生产源码…不要用测试代替"+生产证据→full）。`make ci`（ruff/pyright 0、pytest 440）、`make eval-ci`（65+19）全绿。**诚实标注：本轮为我方自查复核，非第七轮独立复审**；证据 commit：b5de64c + 本提交
  - ⚠️ 2026-07-24 第二轮复审再次撤销：finalize full 门虽逐项，但 `route_after_evaluate` 未读确定性覆盖——必需项未覆盖却 LLM 判 sufficient 时会跳过补检直奔 generate（发现4）；已在本提交补齐（待第三轮复审）
  - ✅ 二次整改后（本提交）：`route_after_evaluate` 增加确定性覆盖缺口判定——有 required 缺口且预算允许时优先 refine（即便 sufficient），full 门/refine 缺口/finalize 全部只读确定性覆盖。图级测试：覆盖缺口先补检再降级 partial、LLM 漏报 required/coverage 记诊断但确定性 full 不受影响
  - ⚠️ 2026-07-25 第六轮复审：解析层已合格，但 full 门仍有 1 处可稳定复现的错误 full——`finalize` 丢弃 `apply_l0` 返回的正文有效引用编号，禁引检查只看保留 claim 的 `evidence_ids`；而 L0/L1 不要求正文 `[E#]` 出现在 claim 中。复现：问"不要引用设计文档，只引用生产源码"，正文写 `[E1][E2]`、claim 只申报 E1 → verification 通过、正文保留 `[E2]`、`final_mode=full`、无禁引告警。**T22.2 暂不验收**
  - ✅ 2026-07-25 双引用账本修复（本提交，最小整改、未动解析器）：`required_evidence_tail(required, support_citations, visible_citations)` 明确拆两套账本——必需证据覆盖只认保留 claim 的引用（正文裸标记不得反过来满足点名要求），禁止引用检查认"claim 引用 ∪ 正文过 L0 后仍保留的 `[E#]`"；`generate_failed` 分支的正文也补跑 `apply_l0`，越界标记不随降级路径漏出。新增 2 条图级测试（正文含 E2/claim 仅 E1 → partial + 告警；可见标记不得覆盖必需项）与 2 条 tail 单元测试，前者在 3576bf0 上确认失败
  - ⚠️ 2026-07-25 第五轮复审撤销：硬门本身成立，但输入不可信——解析漏检使门真空化（c02 整题 none、c08 只剩 3 项）；`forbidden_substitute_types` 无任何消费者，"不要引用设计文档"同时引用生产源码与设计文档仍判满足
  - ✅ 2026-07-25 两类否定真正执行（本提交）：`forbidden_citation_types` 与 `forbidden_substitute_types` 分开落库并分开执行——`forbidden_citation_hits` 进入 `required_satisfied`（被禁类型被直接引用即不得 full）并写入 generate Prompt；"只能作为补充"/"不要用 X 代替 Y"/"不要只引用 X" 仍只禁替代、允许补充引用。`finalize` 抽出公共收尾 `required_evidence_tail`：覆盖缺口/unresolved/禁止引用三类说明对 draft is None、generate_failed、claim 被移除、正常四条分支统一生成
  - ✅ 2026-07-25 第四轮结构性 fail-closed（历史记录）：`full` 必要条件 = `required_satisfied`（resolved 全覆盖 **且** 无 unresolved）；`route_after_evaluate` 把 unresolved 也当覆盖缺口优先 refine（anchor 进补检查询）；finalize 有 unresolved 时最高 partial（保留 claims + 确定性限制说明，不静默降级）。§五.A 8 条图级反例（否定跨句/逗号枚举/单词类名/`.txt`/大小写伪 test/大小写匹配/resolved+unresolved）全绿
- [x] T22.3 权威性分层
  - 判据：区分 current docs / production source·config / tests / historical plans·audits / dev-log；规划材料只支撑"规划过/范围状态"、不支撑"已实现"；分层规则有单测；具体权重变更须经 Evaluation-v1.5 对照，不凭个案
  - ⚠️ 2026-07-25 第三轮复审撤销：`classify_path` 仍用 `base.startswith("application.")`——`application.md/.java/.txt` 被误判 production_config，与"只匹配配置扩展名"声明矛盾，测试证据可凑 full（三审发现3）；已在本提交删除裸前缀（仅按 `.yml/.yaml/.properties` 扩展名），补 3 个负例，待第四轮复审
  - ⚠️ 2026-07-25 第四轮复审撤销：`base.endswith("test.java")` 在 lower() 后伪命中——`src/main/.../Contest.java`、`Latest.java` 被判 test，只引生产 Contest.java 满足"对应测试"→错误 full（四审发现5）
  - ⚠️ 2026-07-25 第五轮复审撤销：文件名启发式仍先于目录判断——`src/main/java/AuditService.java`→historical_plan、`ChangelogService.java`→dev_log、非 src/main 的 `Testimony.java`/`Testing.java`→test，生产文件可满足对应非生产 type-only 要求并放行 full
  - ✅ 2026-07-25 第六轮独立复审裁定"T22.3 可以验收"，据此勾选：分层规则（可信目录 > 生产目录 > 文档专属文件名约定 > 词边界 Test 约定）有 40+ 条参数化单测，生产文件不再冒充 test/historical_plan/dev_log；权重未凭个案变更（检索层 metadata 权重仍属 P1.6）。证据 commit：3576bf0
  - ✅ 2026-07-25 先目录/扩展名后文件名（3576bf0）：audit/changelog/plan/review/roadmap 等**文件名**约定只作用于文档扩展名（`.md/.txt/.rst/.adoc`）；`Test*` 改词边界规则 `^Test(?![a-z])` / `[a-z0-9]Tests?$` / `(?<![A-Z])ITs?$`——Testing/Testimony/SPLIT/Contest/Latest 不是测试，`TestOrderService`/`FooSpec`/`AuditIT`/`OrderTest`(非生产目录) 仍是；13 条参数化负例
  - ✅ 2026-07-25 第四轮结构性重写（历史记录）：`classify_path` 明确目录优先级——可信 test 目录 > 生产目录(src/main) > `*Test/*Tests/*IT/*Spec` **大小写敏感**命名约定；`Contest/Latest`(main)→production_source、`OrderTest.java`(main)→production_source、`src/test/Order.java`→test、根级 `OrderTest.java`→test；后缀 lexer 与 `SUPPORTED_SUFFIXES` 对齐并有防漂移单测
  - ✅ 2026-07-24 `classify_path` 可信目录优先（test/design/plans/review·audit/dev-log 先判，再在生产目录内识别 source/config/migration）：`src/test/**.sql`→test、`docs/**flyway*.md`→historical_plan。冲突矩阵单测；`PRODUCTION_TYPES` 保证测试/设计/规划不替代生产

## T23 not_found 四分类与事实校验（RT-02/15）

- [x] T23.1 四分类枚举与三类实现
  - 判据：枚举 `missing_from_current_evidence`/`unsupported_or_not_ingested`/`confirmed_undocumented`/`forbidden_or_unavailable_by_policy`，以 Answer JSON 平行字段 `not_found_details` 承载（`not_found` 保持 `list[str]`，见规格 §2）；P1.5 实现前二类 + 策略类（后者见 T27）；`unsupported_or_not_ingested` 由 `SUPPORTED_SUFFIXES` + documents 表确定性判定（含有限且单测覆盖的技术词→后缀映射表），措辞用条件式、不得断言仓库有无该文件（仓库级普查属 P1.6）并有单测；`confirmed_undocumented`/全局不存在需 inventory，P1.5 不得断言，降级为"当前证据不足以穷举确认"
  - ✅ 2026-07-25 新增 `src/devkb/agent/not_found.py`（确定性、零 LLM 调用）：四类枚举齐备，`NotFoundDetail` 严格 Pydantic（text/category/source/**basis**+**refs** 为事实校验来源/original_text 留档），经 `AgentState.final_not_found_details` 落 Answer JSON 平行字段 `not_found_details`；`not_found` 仍为 `list[str]` 且与 details 逐条一一对应（有单测）。P1.5 只产出前二类：`confirmed_undocumented` 有"永不产出"单测，策略类留 T27。`unsupported_or_not_ingested` = `SUPPORTED_SUFFIXES` + documents 表快照（`CorpusProfile` ← `DocumentRepo.list_active_rel_paths`，一次只读查询、硬上限 `MAX_CORPUS_PATHS=5000`、未取得快照即 fail-closed、"未命中"永不作结论）+ **9 条封闭技术词→后缀表**（表外词如 Python/Kotlin/Rust 不推断，有负例）；另加"隐藏文件恒不在摄取范围"与"包名 `com.example` 不得被当作 `.example` 后缀"两条词法防御。措辞固定条件式（"不在当前摄取范围、未纳入本项目索引…无法据此确认仓库是否包含此类文件"），有"不得出现仓库中没有/仓库无源码"的单测；全局否定/穷举断言降级为"当前证据不足以穷举确认"。`PROMPT_VERSION`→p1.5-agent-v4（generate 增 not_found 条件式措辞指令，快照 SHA `96242198…`）、`AGENT_STATE_SCHEMA_VERSION`→v5、`ANSWER_SCHEMA_VERSION`→p1.5-answer-v2。取舍见偏差记录 2026-07-25 T23 分类锚点（用户 2026-07-25 已裁决采用目标级锚定）
  - ⚠️ 2026-07-25 复审未通过（拆分逻辑两处阻断点）：①同原因多目标只保留 `members[0].refs`——"数据库迁移、OrderService 与 PaymentService"里 `PaymentService.java` 从 refs 与告警中消失；②单字连接词在任意位置匹配，"涉及/或者/与此同时/参与"被误切并把无信号残段挪到错误位置（"数据库迁移的字段定义未找到涉…"）。已在本提交修复（见下条）
  - ✅ 2026-07-25 **一条一原因**（用户裁决后补强）：`_assess` 逐目标段结算，单条明细的 `category`/`basis`/`refs` 恒自洽；LLM 把两类缺口写成一句时按连接词确定性拆分（括号内不拆、确定性说明不拆、同类不拆）。复审两处阻断点已修：同原因成员的 refs **全量合并**（文案最多列 3 条 + "等 N 条"，refs 与告警不截断）、单字连接词（及/与/或/和）加左右封闭屏蔽表且句首/句尾不算连接处、无信号残段并入**紧邻前一段**（保序，不再挪到首条）。测试补至 55 条：拆分正确性 + `category`↔`basis`↔`refs` 自洽 + **文本中每个目标都有对应事实来源** + 自然连接词不切 + 真连接词仍切 + 保序与残段归位 + 不过度拆分
- [x] T23.2 全检索轮历史事实校验
  - 判据：出现在任一轮证据路径的 active 文件不得被写成"不存在"；前轮已支持方面不得在后轮凭空升级为缺失；用户问题未给出的路径/类名/字段名与"源码不存在"强断言必须有证据支撑，否则只陈述当前证据不足；Fake 矩阵含"前轮出现过该文件→不得报缺失"
  - ✅ 2026-07-25 `evidence_path_history`（每轮 retrieve 累积，reducer=`operator.add`，被后轮挤出也留痕）与 `supported_aspect_history`（每轮 evaluate 累积）进 AgentState；finalize 对**所有**终态分支统一跑 `calibrate_not_found`：①任一轮证据里出现过、或 documents 表中 active 的路径被写成缺失 → 整条改写为确定性事实句（主语用去缺失标记后的方面名，"未找到/不存在"字样消失，方法级缺口信息保留）+ 告警 + `original_text` 留档；②无证据支撑的仓库级否定强断言 → 降级条件式；③前轮已支持方面在后轮被报缺失 → 剔除并告警。确定性 required-evidence 说明只追加披露、不被改写。测试：36 条 unit（分类/事实校验/边界）+ 4 条图级 Fake 矩阵（判据要求的"前轮出现过该文件→不得报缺失"、c10 型"迁移 .sql 未摄取 vs 已索引 Java 未召回"分类分离、前轮已支持不得翻转、Answer JSON 平行字段）+ 2 条集成（真实 PG：`list_active_rel_paths` 只含 active/受限/跨项目不可见；service 注入语料快照后 `corpus_index` 与 `static_suffix_rule` 两类同时出现）。改动前行为已在 2716c3f 上跑出原始输出留证（"源码中不存在 RagService"原样交付、两类缺口不可分、前轮已支持方面照报）。`make ci`（ruff/pyright 0、pytest 482）、`make eval-ci`（65+19）全绿

## T24 跨轮覆盖单调性与 partial 保留（RT-16/20）

- [ ] T24.1 证据方面（aspect）分解与逐方面最佳保留
  - 判据：用户要求拆为 aspects，跨轮逐 aspect 保留最佳证据并记录淘汰原因；最终充分性按单调覆盖矩阵判定；某 aspect 由"已支持"退为"缺失"必须有"原证据被证伪"理由，仅被新轮挤出不算
- [ ] T24.2 partial 保留优先于全量 refusal
  - 判据：≥1 aspect 有直接证据即确定性交付范围准确 `partial`；`refusal` 仅用于零可交付证据；Fake 矩阵精确复现案例九（第一轮 support=2、补 CitationParser 时勿丢 RagService；即使仍缺 RagSupport 也交付 partial 而非全空）

## T25 节点一致性与 finalize 覆盖差分（RT-03/08）

- [ ] T25.1 finalize 前确定性一致性检查
  - 判据：`mode`/正文/`claims`/`not_found`/`limitations` 相符；正文声称已证明的内容不得同时出现在 `not_found`；finalize 不得无事实校验注入 evaluator 的 missing；冲突用例有确定性断言
- [ ] T25.2 第二稿覆盖与一致性复检
  - 判据：regen 后除重过 L0/L1 外，重做覆盖度与一致性检查；必要时准确降级而非交付错误缺口；Fake 矩阵复现案例二（重生成后 not_found 不得由 1 增至 2 且含 active 文件）

## T26 partial 认知口径与安全/配置覆盖矩阵（RT-18/19）

- [ ] T26.1 partial 正文限定已验证范围
  - 判据：partial 正文与总结只对已验证资源/操作下结论，不对未覆盖方面作绝对断言；复现案例七（不得一边承认两路径未确认、一边断言"无法访问任何资源"）
- [ ] T26.2 安全类 resource×operation×enforcement-layer 矩阵
  - 判据：安全题输出该矩阵，区分"Repository 语句自身过滤"与"Service 前置校验后调用非 owner-scoped 语句"；不混为统一双重防线
- [ ] T26.3 配置类分层校验
  - 判据：配置题分别验证 scope/fallback/required·non-empty/length·strength/runtime-validation；错误消息或文档建议不得冒充代码强制条件；复现案例八（Compose `${VAR:?}` 非空 ≠ secret 强度/生产安全）

## T27 policy-refusal 终态（RT-22）

- [ ] T27.1 前置识别与独立终态
  - 判据：明确索取系统 Prompt/凭据/隐藏文件内容或要求越权执行命令的请求，在检索潜在敏感语料与调用任何工具之前返回独立 `policy_refusal`（mode 第四枚举值，见规格 §2）；识别 = 确定性规则前置（命中零 LLM 调用）+ plan policy 标志兜底（不新增调用点，retrieve 前短路）；照常落一个终态 `policy_refusal` 的 run（零 tool invocation、记录触发层 rule/plan）以保可审计；固定无秘密文案；不检索、不生成、不执行、不编造；受保护对象不得写入普通 `not_found`
- [ ] T27.2 召回率与误杀反例
  - 判据：用多种攻击改写、直接索密、角色覆盖、文档内注入、要求编造/执行命令验证召回；用合法安全咨询（"如何保护 API key"）与普通 `.env.example` 文档题验证不误杀；分别报告泄露率、越权工具调用、策略漏拦与误杀

## T28 摄取覆盖披露（案例三/十廉价版）

- [ ] T28.1 refusal/partial 附静态覆盖披露
  - 判据：拒答/部分回答说明本项目摄取/不摄取的后缀，使"格式未摄取"可与"仓库确实无源码"区分；为静态能力披露，不做逐题语言/意图推断（属 P1.6）；披露文案有单测

## T29 unknown project fail-fast 顺序（RT-23）

- [ ] T29.1 project 解析先于模型初始化
  - 判据：输入校验与 `_resolve_project()` 先于 `_get_embedder()`/`_get_llm()`；unknown project 在任何模型/client 初始化前返回结构化 404、统一脱敏错误体、零 run/step/tool、零外部调用
- [ ] T29.2 冷服务 loader spy 回归
  - 判据：新增冷 AppService/API 测试，以 loader/factory spy 断言 unknown project 时 embedder/LLM factory 均 0 次调用；现有注入已实例化 Fake 的测试无法覆盖，须补 spy；保持 404/脱敏/零持久化

## T30 诊断轨迹最小版与 warning 归属（RT-07/10）

- [ ] T30.1 有界逐候选轨迹
  - 判据：每轮保存有界 per-query 候选元数据（chunk_id/rel_path/line range/rank/score/融合·截断原因），使"未召回/低排名被截断/被融合淘汰/已给模型但未采用"可事后区分；候选条数有冻结为常量的硬上限，不得无界；需要建表/加列时新增可逆 Alembic 迁移 0004；refine term 记来源；finalize 注入的 missing 记来源与事实校验结果；继续不保存正文与 secret
- [ ] T30.2 warning attempt/已解决区分
  - 判据：区分 `resolved_warnings` 与 final active warnings，并带 node/attempt；复现案例二/八（第二稿已过 L0/L1 时不得再把历史 `verify:l0_l1_failed` 当作最终未通过展示）

## T31 Evaluation v1.5 harness 与 dev Gate

- [ ] T31.1 harness 扩展与分环节统计
  - 判据：新增 not_found 分类准确率、跨轮覆盖单调性、finalize 一致性、policy-refusal 召回/误杀、fail-fast 断言；分开报告 retrieval / evidence-selection / claim-support / final-consistency，不用总分掩盖失败环节；并入 `make eval-ci`
- [ ] T31.2 v1.5 dev Gate 真实运行
  - 判据：真实 DeepSeek 在 v1.5 dev 上运行一次，13 条真实问答复现达到各自 **P1.5 契约预期**（两栏预期见《Evaluation-v1.5》§2）；固定复现 14–18 的机制检查在确定性层全绿；P0/P1 兼容回归与 L0/L1 继续全绿；报告落盘不覆盖旧结果，不在 dev 上为单题过拟合调参

## T32 P1.5 收尾与 Gate

- [ ] T32.1 README/文档口径更新
  - 判据：README 增加四分类 not_found、policy-refusal、覆盖披露与"能力边界诚实声明"口径；明确 P1.5 不具备仓库级审计/工具调用（属 P1.6）；无未实现能力措辞
- [ ] T32.2 P1.5 验收总清单
  - 判据：逐条核对《P1.5实现规格》§15；所有证据链接到测试、报告、run_id 或命令；无"计划中/待补"被勾选；所有偏差有用户裁决
- [ ] T32.3 v1.5 holdout（依赖 U3.3）
  - 判据：一次性运行并留档；不据结果回改 Prompt/阈值/权重/标注
- [ ] T32.4 当前头提交远端 CI 全绿
  - 判据：GitHub Actions 当前 P1.5 头提交全绿，不以较早提交代替
- [ ] T32.5 P1.5 Gate 关闭
  - 判据：用户确认 P1.5 验收；之后才允许创建 P1.6 实现规格/任务清单与 D6 窄修订 ADR

## P1.5 验收总清单

对《P1.5实现规格》§15 逐条核对（勾选前须全部满足）：

- [ ] 1. Evaluation-v1.5 的 13 条真实问答复现全部达到各自 P1.5 契约预期（P1.6 目标预期仅记录）；14–18 机制检查全绿
- [ ] 2. 不再把 active/indexed 文件断言为不存在；missing/not_found 不含未经证据支持的路径/类名/字段
- [ ] 3. 用户明确要求的证据类型未满足时绝不判 `full`；测试/设计/dev-log 不替代必需生产证据
- [ ] 4. mode/正文/claims/not_found/limitations 一致；≥1 方面有直接证据时交付范围准确 partial，不整体清空
- [ ] 5. Injection/秘密提取/越权命令进入独立 `policy_refusal`，无泄露/执行/敏感检索；合法安全咨询不误杀
- [ ] 6. 全局否定/穷举题给出诚实能力边界声明（不假拒答、不假断言"不存在"）
- [ ] 7. 格式未摄取导致的缺失标为 `unsupported_or_not_ingested` 并附覆盖披露
- [ ] 8. unknown project 在加载 embedder/LLM 前 fail-fast 404，loader spy 证明两 factory 均 0 次
- [ ] 9. 新 v1.5 holdout 只运行一次并留档，不据结果调参
- [ ] 10. P0/P1 兼容回归 + 本地 `make ci && make eval-ci` + 当前头提交 GitHub Actions 全绿；`PROMPT_VERSION` 递增已记录

**明确不作为 P1.5 出口**（属 P1.6）：Controller 穷举 6 类/26 endpoint、`.vue/.ts/.sql` 已摄取、动态工具调用可用。

## 偏差记录

| 日期 | 任务项 | 冲突/缺失描述 | 建议 | 用户裁决 |
|---|---|---|---|---|
| 2026-07-24 | T22 复审 | 首版 T22（commit 26777fd）经代码复审确认 4 处缺陷并撤销勾选，均可稳定复现：①硬门只比类型集合、丢弃点名路径/符号——点名 3 个类却引用无关生产文件仍判 `full`（错误 full）；②evaluate 逐项覆盖未实现、`RequiredEvidence` 为 dataclass 非严格 Pydantic；③`classify_path` 先判 `.sql/flyway/application` 后判可信目录——`src/test/.../*.sql`、`flyway-audit.md` 被判为生产 migration；④解析否定反转（"不要引用设计文档"→必需 design、"计划文档…规划过"→禁止 plan）与漏识别（"对应测试"）。附带规格张力：T22.1 判据"plan/**evaluate** 结构化输出增加逐项覆盖状态字段"与 D6"控制流由确定性代码判定"——逐项覆盖本质是确定性计算 | 整改：required-evidence 改逐项严格 Pydantic 模型（type+原文 anchor+可选 path/symbol，同类型多点名不合并），finalize 逐项匹配直接引用；`classify_path` 可信目录优先（test/design/plan/audit/dev-log 先判，再在生产目录内识别 source/config/migration）；解析按子句与否定范围重写并补冲突矩阵测试；删除"under-detect 仍安全"假设。evaluate 逐项覆盖：建议确定性代码为权威门、evaluate 可选报告仅驱动 refine（并存、确定性为准） | ✅ 2026-07-24 用户裁决：采用「A 的 schema + B 的确定性裁决」——plan/evaluate 按 A 增加逐项字段，但 RequiredEvidenceItem 与 coverage 的裁决权全在确定性代码，LLM 报告仅诊断/驱动查询，不得把 missing 改成 covered、不得增删 required item。已按此整改并用复审原始场景逐条重验（本提交） |
| 2026-07-24 | T22 二审 | 第一轮整改（commit a1f3f37）经第二轮独立复审仍确认 4 处可复现缺陷，再次撤销 T22.1/T22.2（T22.3 分类修复确认有效、保留）：①`_CLAUSE_SPLIT` 含英文 `.`、分句先于路径提取——`a/b/Foo.java` 被切成 `Foo`+`java`，显式路径退化 type-only → 引用 `NotFoo.java` 仍 `full`；且路径匹配用子串，裸 `foo.java` ⊂ `notfoo.java`；②否定作用域整句级——"不要用测试代替生产源码，请引用生产源码"把 production_source 一并加入 forbidden 再全局删除，required 变空 → 引用 `FooTest.java` 仍 `full`；③type-only anchor 用规范标签非原文、用无序 `set` 决定 item_id（PYTHONHASHSEED 敏感）、同句"数据库迁移"使 `DocumentService` 被 `_infer_symbol_type` 误判 migration；④"A schema + B 裁决"只落到 finalize——`route_after_evaluate` 未读确定性 coverage，必需项未覆盖却 sufficient 时跳过 refine；evaluate/plan 只查 LLM 已返回项，全省略时不告警 | 补红测后整改：分句不切 `.`；路径按完整后缀 / 裸名 basename 全等匹配；否定改子句内"用 X 代替 Y"（X 禁止、Y 肯定必需），禁止类型不删其它子句的 required；类型检测按原文位置排序产出 (type, 原文 anchor, span)、item_id 依位置稳定、点名类默认 production_source；`route_after_evaluate` 读确定性覆盖缺口优先 refine；plan/evaluate 完整回显校验、遗漏也记诊断 mismatch | ✅ 2026-07-24 已按整改二次修复（本提交）：4 处均补对抗单测 + 图级测试并用原始场景 + 跨 5 个 PYTHONHASHSEED 逐条自验（item_id→type 完全一致）；`make ci` 327、`make eval-ci` 65+19 全绿。**因勾选纪律 + 上轮曾误勾，T22.1/T22.2 暂不勾选，待第三轮独立复审确认** |
| 2026-07-25 | T22 三审 | 第二轮整改（commit fc16b66）经第三轮复审：二审 4 个原始用例已过，但**组合场景**仍稳定错误 full，撤销 T22.1/T22.2/**T22.3**（三者全撤）：①删英文 `.` 分句致否定跨句污染——"不要引用测试. 请引用 Foo.java" 整句判禁止，`Foo.java` 未成必需，仅引 `FooTest.java` 仍 full；②`_TYPE_PATTERNS` 漏"生产配置"——"请引用生产配置"→required 空→测试证据凑 full；③`classify_path` 仍 `base.startswith("application.")`——`application.md/.java/.txt` 误判 production_config；④非否定子句无条件抽 CamelCase——"消息队列（如 Kafka/RabbitMQ）"把 RabbitMQ 当必需点名类；⑤evaluate 只查 LLM 多报 evidence、漏报（covered=true 但 evidence_ids=[]）不记 mismatch | 先补红测再整改：路径 token 先遮蔽为无点占位符再按句界（含 `.`）分句、逐子句还原（否定停在句号）；`_TYPE_PATTERNS` 加"生产配置"；删 `application.` 裸前缀只按配置扩展名；点名硬约束限定在带"引用/根据"指令的子句（替代结构 Y 侧强制提取）；evaluate 覆盖用 evidence 集合全等比较 | ✅ 2026-07-25 已按整改三次修复（本提交）：5 处 + 自查发现的"替代 Y 侧裸文件名漏必需"共补对抗单测 + 图级测试，用三审原始场景 + 跨 5 个 PYTHONHASHSEED 逐条自验；`make ci` 339、`make eval-ci` 65+19 全绿。**T22.1/T22.2/T22.3 全部暂不勾选，待第四轮独立复审确认** |
| 2026-07-25 | T22 四审 | 三次整改（commit a6583f8）经第四轮复审：二审用例仍过，但**组合场景**又 5 处稳定错误 full——①遮蔽正则用 Unicode `\w` 吞无空格中文（"不要引用测试.请引用Foo.java"→required 空→full；"Foo.java和Bar.java"被吞成一个）；②逗号枚举后续项无重复指令即丢失；③单词/缩写类名漏检（"Order 类""URLParser"→退化 type-only）；④显式路径后缀不全（`.txt` 漏）；⑤`base.endswith("test.java")` 大小写伪命中（Contest/Latest→test）；⑥path/symbol 匹配忽略大小写（Foo.java≡foo.java）。根因：窄修正则治标不治本，解析器 under-detect 即自动放行 full | 用户裁决：**改采结构性 fail-closed**——`RequiredEvidence` 增 `unresolved_constraints`（严格 Pydantic：原文 anchor + reason 枚举）与确定性三态 `status`；`full` 必要条件 = resolved 全覆盖 **且** 无 unresolved；route 把 unresolved 当缺口优先 refine；finalize 有 unresolved 最高 partial + 确定性限制说明。解析：ASCII 边界 file-token 遮蔽、逗号枚举延续、单词类名/角色词区分、后缀对齐 SUPPORTED_SUFFIXES、大小写敏感、强/弱指令作用域。控制流仍全确定性、不新增在线调用点、单 run ≤6（D6 内）；LLM 不得增删/改写/自报 resolved 或 unresolved | ✅ 2026-07-25 已按裁决实现（本提交）：先红后绿（§五.A 图级 8 反例 + 分类/parser 参数化 + 自然问法 + §五.D 结构不变量，均先在 a6583f8 失败）；`AGENT_STATE_SCHEMA_VERSION`→v3、Prompt 未改故 `PROMPT_VERSION` 不变；`make ci` 376、`make eval-ci` 65+19 全绿；跨 5 PYTHONHASHSEED items+unresolved 完全一致。**T22.1/T22.2/T22.3 仍不勾选，待下一轮独立复审** |
| 2026-07-25 | T22 五审 | 四次整改（commit 3e18503）经第五轮复审：三态硬门方向正确，但**核心性质"解析漏检必进 unresolved"尚未成立**，T22.1/T22.2/T22.3 全撤。6 处可复现缺陷：①部分解析成功后剩余目标静默消失（`_extract` 以整段 `produced` 为准）——"请引用 Foo.java 和关键实现"→complete/无 unresolved；c08"相关 Java 配置"被吞、引用三项即 full；c02 整题 status=none 使硬门真空；②点名身份与上下文类型丢失——裸 README 退化 type-only（`docs/architecture.md` 即可满足）、"OrderTest 的生产实现"被判 test 再补泛化生产项（`src/test/OrderTest.java`+任意生产文件即满足）；③文件名启发式先于目录——`src/main/**/AuditService.java`→historical_plan、`ChangelogService.java`→dev_log、`Testimony/Testing.java`→test；④否定约束是死信号（`forbidden_substitute_types` 无消费者、`required_satisfied` 忽略）；⑤unresolved 说明只在"无 claim 移除"分支产生（draft None / generate_failed / removed 全丢）；⑥required 可 11 项却报 complete，与 plan/evaluate schema 上限 10 冲突 | 路线裁决（复审给出，7 条）：保留三态 + unresolved 硬门，但解析器改**约束跨度账本**——①义务 span 独立检测、与目标解析成败解耦，覆盖 c02 类核验/比较表达；②义务对象按枚举与连接词拆目标段，每段须结算 resolved / unresolved / 明确非目标尾语，禁止整句 produced；③裸 README/PROGRESS 保身份，模糊"相关 Java 配置"直接 unresolved；④分类先精确目录与扩展名，audit/changelog 文件名规则只用于文档，`Test*` 用词边界；⑤拆分并实际执行"禁止替代"与"禁止引用"；⑥required 超 schema 上限须统一扩容或记 partial_enumeration；⑦unresolved 说明集中到 finalize 公共收尾 | ✅ 2026-07-25 已按 7 条裁决实现（本提交）：先在 3e18503 复现 6 处原始输出留证，再重写为约束跨度账本（`_detect_obligations` / `_resolve_region` / `_resolve_one` / `_Ledger`）+ `forbidden_citation_hits` + `required_evidence_tail` + `MAX_REQUIRED_ITEMS` 同源上限。§五.B 10 条图级反例在 3e18503 上 10/10 失败、修复后全绿；另补分类/解析/账本不变量约 30 条与 5 条自然问法反例。自查再补 2 洞（否定列表跨顿号被截断、`引用` 名词用法误当祈使指令）。`PROMPT_VERSION`→p1.5-agent-v3、`AGENT_STATE_SCHEMA_VERSION`→v4、快照 SHA→`6c838462…`；`make ci`（ruff/pyright 0、pytest 434）、`make eval-ci`（65+19）全绿；跨 5 PYTHONHASHSEED 完全一致。**T22.1/T22.2/T22.3 仍不勾选，待第六轮独立复审** |
| 2026-07-25 | T22 六审 | 五审整改（commit 3576bf0）经第六轮复审：六处原始问题与七条整改路线基本合格，**T22.1/T22.3 可以验收**，但 full 门仍剩 1 处可稳定复现的错误 full（P1，**T22.2 暂不验收**）：`finalize` 丢弃 `apply_l0` 返回的正文有效引用编号，禁引检查只用保留 claim 的 `evidence_ids`；`verification.l0_errors` 只校验正文 `[E#]` 存在性、不要求它同时出现在 claim 中。复现：问"不要引用设计文档，只引用生产源码"，E1=生产源码、E2=设计文档，正文 `[E1][E2]`、claim 只申报 E1 → verification=True、正文保留 `[E2]`、`final_mode=full`、无禁引告警。既有测试把 E2 同时写进了 claim，故未覆盖"正文与 claim 账本不一致" | 复审给出最小整改（不再动解析器，只修收尾层）：①必需证据覆盖仍只认保留 claim 的引用；②禁止引用检查改认"claim 引用 ∪ 最终正文实际保留的 `[E#]`"；③补正文含 E2、claim 仅含 E1 的图级红测，必须降 partial 并告警；④可把 `required_evidence_tail` 拆成 `support_citations` 与 `visible_citations`，避免正文标记反过来满足必需证据 | ✅ 2026-07-25 已按最小整改实现（本提交）：先复现原始输出（full + 正文 `[E2]` + 无告警），再把 `required_evidence_tail` 拆为 `support_citations`（覆盖判定）/`visible_citations`（禁引判定）两套账本，`finalize` 各分支保留 `apply_l0` 的有效编号并合入可见账本，`generate_failed` 分支补跑 `apply_l0`。新增 2 图级 + 2 单元测试（在 3576bf0 上确认失败）；解析器与 `PROMPT_VERSION`/`AGENT_STATE_SCHEMA_VERSION` 未动。`make ci`（ruff/pyright 0、pytest 438）、`make eval-ci`（65+19）全绿。T22.1/T22.3 按复审裁定勾选；**T22.2 待复审确认本修复后再勾选** |
| 2026-07-25 | T22 收尾 | 六审只给出 1 处 P1（已在 b5de64c 修复并附红测），但"第七轮独立复审"并未发生；按前六轮的自我约束（自认已修不算数）T22.2 会无限期挂起，阻塞 T23 及其后全部任务 | 由用户裁决是否以"自查复核 + 全绿 CI + 对抗探针"代替第七轮独立复审来关闭 T22.2；勾选行必须显式标注复核性质，不得表述为"已通过独立复审" | ✅ 2026-07-25 用户指示"先将 T22 完全收尾"后勾选：已按判据逐条自查（六审原场景复现为 partial + 禁引告警、10 条 finalize 对抗探针无错误 full、补 2 条判据字面要求的 Fake 矩阵用例），`make ci` 440 / `make eval-ci` 65+19 全绿；勾选行已标注"自查复核，非独立复审"。若后续复审再发现错误 full，按同一纪律撤销重开 |
| 2026-07-25 | T23 分类锚点 | 规格 §5 允许用技术词→后缀映射表"覆盖用户未给路径的场景"。按字面把问题里的技术词套到每条 not_found 上，实测会污染同一题里的其他缺口：c10（"请根据数据库迁移与 DocumentService 说明"）会把已索引 Java 的召回缺口一并标成"格式未摄取"，与判据"与已索引 Java 的证据缺口分类区分"直接冲突 | 技术词只匹配**缺口条目自身文本**；"用户只在问题里点名"的场景改由**确定性 required-evidence 说明**承载——该说明由问题原文解析而来且带类型标签（如"前端源码:Vue"、"数据库迁移:数据库迁移"），分类因此仍锚定用户表述，且不会外溢到别的缺口。为此 `required_evidence_tail` 的缺口说明按证据类型拆成多条（原为合并一条） | ✅ 2026-07-25 用户裁决（在头提交实跑 c03/e05/c10 后给出）：**采用目标级锚定**——问题中的封闭技术词先确定性绑定到对应 required-evidence 缺口，再逐条分类；不得作为整题标签传播到其他 not_found；无法绑定时保守归入当前证据缺口或 unresolved，不恢复整题级兜底。依据：规格 §5 只要求覆盖"未给路径"场景、未要求题级传播，而 Evaluation-v1.5 §2 明确 .sql 必须是 unsupported_or_not_ingested、已索引 Java 必须是 missing_from_current_evidence |
| 2026-07-25 | T23 混合原因条目 | 用户在裁决同时发现的独立边界：LLM 把两类原因写成一句（"当前证据未覆盖数据库迁移与 DocumentService 的删除实现"）时，条目会得到 `category=unsupported_or_not_ingested` 却同时是 `basis=corpus_index` + Java 路径，**内部自相矛盾**；根因是"一条文本只能有一个类别"而分类却按信号叠加 | 补"单条 not_found 混合两类原因"红测，并在 finalize 保证"一条一原因"或确定性拆分；不得用恢复整题级兜底来修 | ✅ 2026-07-25 已按此实现（本提交）：先复现原始矛盾输出，再把分类改成**逐目标段结算**——`_assess` 对每段只产出一个原因（证据事实 > 索引事实 > 摄取范围 > 无从证明的强断言），一条语句出现多个原因即按连接词确定性拆成多条（括号内不拆、确定性说明不拆），同类目标不拆、原文照旧。红测 3 组（拆分正确性、`category`↔`basis`↔`refs` 自洽不变量、不得过度拆分）在修复前 4/4 失败 |
| 2026-07-25 | T23 拆分复审 | 5325ecd 修好了原始矛盾，但新增拆分逻辑本身有两处可复现阻断点：①同一原因下多个目标只记 `members[0].refs`，其余事实来源（PaymentService.java）从 refs 与告警中消失；②单字连接词在任意位置匹配，`涉及/或者/与此同时/参与` 被误切，且无信号残段被挪到错误位置（`数据库迁移的字段定义未找到涉…`）。既有自洽测试只查 refs 形状，未查"文本里每个目标都有事实来源"，也未覆盖自然连接词 | 补两组红测；同原因成员 refs 全量聚合；切分只在真正的目标连接处生效并保序 | ✅ 2026-07-25 已修（本提交）：先复现两处（含 `['未找到 OrderService ', '者 DocumentService 的实现']` 的分词证据）再改——refs 按原文顺序合并去重、文案最多列 3 条并记"等 N 条"、单字连接词加左右封闭屏蔽表且句首/句尾不算连接、残段并入紧邻前一段。红测 7 条修复前全红；另补"文本中每个目标都有对应事实来源"不变量。`make ci` 497 / `make eval-ci` 65+19 全绿 |
