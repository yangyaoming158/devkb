# P1 任务清单（Agentic RAG MVP）

> 开始日期：2026-07-16
> 前置 Gate：P0 已复验通过
> 唯一实现依据：《P1实现规格》；评测依据：《Evaluation-v1》。
> 规则：完成判据全部满足后才勾选，并追加 `✅ YYYY-MM-DD 结果与证据 commit`；规格冲突或缺失写入文末偏差记录，等待用户裁决。

## 执行顺序

```text
U2.1 盲标注审计
 └─→ T11.1 dev 基线
T11 基准/决策
 ├─→ T12 数据层
 │    ├─→ T14 FTS → T15 Hybrid/RRF
 │    └─→ T18 轨迹
 ├─→ T13 Java/config 摄取
 └─→ T16 LangGraph 骨架
          └─→ T17 拒答/L1（同时依赖 T15）
                    ├─→ T18 回放
                    └─→ T19 FastAPI
T20 Evaluation harness 可在 T11 后并行，最终依赖 T15/T17/T19
全部完成 → T21 P1 验收
```

同一时间只推进一个主任务；允许先写失败测试或最小 spike，但不得提前实现 P2。

## U2 用户/人工任务

- [x] U2.1 在首次 P1 dev 运行前完成 Evaluation v1 盲标注审计
  - 判据：只看题目与原语料，复核 31 问的路径/锚点/answerable；为 6 个不可答题冻结 expected mode（refusal/partial）；若有修订，写 `evalsets/v1/label-change-log.md`，不得参考 P1 输出
  - ✅ 2026-07-16 source-only 审计并经用户全部确认：25/25 可答题、35/35 路径/anchor 通过；u01/u02/u03/u05=refusal，u04/u06=partial；v0 JSONL 无变更，证据见 `evalsets/v1/`
- [ ] U2.2 P1 验收时人工复核 5 个代表性回答与完整轨迹
  - 判据：至少覆盖 full、refine 后 full、partial、refusal、L1 重生成各一种；逐引用打开真实路径/行号；结果写入 `evalsets/reports/p1-manual-check.md`
- [ ] U2.3 P1 holdout 运行授权
  - 判据：确认实现/Prompt/阈值/RRF 权重已冻结，用户明确同意后才运行一次 `--confirm-holdout`

## T11 P1 基准、依赖与决策落盘

- [x] T11.1 建立全量 mini-mall 的 P1 前 dev 基线
  - 运行 17 个 retrieval dev 的 `vector-exact`；记录 Recall@5/10、MRR@10、逐题排名、文档/chunk/语料 hash、commit 和配置
  - 判据：原始 JSON + Markdown 报告可由命令复现；不运行 holdout
  - ✅ 2026-07-16 P0 冻结范围全量 39 文档/1370 chunks 上完成 17 题真实 Qwen `vector-exact`：Recall@5=0.647、Recall@10=0.824、MRR@10=0.511，25/25 标注锚点可解析；JSON/Markdown 已留档且 `holdout_accessed=false`（证据：本提交）
- [x] T11.2 新依赖最小 spike 与版本/许可核查
  - 分别验证 LangGraph 状态图、FastAPI async 测试、tree-sitter Java grammar、jieba 确定性分词；选择最小依赖组合并锁入 uv
  - 判据：每项有版本、最小成功样例、维护状态/许可来源与日期；删除一次性 spike 或收敛为正式测试；未引入 P2 依赖
  - ✅ 2026-07-16 锁定 LangGraph 1.2.9/core 1.4.9、FastAPI 0.139.1/Uvicorn 0.51.0、tree-sitter 0.26.0/Java 0.23.5、jieba 0.42.1，HTTPX 0.28.1 仅作 dev 直接依赖；4 个 spike 收敛为正式测试，官方维护/许可来源、转递依赖边界和 jieba 老化风险已留档；无 P2 依赖（证据：本提交）
  - 🔁 2026-07-17 版本修订：tree-sitter 0.26.0 在真实语料上节点遍历内存损坏（SIGSEGV，最小 spike 无法复现），降级锁定 `>=0.25.2,<0.26`，392 个真实 Java 全量解析 0 失败；复现与放开条件记录于 spike 文档（证据：本提交）
- [x] T11.3 转写 ADR-0005、ADR-0006、ADR-0008
  - ADR-0005：project_id 隔离；ADR-0006：LLM/确定性边界与最大 6 次；ADR-0008：引用格式与 L0/L1/L2 分级
  - 判据：内容与《范围冻结与架构决策》D6–D9 一致，不自行扩大范围；编号审计无冲突
  - ✅ 2026-07-17 按冻结 D6–D9 完成三份 ADR 转写：固化 scoped Repository/双项目测试/RLS 推迟、4 类在线 LLM 调用点+供应商请求硬上限 6、引用身份与 L0/L1 在线/L2 离线分级；0001–0009 各一份无编号冲突（证据：本提交）
- [x] T11.4 冻结 P1 Prompt/状态/API 版本标识
  - 记录 Prompt version 命名、AgentState schema version、Answer schema version 和 API version 口径；不创建兼容性框架
  - 判据：代码和报告可输出这些版本；变更时必须产生新的报告而非覆盖旧报告
  - ✅ 2026-07-17 冻结 `p1-agent-v1`、`p1-agent-state-v1`、`p1-answer-v1`、`p1-api-v1`，集中由 `contracts.py` 输出可写入 JSON 的四字段映射；API 路径不加版本前缀，版本变更须生成新报告且不建立兼容性框架（证据：本提交）

## T12 P1 数据层与迁移

- [x] T12.1 设计并实现 P1 Alembic revision
  - chunks 增加 `search_text/search_tsv`、GIN、HNSW；新增 `agent_steps/tool_invocations`
  - 判据：临时库 `upgrade head → downgrade 0001 → upgrade head` 全绿；P0 四表数据不丢；扩展后表/索引与规格一致
  - ✅ 2026-07-17 revision 0002：`search_text` NOT NULL DEFAULT '' + `search_tsv` STORED 生成列（一致性由库保证、降级仅删列）、GIN、部分 HNSW(m=16/ef_construction=64，仅非空 embedding)、`agent_steps`/`tool_invocations` 含 unique(run_id,seq) 与 FK；临时库往返迁移 + schema 一致性 2 项集成测试通过，本地库非破坏 upgrade 后 mini-mall 1370 chunks 与嵌入逐字节完好（证据：本提交）
- [x] T12.2 P0 chunks 搜索字段 backfill
  - 使用与新摄取相同的 token 化函数回填现有数据，不要求重摄或重嵌
  - 判据：1370 个 P0 chunks 的 search_tsv 非空率符合内容预期；重复 backfill 幂等；失败可回滚
  - ✅ 2026-07-17 按规格 §7 实现共用 `tokenize_for_search/build_search_text` 纯函数（golden 全集与冻结留给 T14.1），新增 `devkb backfill-search`；真实库 1370/1370 非空（100%）、重跑更新 0、单事务失败即整体回滚，未触碰 embedding；tsquery 抽查 `orderstatemachine`=6 块、`订单`=110 块符合预期（证据：本提交）
  - 🔁 2026-07-17 复评修复（阻断：新摄取不生成 search_text）：token 化函数迁至 `fts.py`，`replace_for_document` 在插入点统一计算——新摄取/重摄取/backfill 必然同函数，回归测试断言摄取即可词面检索且 backfill 重算 0 更新（证据：本提交）
- [x] T12.3 扩展 Repository
  - 新增 FTS、HNSW/exact 模式、step/tool 写读；所有类继续以 project_id 绑定
  - 判据：SQL 构造仍只存在于 repositories.py；方法不接受外部 project_id；双项目隔离覆盖新表和新查询
  - ✅ 2026-07-17 `lexical_search`（websearch_to_tsquery 全参数绑定 + ts_rank_cd + 确定性并列排序）、`vector_search(mode=exact|hnsw, ef_search)`（set_config 事务级开关强制走顺扫/HNSW，查询后即恢复）、`AgentStepRepo`/`ToolInvocationRepo` 写读均以 project_id 实例化；D7 静态扫描通过，新查询/新表的双项目隔离 4 项测试全绿（证据：本提交）
  - 🔁 2026-07-17 复评修复（阻断：轨迹表放行跨项目/跨 run 错配写入；中等：GUC 恢复写死 'on' 且 ef_search 残留）：新增迁移 0003 复合 FK `(run_id,project_id)→agent_runs`、`(step_id,run_id)→agent_steps`，错配写入库层即 IntegrityError；vector_search 改为记录并恢复调用前 GUC 值（含 hnsw.ef_search），结果不再依赖同事务调用顺序；生产库已升级 0003（证据：本提交）
- [x] T12.4 数据层集成测试
  - 判据：GIN/FTS 查询、HNSW/exact、backfill、run-step-tool FK/顺序/删除行为和跨项目越权全部通过
  - ✅ 2026-07-17 新增 9 项数据层测试：FTS 密度排名/敌意输入注入安全/GIN 计划、HNSW-exact top-10 全等 + 无嵌入块排除 + ef_search 校验/HNSW 计划、step FK 与 unique(run_id,seq)/tool FK、乱序写入按 seq 还原、级联删除不波及无关 run；连同 backfill（2 项）与跨项目越权（4 项）判据全部通过；修复 hnsw 模式需同时关 enable_sort 才能保证真走 HNSW；pytest 90 passed（证据：本提交）
  - 🔁 2026-07-17 复评修复回归补齐：摄取即填充 search_text、跨项目 step/跨 run tool 错配写入被拒（0003）、GUC 前值恢复（含外部预设值不被覆写）3 类新测试；GIN 计划断言改为确定性形状验证（合成小数据上赌 planner 选型不稳定，全形状自然计划留 T14.4 真实语料落盘）；pytest 93 passed（证据：本提交）

## T13 Java 与配置文件摄取

- [x] T13.1 Java parser 与 chunk 模型
  - tree-sitter 识别 package/import、类型、字段、constructor、method；保留真实路径、行号、title_path、签名和邻近注释
  - 判据：仓库 fixture 覆盖 class/interface/enum/record、注解、泛型、嵌套类、超长方法和语法错误；golden 锁边界与行号
  - ✅ 2026-07-17 `ingest/java.py`：package/imports 区、类型头、enum constants、constructor/method/field group/static 块逐成员分块，紧邻 Javadoc/注释吸附，title_path=FQN 面包屑（多顶层类型各用自身 FQN），content 逐字对应源行；4 个 fixture + 4 份 golden + 18 项测试（含语法错误容错与 ParseError）；392 个真实 mini-mall Java 冒烟解析 3306 chunks 零失败（证据：本提交）
- [x] T13.2 Java 超长结构拆分与容错
  - 判据：代码围绕真实语句/行拆分，子块带最小签名上下文；引用内容逐字对应源文件；坏 Java 文件隔离且批次继续
  - ✅ 2026-07-17 超目标成员沿方法体顶层语句边界贪心装箱、单条超长语句按行二分；后续子块 content=签名上下文+子块源行而 start/end_line 只指真实区间（两段均逐字来自源文件，不变量测试锁定）；管道按后缀分派 .java → chunk_java，纯垃圾 Java 标 failed（doc_type=java）批次继续、重摄取幂等；真实语料 392 文件 3468 chunks、164 成员被拆、零失败（证据：本提交）
- [x] T13.3 可复现 mini-mall P1 语料视图
  - 新增版本化 `corpora/mini-mall-p1.toml` 和 `tools/build_corpus_view.py`；复制到 `/tmp` 新目录并生成源 commit/rel_path/hash/type manifest
  - 判据：当前审计基线为 37 docs Markdown + 2 根 README + 392 Java + 14 config = 445 文件；相对路径/字节不变；manifest 明确排除隐藏目录、node_modules、target/build/dist 和任意层级 CLAUDE/AGENTS/TASKMASTER；不使用 symlink、不提交语料副本
  - ✅ 2026-07-17 标准库工具 + 版本化 toml（含 expected 哨兵）落地；真实构建 445 文件（39 md + 392 java + 14 config）与审计基线精确一致，源 commit=cfb49f5（dirty 仅 `.taskmaster/state.json`，本就在排除范围）；隐藏 manifest 记录 commit/rel_path/sha256/type，构建后强制自检指令文件/隐藏/生成物为零、逐文件字节一致；3 项单测覆盖 include/exclude 语义、全新目录与哨兵拦截（证据：本提交）
- [x] T13.4 yml/yaml/properties 纯文本摄取
  - 判据：三种后缀进入摄取、保留行号、从不解析/展开 secret 占位符；隐藏目录和 5MB 限制继续生效
  - ✅ 2026-07-17 新增 `chunk_plaintext`（空行分块+贪心装箱+超长行二分，title_path 恒空——YAML `#` 注释不是标题），三种后缀 doc_type=config 进入摄取；`${RABBIT_PASSWORD}` 等占位符逐字保留且词面可检索、行号逐字契约、隐藏目录跳过、超 5MB 标 failed、重摄取幂等均有测试（证据：本提交）
- [x] T13.5 真语料摄取与幂等
  - 只从 T13.3 生成的语料视图摄取 Markdown + Java + 配置，记录源 commit、manifest hash、文件/chunk/耗时/GPU 内存和失败原因；禁止直接 ingest mini-mall 仓库根目录
  - 判据：摄取前断言指令文件/生成物/依赖目录均不存在；第二次运行新增 0；随机抽查至少 5 个 Java symbol 的路径/行号/正文完全对应；P0 Markdown chunk 不因 Java parser 改动漂移
  - ✅ 2026-07-17 从审计视图真实摄取：406 成功（392 java/3387 块 + 14 config/31 块）、39 P0 Markdown 全跳过零漂移、0 失败、95s、GPU 峰值 7103MB；二次运行 445 全跳过新增 0；随机 6 个 Java chunk（seed=20260717）逐字对照原仓库通过；摄取前断言与 manifest hash/源 commit 见 `benchmarks/p1_ingest_mini_mall.md`（证据：本提交）

## T14 PostgreSQL FTS

- [x] T14.1 实现统一 token 化纯函数
  - jieba CJK、camel/Pascal/snake、路径、数字、点/斜杠/短横线；保留原词和拆分词
  - 判据：中文、英文、Java 符号、RabbitMQ 名称、URL/路径、错误码均有 golden；输出确定性、去重且有长度上限
  - ✅ 2026-07-17 六类判据输入 + 混合查询共 8 case golden 快照人工审阅后冻结（`tests/fixtures/golden_fts/`），另有确定性/小写/长度上限/空输入独立断言；"去重"按规格 §7 第 7 条落为重复 token 确定性上限（MAX_TOKEN_REPEAT=10，保留词频信号），T12.2 已有该测试；函数冻结标注写入 fts.py（变更须审 golden diff + 重跑 backfill-search）（证据：本提交）
- [x] T14.2 摄取期索引与查询期 query 构造
  - 判据：只使用参数绑定；特殊字符/超长输入不会形成 SQL 注入或解析崩溃；active/project_id 过滤始终存在
  - ✅ 2026-07-17 查询构造冻结：`tokenize_query`（同一冻结 token 化函数、去重保序、4096 字符/32 token 硬上限）→ 每 token 绑定参数经 `plainto_tsquery('simple')` → tsquery `||`（OR）合并；OR 语义修复中英混合查询在 websearch AND 下零命中的问题，token 内部词素保持 AND 与索引侧对称；注入/畸形/超长输入测试全过且表完好，failed 文档陈旧 chunks 与跨项目均不可见（摄取期 GIN 索引与插入点 search_text 生成为 T12 既有）（证据：本提交）
  - 🔁 2026-07-17 复评修复：`explain_lexical_search` 撤销 literal_binds + f-string 拼接，改为 `_Explain` 编译构造（EXPLAIN 与内层语句的绑定参数原样执行，`'simple'` 恢复绑定参数）——EXPLAIN 路径与业务路径同样满足"只使用参数绑定"；新增 EXPLAIN 敌意输入回归测试（注入/畸形/空查询，表完好）（证据：本提交）
- [x] T14.3 FTS 排名与解释性字段
  - 判据：结果包含 lexical rank/score/命中 query；已知标识符 fixture 的正确 chunk 排名可精确断言
  - ✅ 2026-07-17 `retrieval.lexical_retrieve` 返回 `LexicalHit`（chunk 元数据 + lexical_rank/ts_rank_cd score/原始 query/query_tokens，§8 第 6 条轨迹解释口径）；受控种子数据断言完整排序与全部解释字段，真实 Java fixture 上 `shouldRetry` 查询精确命中该方法块 rank=1 且行号覆盖 L69（证据：本提交）
- [x] T14.4 FTS 回归与性能记录
  - 判据：真实 dev 的 lexical-only 报告落盘；GIN 查询计划可验证；P50/P95 有记录，不把小样本性能外推为生产结论
  - ✅ 2026-07-17 445 文档/4788 chunks 快照上 17 题 lexical-only 报告落盘（`evalsets/reports/p1-dev-retrieval-lexical-20260717T185636+0800`）：R@10=0.059（token 类 1/6 命中且 rank=1，自然语言 0/11）——负结果如实记录并附根因诊断（中文问题 vs 英文 dev-log 证据的词汇鸿沟；6 个排序变体全部无效，非调参问题；14/17 连 top-100 未进）；GIN 计划经 `explain_lexical_search`（与业务查询同源形状）验证：稀有 token 自然走 Bitmap Index Scan on ix_chunks_search_tsv，宽 OR 按成本正确顺扫；P50=17.6ms/P95=78.3ms（85 样本，报告声明不外推）；宽 OR 高频词问题记 backlog（证据：本提交）
  - 🔁 2026-07-18 复评修复：从干净提交（3a2de50，dirty=false）重新生成报告 `evalsets/reports/p1-dev-retrieval-lexical-20260718T003516+0800`——指标、逐题名次、top-100 诊断与被审报告逐项一致，40901 在绑定参数 EXPLAIN 下仍确认 GIN；排序变体诊断归档为 `benchmarks/p1_lexical_variants_diag.py` + `benchmarks/results/lexical_variants_diag.txt`（重跑与 scratchpad 结果一致），报告措辞改为引用归档物；旧报告按时间戳不覆盖原则保留，证据以本行报告为准（证据：本提交）

## T15 Hybrid、RRF 与 HNSW

- [ ] T15.1 RRF 纯函数
  - 默认 k=60；支持多个 query/channel；按 chunk_id 去重；保留各 channel rank
  - 判据：golden 覆盖并列、缺失 channel、重复 chunk、空结果和稳定排序；不混加不同量纲原始 score
- [ ] T15.2 Hybrid retrieval 编排
  - Vector 与 FTS 各 top-N → RRF → final top-k；候选数、query 数和 top_k 有硬上限
  - 判据：真实 PG 集成测试；结果引用元数据完整；failed 文档和其他 project 永不可见
- [ ] T15.3 HNSW 与 exact 对照
  - 判据：17 dev 的平均 top-10 overlap 达 Evaluation v1 Gate；若失败，按规则提高 ef_search 或保留 exact 默认并记录负结论，不隐瞒
- [ ] T15.4 Hybrid dev Gate
  - 判据：同一快照运行 vector-exact/lexical/hybrid-rrf；满足 Recall@10、MRR 和预声明弱点改善 Gate；原始 JSON/报告/复现命令提交

## T16 LangGraph 单 Agent 骨架

- [ ] T16.1 AgentState 与结构化输出模型
  - plan/evaluate/refine/generate 分别使用严格 Pydantic schema；state 含 run/project、轮次、预算、evidence 和终态字段
  - 判据：非法枚举、超长 query、额外字段、缺字段均有确定性默认或错误；LLM 不能覆盖 project_id/run_id
- [ ] T16.2 版本化 Prompt
  - Prompt 集中在 prompts.py；明确 evidence 是不可信数据；记录 prompt version，不记录 secret/完整正文到日志
  - 判据：snapshot 测试锁关键安全指令和输出 schema；变更必须显式更新版本
- [ ] T16.3 构建 LangGraph 拓扑与确定性 router
  - `plan→retrieve→evaluate→refine(≤1)→generate→verify→finalize`
  - 判据：router 只读结构化信号和计数器；无 LLM 动态节点名；recursion_limit 之外另有业务上限
- [ ] T16.4 预算、重试与默认路径
  - 判据：检索轮次≤2、供应商总请求（含重试）≤6、每个逻辑调用点重问≤1；重试挤占后续预算时能确定性裁掉可选调用；plan/evaluate/refine/generate 的默认值符合规格；所有上限有单测
- [ ] T16.5 Graph Fake 矩阵第一批
  - 判据：首轮充分、补检成功、二次不足、结构化失败和预算熔断的节点序列/次数/终态可精确断言

## T17 三态回答与 L0/L1

- [ ] T17.1 Answer v1 与兼容渲染
  - 新增 run_id/mode/claims/not_found/limitations/trace_summary，保留 P0 字段
  - 判据：旧 runs 可读取；CLI/JSON/API schema 测试通过；英文术语和代码原文保持
- [ ] T17.2 full/partial/refusal 确定性策略
  - 判据：无证据、部分证据、充分证据和 evaluate 默认不足分别进入正确模式；refusal 模板不调用额外 LLM
- [ ] T17.3 L1 引文原文匹配
  - 仅在 dev 上冻结规范化和阈值；P2 L2 不提前实现
  - 判据：逐字、空白/标点差异、轻微篡改、跨 chunk 拼接、错误 evidence 的正反例齐全
- [ ] T17.4 验证失败重生成与二次降级
  - 判据：仅 L0/L1 可触发一次重生成；第二次失败删除不可信 claim 并 partial/refusal；warning、调用数和 cost 正确落库
- [ ] T17.5 Graph Fake 矩阵补齐
  - 判据：Evaluation v1 §5.3 的 12 类路径全部覆盖；不只断言字符串，必须断言节点、工具、预算、错误和持久化终态

## T18 轨迹、成本与回放

- [ ] T18.1 节点/工具轨迹写入
  - 判据：成功、refusal、降级和失败路径都有连续 seq、attempt、latency 和有限摘要；无 chain-of-thought/secret/整篇正文
- [ ] T18.2 多调用 usage/cost 聚合
  - 每次 LLM generation 记录分档 usage/cost；run 汇总值可由明细复算
  - 判据：Fake 定价测试 + 至少一个真实多调用 run 人工复算一致；未知模型不编造 cost
- [ ] T18.3 `runs show/replay`
  - 判据：回放不重新执行任何节点；能看出为什么补检/拒答/重生成；其他 project 的 run_id 返回 NotFound
- [ ] T18.4 轨迹故障一致性
  - 判据：数据库/工具/LLM 在各节点失败时，run 和最后 step 都有终态；无永久 `running` 的正常失败用例

## T19 FastAPI 与共用应用服务

- [ ] T19.1 抽取 CLI/API 共用 service
  - 判据：CLI 和 API 不复制摄取/检索/Agent 逻辑；数据库/SDK/模型异常映射为稳定 DevKbError；普通 CLI 不输出原始 traceback
- [ ] T19.2 `POST /ask`
  - 判据：Pydantic 输入校验、同步 Answer v1、project 隔离、top_k/问题长度上限、错误码测试；无 SSE
- [ ] T19.3 `GET /runs/{id}` 与 `/healthz`
  - 判据：runs 返回步骤和工具；healthz 只查进程/DB/迁移且不加载模型/调用 LLM；不泄漏配置 secret
- [ ] T19.4 同步模型隔离与并发上限
  - embedding 放入线程，semaphore 默认 1；事件循环不被阻塞
  - 判据：并发 API 测试能观察上限；请求失败/断开后 run 最终状态一致
- [ ] T19.5 本地暴露边界
  - 判据：默认 bind 127.0.0.1；README 显著声明 P1 无鉴权不可公网暴露；不加入 JWT/CORS 管理后台/UI

## T20 Evaluation v1 harness 与 CI

- [ ] T20.1 `devkb eval run` 与报告 schema
  - mode 只接受 `vector-exact|vector-hnsw|lexical|hybrid-rrf|agentic|all`；原始 JSON + Markdown；记录 commit/语料 manifest/hash/模型/Prompt/配置
  - 判据：重复 dev 产生新报告不覆盖；holdout 缺 `--confirm-holdout` 必须拒绝
- [ ] T20.2 指标实现
  - Recall@5/10、MRR@10、overlap、拒答/误拒、L0/L1、citation proxy、轮次/调用/token/cost/latency
  - 判据：手算微数据集逐项对齐；空集/多 relevant/partial 有单测
- [ ] T20.3 `make eval-ci`
  - fixture + Fake + 真实 PG 的确定性回归；报告 schema/Gate 校验
  - 判据：不装 ml、不触网；故意破坏 RRF/预算/L1 时测试必红；接入 GitHub Actions
- [ ] T20.4 真实 dev 检索与 Agent Gate
  - 判据：按 Evaluation v1 §5.1/5.2 运行并通过；失败如实留档后在 dev 上迭代，不能接触 holdout

## T21 P1 收尾与验收

- [ ] T21.1 README 与架构材料更新
  - 一页起步增加 API/Agent/Hybrid；当前架构图、LangGraph 状态图、三态策略、已知限制与安全边界准确
  - 判据：从干净环境按 README 可启动；无未实现能力措辞；明确 Agentic ≠ Multi-Agent
- [ ] T21.2 真语料最终实跑与手工演示
  - 判据：从已审计语料视图完成 Markdown+Java+config 摄取/幂等；full/refine/partial/refusal/L1 路径各有可回放 run；U2.2 人工复核通过
- [ ] T21.3 P1 holdout
  - 依赖 U2.3；只运行一次
  - 判据：报告含全部原始结果、P1 同快照相对对照和 P0 历史绝对值对照；满足 `hybrid-rrf Recall@10 >= 同快照 vector-exact Recall@10` 等硬 Gate；不据此调参
- [ ] T21.4 P1 验收总清单
  - 逐条核对《P1实现规格》§15；所有证据链接到测试、报告、run_id 或命令输出
  - 判据：无“计划中/待补”仍被勾选；所有偏差有用户裁决
- [ ] T21.5 当前头提交远端 CI
  - 判据：GitHub Actions 当前 P1 头提交全绿，不以较早提交的绿灯代替
- [ ] T21.6 P1 Gate 关闭
  - 判据：用户确认 P1 验收；之后才允许依据 ADR-009 创建 P2 实现规格/任务清单/Evaluation-v2

## P1 验收总清单

- [ ] 1. 迁移/backfill/GIN/HNSW/轨迹表通过且 P0 数据兼容
- [ ] 2. 已审计语料视图排除指令/生成物/依赖目录；Markdown+Java+config 摄取和幂等通过，Java 引用真实
- [ ] 3. Hybrid/RRF dev 检索 Gate 通过
- [ ] 4. LangGraph 三态/补检/L0/L1/预算路径全覆盖
- [ ] 5. dev 拒答、误拒、引用 Gate 通过
- [ ] 6. run/step/tool/cost 可回放、可复算、失败有终态
- [ ] 7. CLI + 3 个 FastAPI 接口与项目隔离通过
- [ ] 8. 本地 CI、eval-ci、当前头提交远端 CI 全绿
- [ ] 9. P1 holdout 一次运行并通过硬 Gate
- [ ] 10. README、架构图、状态图、评测和演示材料与实现一致

## 偏差记录

| 日期 | 任务项 | 冲突/缺失描述 | 建议 | 用户裁决 |
|---|---|---|---|---|
