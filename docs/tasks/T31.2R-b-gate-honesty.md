+++
task_id = "T31.2R-b"
status = "accepted"
risk = "high"
base_sha = "d5a36b5b287bdcc4402ec50bafbcc85759745cf8"
max_changed_files = 10
allowed_paths = [
  "src/devkb/eval_contract.py",
  "tests/unit/test_eval_contract.py",
  "tests/unit/test_eval_reports_schema.py",
  "tests/integration/test_eval_contract_harness.py",
  "evalsets/v1.5/contract_dev.jsonl",
  "docs/Evaluation-v1.5.md",
  "docs/tasks/T31.2R-b-gate-honesty.md",
  "docs/P1.5任务清单.md",
  "docs/dev-log.md",
  "docs/backlog.md",
]
allow_dependency_changes = false
allow_migration_changes = false
allow_golden_changes = false
size_exception = "【2026-08-06 第二次扩容，用户批准 9→10】实现期触发「需要计划外文件」停止条件并停止上报：schema 升 v2 后，tests/integration/test_eval_contract_harness.py:256 的 `assert report[\"schema_version\"] == \"p1.5-contract-v1\"` 必然失败（它断言的是**新生成报告里的字面值**，不是常量符号）。仓库事实 F8 只记了 test_eval_reports_schema.py:242 锁常量——我当时 grep 的是符号名 CONTRACT_REPORT_SCHEMA_VERSION 且限定在那两个文件内，**从未全仓 grep 字面量 \"p1.5-contract-v1\"**，与 F6c 那轮（只 grep 13 不 grep 其分解 11）是同一种查法缺陷。改动为一行 v1→v2，零逻辑变更、零断言强度变化；schema 升版本是前审裁决 1 判定的**必然连锁**，故这条失败不可回避，拆出去只会让本任务交付一个自己跑不绿的状态（违反《Definition of Done》「目标测试、make ci 和任务要求的 eval 门禁通过」）。核心源文件仍为 1。 || 原文：总文件 9 > 中任务建议上限 8，但**核心源文件只有 1 个**（src/devkb/eval_contract.py），远低于 5 的上限；超出的 1 个名额全部是文档/评测数据（docs/backlog.md 用于关闭 T312-P2-01）。三件事打包成一个任务是**用户 2026-08-05 的裁决**（当时在「一个修复任务打包三件事」与「拆两个任务」之间选择了后者：R-a 行为层、R-b 评测/文本层），本任务即 R-b 的完整内容。工作流《任务粒度》的「存在两个以上独立回滚点」在此确实成立（G2 判据、c11 标签、renderer 各可独立回滚），故按规则请用户在前审时一并确认是否维持打包；若要求再拆，建议切成 R-b1（G2 判据+schema 版本）与 R-b2（c11 标签 + renderer + backlog 关闭）。"
+++

# Task Packet：T31.2R-b dev Gate 判据诚实化——G2 词表收窄并降为报告项、c11 标签条件式、契约 Markdown 末尾空行

## 任务目标

修好 T31.2 首次 dev Gate 暴露的三处**判据/渲染缺陷**后，`zero_absence_assertion_on_known_paths` 不再因"按规格诚实声明覆盖缺口"而必然失败、c11 的机器标签与其冻结散文不再互相矛盾、契约 Markdown 报告落盘即可通过 `git diff --check`——三者都**不改 agent 行为**，只改评测判据与渲染。

## 背景与当前问题

- **当前行为**：
  1. **G2 结构性不可通过**。`NEGATION_PHRASES`（`eval_contract.py:118`）= `_ABSENCE_MARKERS` ∪ `_REPO_LEVEL_NEGATION` 共 23 条。前一张表含 `未覆盖`/`未包含`/`缺失`，而 `prompts.py:78` **逐字指示** generate「用条件式措辞（如"当前证据未覆盖…"）」，四处确定性诚实尾注也逐字含该词。于是"越按规格诚实声明覆盖缺口，越必然触发 G2"。
  2. **c11 数据集自相矛盾**。`expected_mode_p15 = "partial"`，同一行 `p15_expectation` 的冻结散文却写着「若 PROGRESS/plans 未召回则 refusal-with-boundary 亦可接受」。本轮 `PROGRESS.md` `never_retrieved`、系统给了 refusal——**符合散文、不符合机器字段**。
  3. **契约 Markdown 末尾双换行**。`render_contract_markdown`（`eval_contract.py:1209`）是 `"\n".join(lines) + "\n"`，而 `lines` 末元素已是 `""`，于是每份 `.md` 以 `\n\n` 结尾，`git diff --check` 报 `new blank line at EOF`（登记为 `T312-P2-01`）。

- **复现或仓库证据**：
  - G2：`evalsets/reports/p1.5-dev-contract-20260805T174422+0800.json` 的 `known_path_absence_violations` **13 条**、`undecided` 0 条、`gate: false`。逐条打开后**一条真违规都没有**（详见下方 F3/F4 的定量拆解）。
  - c11：同报告 `contract_expectations_met: false` 的 5 题之一。
  - renderer：T31.2 执行期 `make verify-full` 实际被它挡住，已在 `docs/dev-log.md` 与 `docs/backlog.md` 留档。

- **用户影响**：三者都不影响线上问答，只影响**评测结论的可信度**。但 G2 与 c11 会让 T31.2 重跑后仍然假失败，dev Gate 因此永远拿不到可信基线；`T312-P2-01` 还会挡住 T32.4 的远端 exact-head CI。

## 范围

- **允许修改模块**：`devkb.eval_contract`（评测 harness）、其**两个单测文件与一个 integration 测试文件**（`tests/unit/test_eval_contract.py`、`tests/unit/test_eval_reports_schema.py`、`tests/integration/test_eval_contract_harness.py`——第三个由 2026-08-06 第二次扩容加入，见 `size_exception`）、v1.5 dev 评测集、《Evaluation-v1.5》与三份流程文档。
- **允许修改文件**：见 metadata `allowed_paths`（与 `max_changed_files` 同值，**权威值只在 metadata 一处**；其中**核心源文件仅 1 个**）。
- **最大核心文件数**：1。
- **预计 diff**：源码手写净增约 20 行以内（词表来源换成一张表、`SECTION_GATE_KEYS` 删一键、schema 版本常量、renderer 去掉 `+ "\n"`）；测试与文档为主。

## 非目标

- **不实现**：任何 agent 行为改动（`src/devkb/agent/` 全目录零改动）；不改 `prompts.py` 的措辞指令；不新增/改写 not_found 的两张否定词表本身（`not_found.py` 零改动——本任务只改 `eval_contract` 引用**哪一张**）。
- **不重构**：`score_known_path_absence` 的跨句指代/未判定桶逻辑逐字不动；五分节结构、`SECTION_KEYS`、`all_hard_gates_passed` 的求值方式不动。
- **不修改**：`evalsets/v1.5/contract_holdout.jsonl`（**全程不打开**）；`evalsets/reports/` 下任何已提交报告（见「异常与恢复流」为什么不必重渲染）；c02 的标签（**用户 2026-08-05 已裁决 c02 判真失败**，不得借本任务放宽）；`evalsets/v0/` 的 P1 holdout。
- **不重跑**：真实 dev（T31.2 重跑在本任务验收后单独进行）。

## 仓库事实调查

> 每条都在本轮实际执行了命令/读了代码，不凭 T31.1 的记忆。

- **F1 词表构成**：`eval_contract.py:118` `NEGATION_PHRASES = (*_ABSENCE_MARKERS, *_REPO_LEVEL_NEGATION)`，两表分别在 `not_found.py:173-185`（11 条）与 `:150-163`（12 条），合计 23 条。`_SOFT_NEGATION` 刻意未并入（`:116-117` 注释）。
- **F2 违规词频（从已提交报告实测）**：13 条的命中短语是 `未覆盖`×10、`缺失`×1、`不存在`×1、`未包含`×1。**前三种里的 `未覆盖`/`缺失`/`未包含` 全部属于 `_ABSENCE_MARKERS`**。
- **F3 收窄后的定量结果（实测，非估算）**：只保留 `_REPO_LEVEL_NEGATION` 后，13 条 → **恰好 1 条**。
- **F4 残余那 1 条是语义误配**：`field=claims`、`path=BusinessException.java`、`phrase=不存在`，原文为「DocumentService.ensureKbOwner 调用 findByIdAndOwner 并在知识库不存在时抛 BusinessException。」——说的是**知识库**这个运行期对象不存在，不是说该 `.java` 文件不存在。**共现式扫描在结构上判不了这个区别**，这正是「收窄」必须搭配「降为报告项」的原因（用户 2026-08-05 裁决 "B+C"）。
- **F5 冲突的另一端是规格自己要求的**：`prompts.py:78` 指示「not_found 只描述**当前证据范围内**的缺口，用条件式措辞（如"当前证据未覆盖…"）」；确定性尾注四处逐字含 `未覆盖`——`security.py:55` `_LAYER_SUFFIX`、`config_layers.py:92` `_TAIL`、`nodes.py:560`、`answer.py:63`。**已逐个打开核对，四处均属实。**
- **F6 Gate 键派生**：`SECTION_GATE_KEYS`（`:87-107`）是**唯一**映射，`GATE_KEYS`（`:110`）由它按 `SECTION_KEYS` 展平派生，共 13 键；`zero_absence_assertion_on_known_paths` 在 `final_consistency` 一节。
- **F6b（前审 PG-T312Rb-01 指出，我复核后确认，且比原判更糟）`gate_summary` 现在根本不是投影**：`:1122` 写的是 `"gate_summary": gates`——**直接透传** `:925` 那个手写字面 dict。而 `:81` 的注释白纸黑字声称「section 是权威结果，`gate_summary` 只是它的投影」。今天两者键集合相等纯属**手写时恰好对齐**，没有任何机制保证。后果：只删 `SECTION_GATE_KEYS` 里的键，`sections[...]["gate"]`（`:1114` 按 `SECTION_GATE_KEYS[name]` 取）会正确变成 6 键合取，但 `gate_summary` **仍然是 13 键**，报告自相矛盾。所以本任务必须**同时**删 `gates` 字面 dict 里那一项、并把 `gate_summary` 改成真投影，让那句注释从"说法"变成"机器事实"。

  **副产物（按第三轮收紧的判据重扫时发现）**：断言"投影"的注释**一共三处**——`:81`、`:83`（「现在两者由同一张映射派生」）、`:1112`（「`gate_summary` 只是投影」）。**这三句今天全都不成立**，是随 T311-CR-01 修复一起写下的意图声明而非事实。修④之后三句同时变真，并由 U7 的游离键负例机器背书。**故这三处不需要改文字，也不列入 F6c**——它们不是失实的**键数**，而是被代码补齐的**设计声明**。（`:1267` 的「七组评分」不变：`score_known_path_absence` 仍照常计算，只是不再进 `gate_summary`。）
- **F6c 硬编码的键数与"违规"措辞共 8 处，不是 2 处**（第一轮我只找到 2 处 → 前审第二轮指出漏 3 处 → 我按新查法重扫又自查出 2 处 → 前审第三轮再指出 1 处 `:108`，复核**属实**）：
  1. `:1146` 字面量 `"## Gate 汇总（13 键）"`；
  2. `:1148-1150` 的**来源说明**——「§5.1 九条 →（第 1 条拆 contract/extended、第 4 条拆 4a/4b）共 **11** 键；`l0_l1_all_pass` 来自 §7 硬 Gate + §14；`fail_fast_isolation_ok` 来自 §5.2 第 8 条」。11+1+1=13。`zero_absence_assertion_on_known_paths` 正是 §5.1 **第 2 条**那一键，降级后该数应为 **10**，合计 12。**只改标题不改这段，报告会一边写 12 键一边把来源加成 13**；
  3. `:1196` 的 `违规 N`（Markdown 面）；
  4. `:1067-1071` 的 **JSON `final_consistency.note`**——现文为「not_found 只统计可判定项…已知路径否定扫描只覆盖同语段可判定子集，跨句指代进未判定，不进分子分母」，**不含**「规则命中计数，非违规判定」。U10 要求 Markdown 与 JSON note **两个面**都带该声明，而第一轮实现计划只写了 `:1196`——**是我的计划与我自己的冻结测试打架**；
  5. `:84` 的注释「`fail_fast_isolation_ok` 作为**第 13 键**被显式收进 safety_reliability」——降级后它是第 12 键，该注释变成**失实注释**。（**同段其余各行不动，但理由分两种，见下方分类：**`:81` 与 `:83` 后半是设计声明、`:82`–`:83` 前半才是史料。上一轮我把 `:81-83` 笼统写成"史料"，前审第四轮指出，属实。）
  6. **`:981`——第二处「第 13 键」**：`gates` 字面 dict 里 `fail_fast_isolation_ok` 上方的注释同样写着「第 13 键，来自 §5.2 第 8 条」。**本条由我按新查法自查发现，不在前审第二轮列出的三点内**；漏掉它会留下与 `:84` 完全同型的失实注释。
  7. **`:907`——`aggregate_contract` 的 docstring**：「五节各自成表 + §5.1 九条落到 **12 个**具名硬键」。**同样是我自查发现的**。这句**今天就已经不准**（§5.1 九条实际落到 11 键，加 `l0_l1_all_pass` 才 12，再加 `fail_fast_isolation_ok` 才 13）；降级后总数虽然巧合地变成 12，但「§5.1 九条落到 12 个」这个**归属**仍然错。须改成与 `:1148-1150` 同口径的表述。

  8. **`:108-109`——`GATE_KEYS` 上方的推导注释**：「§5.1 九条 → 具名硬键（第 1 条拆 contract/extended、第 4 条拆 4a/4b），另加 §7/§14 的 L0/L1 与 §5.2 第 8 条的 fail-fast」。**前审第三轮指出，我复核属实——我上一轮把它误归进"不该动"了**。误判的原因很具体：我按「提到 §5.1 九条」这个**表面特征**归类，而 `:108` 与 `:90`/`:101`/`:1150` 的区别不在提没提九条，在于**它断言的是一条推导链**（九条 → 键），另外三条断言的是**成员关系或规格自身的条数**。第 2 条降级后推导链断了，这句就失实。须改成类似「§5.1 第 2 条降为报告项，其余条款展开为 **10** 个具名硬键，另加两个非 §5.1 键，共 12 键」。

  > **F6c 之外一律不动，按理由分五类**（前审第四轮要求"理由统一写准"——上一轮我把 `:81-83` 整段笼统称作"史料"，其中只有 `:82`–`:83` 前半是）：
  >
  > | 类 | 位置 | 为什么不改 |
  > |---|---|---|
  > | A **设计声明，由修④补实** | `:81`（「section 是权威结果，`gate_summary` 只是它的投影」）、`:83` 后半（「现在两者由同一张映射派生」）、`:1112`（同一句的第二处） | **今天全都不成立**（`:1122` 是原样透传，见 F6b）。它们不是失实的**键数**，而是代码尚未兑现的**设计声明**；修④之后三句同时变真，并由 U7 游离键负例机器背书。**改代码即可，文字本就写对了** |
  > | B **历史成因，史料** | `:82`–`:83` 前半（「首版把总判定定成 12 个键的合取……安全分节 False 时总判定仍可为 True（代码审查 T311-CR-01）」） | 描述的是**首版缺陷**，过去时陈述，与当前键数无关 |
  > | C **成员关系/规格事实** | `:90`（L0/L1 **不属** §5.1 九条）、`:101`（fail-fast 来自 §5.2 第 8 条**而非** §5.1 九条）、`:1150`（§5.1 **没有**十条）、`:614`（B5 的十条结构规则） | 断言的是成员关系否定或规格自身条数，**不随键集合变化**（第三轮收紧的判据） |
  > | D **扫描器设计说明** | `:119`、`:428`、`:438` 的 `违规` | 解释的是为何跨句指代只判 undecided、为何不拿 refs 当信号，**不是报告输出面**，语义至今准确 |
  > | E **仍成立的机器锁说明** | `:85-86`（U14 用 Counter 锁重复归属）、`:1267`（七组评分） | 与键数无关；`score_known_path_absence` 仍照常计算，只是不再进 `gate_summary`，故"七组"不变 |
- **F7（最关键的连锁）降级**必然**触发 schema 版本变更**：`tests/unit/test_eval_reports_schema.py:137` 的 `_assert_p15_contract_shape` 断言 `set(aggregates["gate_summary"]) == set(CONTRACT_GATE_KEYS)`，且分派条件是 `schema.startswith("p1.5-contract-v")`——**对所有 v* 版本一律生效**。已提交的 dev 报告是 13 键且**永不重写**，所以键集合一旦变成 12，该断言必须按版本分派，`CONTRACT_REPORT_SCHEMA_VERSION`（`:61`，现为 `"p1.5-contract-v1"`）必须升到 `v2`。
- **F8 版本常量被字面锁**：`test_eval_reports_schema.py:242` 断言 `CONTRACT_REPORT_SCHEMA_VERSION == "p1.5-contract-v1"`。
- **F9 条件式标签的能力早就有，不需要写新代码**：`parse_expected_modes`（`:222-230`）已支持 `X_or_Y` 语法（`partial_or_full` → `("partial","full")`），越出 `_MODE_ATOMS` 即 `InvalidInputError`。dev 13 题现有取值为 `partial`×10 / `refusal`×2 / `policy_refusal`×1，**`refusal` 已是合法原子**。→ c11 是**纯数据改动**。
- **F10 c11 现状**：`expected_mode_p15: "partial"`；`p15_expectation` 末句「若 PROGRESS/plans 未召回则 refusal-with-boundary 亦可接受」；`notes` 末尾标「Fable §2 已冻结」。
- **F10b（回应前审 PG-T312Rb-03）条件性由**第二个合取项**承担，不需要新代码**：`contract_expectations_met` 的逐题谓词是 `bool(row["mode_matches"]) and not row["evidence_selection"]["retrieved_not_cited"]`（`:927-930`）。把 c11 标成 `partial_or_refusal` 后，三种情形分别落在——①`PROGRESS.md` **未召回** + `refusal`：`mode_matches` 真、`retrieved_not_cited` 空 → **达标**；②`PROGRESS.md` **已召回却未被直接引用** + `refusal`：`mode_matches` 虽真，但 `retrieved_not_cited` 非空 → **仍判失败**；③`full`：`mode_matches` 假 → **仍判失败**。所以「无条件枚举」这个担心在**本 harness 的实际求值下不成立**，但它**完全不体现在数据行里**，只靠第二个合取项默默兜住——前审要求配对冻结测试是对的：不锁死，将来任何人放宽 `retrieved_not_cited` 都会把 c11 悄悄变成真的无条件枚举。故 U8 拆成 U8a/U8b/U8c 三条配对用例。
- **F11 冻结文档措辞**：《Evaluation-v1.5》§5.1 第 2 条 =「**零"把 active/indexed 或全轮历史出现过的文件断言为不存在"**」；**该条同时出现在 §「Holdout 硬 Gate」清单**（第 130 行「§5.1 第 2、3、4、5、7 条……在 holdout 上同样成立」）——降级必须同步改这两处，只改一处会让 dev 与 holdout 口径分裂。
- **F12 renderer 对照**：v1 的 `evaluation.py:915` 是纯 `"\n".join(lines)`（其 `lines` 同样以 `""` 结尾），所以三份已提交 v1 报告一直干净。修法就是与 v1 对齐。
- **F13 已提交报告不必重渲染**：`workflow_guard.py:639` 执行的是 `git diff --check <base_sha>`，只看**本任务 diff**。那份 `.md` 已在 `base_sha` 之前提交，不进本任务 diff，不会再触发；T31.2 重跑会用修好的 renderer 生成新报告。
- **F14 13 这个数字被锁在四处**：`test_eval_contract.py:236`、`:803-822`（显式字面量列表，注释写明"不拿 GATE_KEYS 自证"）、`:932-947`（第二份字面量 + `len == 13`）、以及 `:825` 的 `@pytest.mark.parametrize("key", sorted(GATE_KEYS))`。
- **F15 `evalsets/v1.5/` 不在 `GOLDEN_PREFIXES` 内**：`workflow_guard.py:67-72` 只含 `evalsets/reports/`、`tests/fixtures/golden/`、`tests/golden/`、`tests/snapshots/`。所以改 `contract_dev.jsonl` **不需要** `allow_golden_changes`，本 packet 据此填 `false`（如实反映脚本判定）。**但这本身像是门禁漏检**——评测集标签与 golden 同等敏感，却无人拦。登记为 P3 候选，不在本任务修。
- **当前测试**：`tests/unit/test_eval_contract.py`（G2 相关见 `:457`、`:687`、`:726`）、`tests/unit/test_eval_reports_schema.py`；两者都在 `make eval-ci` 内。
- **尚未证实的假设**：
  - **收窄只在本轮已落盘的这批文本上验证过**：F3 的"13 → 1"是对**同一批文本**用新词表重算，`推不出`①未来运行的文本会不会命中新词表、②收窄掉的 11 条 `_ABSENCE_MARKERS` 里会不会**漏判**真违规（真违规换个说法就能绕过共现扫描，这也正是它只能当报告项的原因，见「判据可证边界」）。故验收断言只写"对本轮已落盘文本重算 = 1 条"这一**可复现事实**，不写任何覆盖率或发生率承诺。
    > **本行第六轮修订**：原文写「未验证收窄后**其余 12 题**是否会新增违规」——`12` 是从"13 条违规"顺手减出来的，而本轮实际是 20 题（13 契约 + 7 反例），`条`与`题`也不是一回事，**这个数字没有任何来源**。已删除该数字而非改对它：同一事实不写第二份手写副本，正是本任务在 `eval_contract.py` 侧要立的规矩。
  - 未验证 §5.1 第 2 条降级后 holdout Gate 清单是否还有其他文档引用；实现时须全文检索《Evaluation-v1.5》再改。

## 关键不变量

- **业务**：G2 的**计算与报告**不消失——`known_path_absence_violations` / `known_path_absence_undecided` 仍逐条落进 `final_consistency`；改变的只是"它是否参与 `all_hard_gates_passed`"。
- **状态**：`SECTION_KEYS` 五分节不变；`SECTION_GATE_KEYS` 仍是唯一映射、`GATE_KEYS` 仍由它派生（不得改成两处各写一份）。
- **权限/项目隔离**：不适用——本任务不触碰任何查询构造，`repositories.py` 零改动（D7 不受影响）。
- **幂等/重试**：不适用——评测 harness 是纯函数式打分，无重试语义。
- **事务/部分成功**：不适用——不写库。
- **LLM/Tool/RAG**：**零 LLM 调用点变化**，`PROMPT_VERSION` 不变，D6 的 4 调用点/6 请求上限不受影响；`src/devkb/agent/` 全目录零改动。
- **失败行为**：三态 fail-closed 不变——`None` 仍然不通过；降级后的 G2 **不得**把 `None` 洗成 `True` 混进别的键。
- **向后兼容**：已提交的 `p1.5-contract-v1` 报告必须**继续通过** `make eval-ci`（这正是 F7 要求版本分派的原因）；P0/P1 报告的 schema 断言逐字不动。
- **判据可证边界**：
  - 收窄后的 G2 **能证明**：交付文本里没有出现"仓库级不存在"词表 ∩ 已知路径的同段共现。**推不出**"系统从未把已知文件说成不存在"——F4 那条反过来说明共现扫描既会误报、也可能漏报（换个说法就绕过了）。故降级后其结论在报告里只能表述为**观察计数**，不得表述为"零违规"或任何合规结论。
  - 「13 → 1」**能证明**：对**本轮已落盘的这批文本**重算的确定性结果。**推不出**任何关于未来运行的发生率或"收窄不会漏判"。
  - c11 改条件式 **能证明**：该题的机器标签与其冻结散文不再互斥。**推不出**"系统在 c11 上表现合格"——`partial_or_refusal` 是**放宽**，必须在报告与清单里显著标记，不得被读成能力证据。

## 实现计划

| 顺序 | 文件 | 必要性 | 最小修改 | 对应测试 |
|---:|---|---|---|---|
| 1 | `src/devkb/eval_contract.py` | 唯一 harness 源文件 | ①`NEGATION_PHRASES` 改为只取 `_REPO_LEVEL_NEGATION`（保留并改写注释，写明为何弃用 `_ABSENCE_MARKERS`）；②`SECTION_GATE_KEYS["final_consistency"]` 删 `zero_absence_assertion_on_known_paths`；③**`:925` 的 `gates` 字面 dict 同步删掉该项**，其计算结果改落 `final_consistency` 诊断字段（F6b）；④**`:1122` 改为真投影** `{key: gates[key] for key in GATE_KEYS}`，让 `:81` 那句注释变成机器事实；⑤`CONTRACT_REPORT_SCHEMA_VERSION` → `"p1.5-contract-v2"`；⑥`render_contract_markdown` 末行改 `return "\n".join(lines)` | U1–U7 |
| 2 | `src/devkb/eval_contract.py`（同文件，键数/措辞面共 **8** 处，见 F6c） | 前审 PG-T312Rb-02（**三轮**：第二轮指 3 处、第三轮指 `:108`）+ 自查 2 处 | ⑦`:1146` 改为**派生** `f"## Gate 汇总（{len(GATE_KEYS)} 键）"`；⑧`:1148-1150` 来源说明的「共 11 键」改为 **10**，并补一句「§5.1 第 2 条已降为报告项，见 `final_consistency`」——让"少的那一键去哪了"在报告里可追；⑨`:1196` 改计数口径「已知路径 × 仓库级否定词表共现：命中 N 段、跨句指代未判定 M 段（**规则命中计数，非违规判定**）」；⑩`:1067-1071` 的 JSON `final_consistency.note` 追加同一句声明（U10b 要求两个面都有）；⑪`:84` 与 **⑫`:981`** 两处「第 13 键」→「第 12 键」；⑬`:907` docstring 的「§5.1 九条落到 12 个具名硬键」改为与 `:1148-1150` 同口径（**该句今天就已不准**，不是本任务改坏的）；**⑭`:108-109` 的推导注释**改为「§5.1 第 2 条降为报告项，其余条款展开为 10 个具名硬键，另加两个非 §5.1 键，共 12 键」。**F6c 这 8 处之外，本文件的注释与 docstring 一律逐字不动**（A–E 五类理由见 F6c 表）| U10a/U10b |
| 3 | `tests/unit/test_eval_contract.py` | 13 被锁在四处 | 两份字面量列表与 `len` 断言 13→12；G2 用例改为断言"仍在 section 内逐条可见、但不在 `gate_summary`"；新增词表收窄正/负例；新增**投影负例**（往 `gates` 塞游离键不得泄漏进 `gate_summary`） | U1–U5、U7、U10 |
| 4 | `tests/unit/test_eval_reports_schema.py` | F7 的连锁 | `_assert_p15_contract_shape` 按 `schema_version` 分派（v1=13 键、v2=12 键）；`:242` 的字面量断言更新为 v2 并保留 v1 仍受支持；`:120` 的陈旧注释「12 具名硬键」一并改对（现为 13，改后 12——它此前就与同函数 docstring 的「13 键齐备」自相矛盾） | U6 |
| 5 | `evalsets/v1.5/contract_dev.jsonl` | c11 标签 | **仅 c11 一行**：`expected_mode_p15` `"partial"` → `"partial_or_refusal"`，并在 `notes` 追加显著标记（放宽 + 裁决日期 + 条件由 `retrieved_not_cited` 承担 + 不得读作能力证据） | U8a/U8b/U8c |
| 6 | `docs/Evaluation-v1.5.md` | 冻结文档同步 | §5.1 第 2 条由硬 Gate 改为**报告项**并写明理由；**Holdout 硬 Gate 清单（第 130 行）同步删除第 2 条**（前审裁决 2：dev 与 holdout 同步降级，不留 split 间语义分裂）；§2 c11 两栏预期标注条件式 | — |
| 7 | `docs/backlog.md` | 关闭 **P2** | `T312-P2-01`（登记为 **P2（阻塞门禁）**，前审 PG-T312Rb-04 更正我原写的"P3"）标记为本任务已修复 | — |
| 8 | `docs/P1.5任务清单.md`、`docs/dev-log.md`、本 packet | 记录纪律 | T31.2 条目下追加 R-b 子项行；dev-log 叙事；**T31.2 仍不勾选** | — |

**第 1–8 行已在 `9c8f78d` 落地，本次 RETURN_TO_DESIGN 修订不改动其中任何一行**（首审对 `src/devkb/eval_contract.py`、`evalsets/v1.5/contract_dev.jsonl`、《Evaluation-v1.5》、`docs/backlog.md` **零 finding**）。下列第 9–11 行是本次修订新增的，**全部只改测试与本 packet，生产代码零改动**：

| 顺序 | 文件 | 必要性 | 最小修改 | 对应测试 |
|---:|---|---|---|---|
| 9 | `tests/unit/test_eval_contract.py` | 关闭 CR-01 的 U1/U3/U7/U11 | ⓐ**删除合成夹具** `_T312RB_LANDED_VIOLATIONS`，U3 改为读已落盘报告的 13 条真实 `segment`/`path` 逐条重算；ⓑU7 增游离源键负例（monkeypatch 缩短 `GATE_KEYS`），原"键集合相等"降为同一用例里的第一条断言；ⓒU11 的绕过文本装进 scored row 走 `aggregate_contract`，断言**其原文逐字出现在 `questions[*].answer.answer_text`**、violations 为空、分母 ≥1（补充）、无合规结论；ⓓU1 断言明细逐字落进聚合报告的 `final_consistency` | U1、U3、U7、U11 |
| 10 | `tests/unit/test_eval_reports_schema.py` | 关闭 CR-01 的 U6 | 现场 `aggregate_contract(...)` 产出 v2 aggregates → 裹进最小信封 → **实际传入** `_assert_p15_contract_shape`；再加"谎标成 v1 必须失败"的分派负例。`tests/` 无 `__init__.py`，跨测试文件 import 不可靠，故 v2 信封在本文件内构造，**不新建共享文件**（那属范围扩张）| U6 |
| 11 | 本 packet | 关闭 CR-02/CR-03 | 范围节同步为 metadata 权威（不再手写份数）并列出 integration test；冻结测试矩阵 5 行改写为"可执行的约定输入"；填 Review Ledger；最终验收改为绑定重跑后的 exact HEAD | — |

### 数据流

`answer` 文本 → `assertion_surface` → `split_assertion_segments` → 每段 `(_known_tokens_in, _phrases_in)` →（**唯一变化点**：`_phrases_in` 查的表从 23 条变 12 条）→ violations/undecided/decidable → `final_consistency` section →（**第二个变化点**：不再投影进 `gate_summary`）。

### 状态流

无状态机变化。`all_hard_gates_passed = all(section.gate is True)`，而 `final_consistency.gate` 现由 6 键而非 7 键合取——`_conjunction` 的三态语义逐字不动。

### 异常与恢复流

- 已提交的 `p1.5-contract-v1` 报告**不重渲染、不重写**（F13：它不在本任务 diff 内，不会再触发 `git diff --check`；重写已落盘报告违反《Evaluation-v1.5》「不覆盖旧结果」）。
- 若实现中发现收窄后**本轮重算不是 1 条**（与 F3 不符）：停止，按「风险和停止条件」上报，不得调词表去凑数字。

### 风险和停止条件

- **如果需要计划外文件**：停止并上报。特别是——若发现《P1.5实现规格》或其他冻结文档也把 §5.1 第 2 条写成硬 Gate，**不得自行扩大改动范围**，先上报。
- **如果规格与代码事实冲突**：停止并写入《P1.5任务清单》偏差记录等裁决（G2 与 c11 本身就是这样上来的）。
- **如果超过文件预算**：停止上报，不得靠合并提交掩盖。
- **如果核心不变量无法证明**：特别是"已提交 v1 报告继续通过 eval-ci"——若版本分派方案做不到，停止上报，不得改写已落盘报告来迁就。
- **如果发现收窄会漏判真违规**：停止上报——降级的前提是"它仍被计算与报告"，若连计算都不可信，须重新设计而非继续。

## 冻结测试

| 类别 | 输入/前置状态 | 预期结果 | 测试位置 |
|---|---|---|---|
| 正常 | 交付文本含 `不存在` + 已知路径同段，**该文本必须装进一条 scored row 并经 `aggregate_contract` 聚合** | 仍记为 violation，且该条**逐字出现在聚合报告的 `final_consistency.known_path_absence_violations` 里**——只断言 `score_known_path_absence` 的返回值不算达成本行（`T312Rb-CR-01`ⓔ，**本轮自查补入、首审未列**）| U1 |
| 正常 | 报告渲染后落盘 | `.md` 恰好以**单个** `\n` 结尾（`text.endswith("\n") and not text.endswith("\n\n")`） | U5 |
| 边界 | 交付文本含 `未覆盖`/`未包含`/`缺失` + 已知路径同段（= 本轮 13 条里的 12 条形态） | **不再**记为 violation | U2 |
| 边界 | **读取** `evalsets/reports/p1.5-dev-contract-20260805T174422+0800.json`，取 `aggregates.sections.final_consistency.known_path_absence_violations` 的**每一条真实 `segment` 与其 `path`**逐条重算（**禁止用合成句或统一替换成 `README.md` 的近似句代替**——首审 T312Rb-CR-01）| ①该列表长度为 **13**（锁"这份已落盘报告没被换过"）；②重算后命中恰为 **1** 条，`phrase == "不存在"` 且 `path` 以 `BusinessException.java` 结尾 | U3 |
| 失败（c11 配对①）| c11 标 `partial_or_refusal`；`PROGRESS.md` **未召回** + 实际 `refusal` | **达标**（`mode_matches` 真 ∧ `retrieved_not_cited` 空） | U8a |
| 失败（c11 配对②）| c11 标 `partial_or_refusal`；`PROGRESS.md` **已召回但未直接引用** + 实际 `refusal` | **仍判失败**——条件性由 `retrieved_not_cited` 承担（F10b）。**这条是三者中最关键的**：它证明放宽的是 mode 枚举、不是"召回了也可以不引用" | U8b |
| 失败（c11 配对③）| c11 标 `partial_or_refusal`；实际 `full` | **仍判失败**（`full` ∉ 原子集） | U8c |
| 重复/重试 | 不适用——harness 为纯函数打分，无重试语义 | — | — |
| 越权/非法状态 | `expected_mode_p15` 写成非法原子（如 `"partial_or_bogus"`） | 仍抛 `InvalidInputError`，不静默当未判定 | U9 |
| 组合/变形不变量 | `gate_summary` 键集合 | **12** 键；`zero_absence_assertion_on_known_paths` ∉ `gate_summary`，但其明细仍 ∈ `final_consistency`；`final_consistency.gate` 由 **6** 键合取 | U4 |
| 组合/变形不变量（投影负例）| 造出一个"在手写 `gates` 里、不在 `GATE_KEYS` 里"的**游离源键**。`gates`（实现计划里按实现前行号写作 `:925`，`9c8f78d` 后为 `:942`）是函数内字面 dict、外部塞不进去，故**等价且可执行的构造是 monkeypatch 缩短 `GATE_KEYS`**，让 `gates` 里被摘掉的那一项成为游离源键 | ①`gate_summary` 恰等于缩短后的键集合；②被摘掉的键**不出现在** `gate_summary`。透传实现下它必然出现，故本行**实现前必红**——只断言"键集合相等"不算达成本行（首审 T312Rb-CR-01：那条在透传实现下本来就是绿的）| U7 |
| 组合/变形不变量 | 渲染后的 Markdown **标题与来源说明**| ①标题含「Gate 汇总（12 键）」且数字**由 `len(GATE_KEYS)` 派生**；②来源说明里的 §5.1 键数断言为 `f"共 {len(GATE_KEYS) - 2} 键"`——**恰好 2 键非 §5.1（`l0_l1_all_pass` + `fail_fast_isolation_ok`），这是来源说明自己陈述的事实**，故该表达式是对它的机器锁：将来任何人再降/再加一个 §5.1 键而忘了改这段散文，本断言立刻转红。**不为此新建 provenance 映射**（属范围扩张）| U10a |
| 组合/变形不变量 | 已提交的 `p1.5-contract-v1` 报告（`_load_reports()` 遍历）**加上当前 harness 现场产出的 v2 aggregates**（`aggregate_contract(...)` 的真实返回值裹进最小报告信封，**必须实际传进 `_assert_p15_contract_shape`**——首审 T312Rb-CR-01：原实现只让已提交的 v1 走了该 helper）| ①两者**各按自己的版本**通过 shape 断言（v1=13 键、v2=12 键）；②**分派负例**：把同一份 v2 aggregates 谎标成 `p1.5-contract-v1` 时该 helper 必须失败——否则"按版本分派"可能只是两个分支恰好都放行 | U6 |
| 诚实边界 | 降级后的报告文本，**参数化跑两个面**：Markdown 渲染结果 与 JSON `final_consistency.note` | 两个面**各自**：①须**逐字**出现「规则命中计数，非违规判定」；②**在承载 G2 的那一行/那一句范围内**（不是全报告——全报告必然含"总判定/通过"等合法字样），禁用措辞 `零违规`、`无违规`、`合规`、`不合规`、`通过`、`未通过` 零命中。**裸词 `违规` 刻意不入禁用表**：要求出现的那句自身含「非违规判定」，把它列进去会让断言自相矛盾——禁的是"下合规/违规结论"，不是这两个字。**两条都实现前必红**：`违规 N` 现就在 `:1196`（Markdown 面），JSON note（`:1067-1071`）今天连该声明都没有 | U10b（参数化 2 例）|
| 判据 fail-closed 负例 | ①收窄后的 G2 被断言成"系统从未把已知文件说成不存在"。构造"换个说法绕过词表"的文本（已知路径 + 表外否定措辞），**该文本必须装进 scored row 并经 `aggregate_contract` 聚合**（首审 T312Rb-CR-01：原实现的报告由无关的 `_green_rows()` 生成，绕过词表的文本从未进入报告）| 该命题**推不出**：①**该文本逐字出现在报告里**——`aggregates.questions[*].answer.answer_text`（`assemble_row` 保留 `"answer": answer`、`aggregate_contract` 保留 `"questions": rows`，已落盘报告 20 题逐条可查）；②`known_path_absence_violations` 为空；③**补充**：`known_path_absence_decidable_segments` 把该语段计进了分母（≥1），"扫过 0 段"与"扫过 N 段命中 0"可区分——**分母是补充证据，不替代①**；④仍**不产生任何合规结论**（G2 ∉ `gate_summary`，两个面仍只给「规则命中计数，非违规判定」）。**第十轮更正**：第九轮我曾断言原文"不可能出现在报告里"并据此把①换成分母，**该断言是错的**（`:1405`/`:1137` 两处保留原文），原冻结用语一直可执行，已恢复为主判据 | U11 |
| 判据 fail-closed 负例 | ②c11 的 `partial_or_refusal` 被读成"c11 合格" | 该命题**推不出**：断言放宽标记在报告/清单中存在，且 `p16_target` 仍要求三态闭合（不因本次放宽而降低 P1.6 目标） | U12 |

## 验收标准

- [x] `NEGATION_PHRASES` 只含 `_REPO_LEVEL_NEGATION`；`not_found.py` 两张表本身零改动 —— `eval_contract.py:133` 逐字为 `_REPO_LEVEL_NEGATION`；`git diff d5a36b5 -- src/devkb/agent/not_found.py` 空
- [x] 对本轮已落盘的 13 条原文重算 = **1** 条，且是 `不存在`/`BusinessException.java` 那条——**由测试实际读取那份报告文件执行**，不得用合成句代替 —— U3 实读 `_T312RB_LANDED_REPORT`（`test_eval_contract.py:1898`），13 条真实 `segment`/`path` 逐条重算；变形 M2 转红
- [x] `gate_summary` 为 **12** 键，且**由 `GATE_KEYS` 真投影得到**（不是手写 dict 恰好对齐）；**游离源键**不泄漏（U7，由缩短 `GATE_KEYS` 构造，实现前必红） —— `eval_contract.py:1143` 为 `{key: gates[key] for key in GATE_KEYS}`；U7 游离源键负例 + U7b 无源键 `KeyError`；变形 M1 转红
- [x] `final_consistency.gate` 由 **6** 键合取；G2 明细仍在该 section 内逐条可见 —— U4 断言 `len(SECTION_GATE_KEYS["final_consistency"]) == 6`；明细进 section 由 U1 断言，变形 M5 转红
- [x] **F6c 八处全部改完，无一漏改**：`:1146` 标题派生自 `len(GATE_KEYS)`；`:1148-1150` 来源说明 §5.1 键数为 10 并注明第 2 条已降为报告项；`:1196`（Markdown）与 `:1067-1071`（JSON note）两个面都带「规则命中计数，非违规判定」；`:84` 与 `:981` 两处「第 13 键」→「第 12 键」；`:907` docstring 归属口径改对；`:108-109` 的推导注释改为「第 2 条降为报告项 + 其余展开 10 键 + 2 非 §5.1 键 = 12」 —— `:85`/`:995` 两处「第 12 键」、`:1087` JSON note、`:1167` 标题派生自 `len(GATE_KEYS)`、`:1171` `len(GATE_KEYS) - 2`、`:1221` Markdown 计数口径，另 `:108-109` 推导注释与 `:907` docstring 见首次实现自审
- [x] **F6c 八处之外，`eval_contract.py` 的注释与 docstring 零改动**（比数个数更可查：`git diff` 里该文件的注释行改动应恰好落在这 8 处内）。特别确认 A 类三处 `:81`/`:83` 后半/`:1112` **文字未动**——它们由修④在代码侧变真，不靠改措辞 —— A 类三处 `git diff d5a36b5 | grep -E "^-.*(投影|同一张映射派生)"` 零命中；B/C/D/E 十项逐条零删除行
- [x] 渲染后的报告**不存在"标题 12 键、来源加总 13 键"的自相矛盾**；U10a 的 `len(GATE_KEYS) - 2` 机器锁生效 —— U10a 全绿：标题与来源说明的键数均由 `len(GATE_KEYS)` 派生，散文再漂移即转红
- [x] `CONTRACT_REPORT_SCHEMA_VERSION == "p1.5-contract-v2"`，且**已提交的 v1 报告继续通过** `make eval-ci` —— `test_schema_version_is_frozen` + `test_current_harness_emits_exactly_the_locked_v1_2_shapes`；已提交 v1 报告在 `make eval-ci` 584 + 65 passed 内继续全绿
- [x] c11 `expected_mode_p15 == "partial_or_refusal"`；U8a/U8b/U8c 三条配对用例全绿——尤其 **U8b（已召回未引用仍判失败）**；**c02 保持 `partial`（判真失败）** —— 实读 `contract_dev.jsonl`：c11 = `partial_or_refusal`、c02 = `partial`；U8a/U8b/U8c 与 U12 全绿
- [x] 契约 `.md` 以单个 `\n` 结尾；`T312-P2-01`（**P2**）在 backlog 标记关闭 —— U5 双向断言（`endswith("\n")` ∧ `not endswith("\n\n")`）；`docs/backlog.md` 的 `T312-P2-01` 已标 ✅ 关闭
- [x] 《Evaluation-v1.5》§5.1 第 2 条**与** Holdout 硬 Gate 清单（第 130 行）两处同步降级，无一处漏改 —— §5.1 第 2 条改为删除线 + 「降为报告项」；Holdout 硬 Gate 清单改为「第 3、4、5、7 条」并注明第 2 条已随 dev 侧一并降级——两处均在 diff 内
- [x] `src/devkb/agent/` 全目录零改动；`PROMPT_VERSION` 不变 —— `git diff d5a36b5 -- src/devkb/agent/` 空；`PROMPT_VERSION` 在整个 `src/` diff 中零出现
- [x] 两条「推不出」各有 fail-closed 负例；报告对 G2 只给计数、不给合规结论 —— U11（原文可见 + violations 空 + 分母 ≥1 + 无合规结论，变形 M3/M4 转红）与 U12（放宽标记存在且 `p16_target` 未降低）
- [x] 《P1.5任务清单》在 **T31.2 条目下**追加 R-b 子项行；**T31.2 仍不勾选**（要等本任务验收后重跑 dev） —— `docs/P1.5任务清单.md:185` 为 R-b 子项行；`:178` 的 T31.2 仍是 `- [ ]` 未勾

**以下到本节末尾为 RETURN_TO_DESIGN 后新增/改写的验收条**（关闭首审 `T312Rb-CR-01/02/03`），判据一律是"约定输入被真实执行"而不是"用例是绿的"。**本块刻意不写条数**——第九轮就是在这里写"五条"却列了六条（第十轮 `PG-T312Rb-09-02`），边界改由"到节末为止"界定：

- [x] **U1/U11 的文本都经 `aggregate_contract` 进了聚合报告**：U1 的 violation 逐字出现在 `final_consistency.known_path_absence_violations`；U11 的绕过文本**原文逐字出现在 `questions[*].answer.answer_text`**、violations 为空，`known_path_absence_decidable_segments ≥ 1` 作补充而非替代 —— U1 断言明细逐字等于单元素列表（变形 M5 转红）；U11 断言原文见于 `questions[*].answer.answer_text` 且分母 ≥1（变形 M3/M4 转红）
- [x] **U3 读的是那份已落盘报告本身**：`_T312RB_LANDED_VIOLATIONS` 合成夹具已从测试中删除，`grep -n "_T312RB_LANDED_VIOLATIONS" tests/` 零命中 —— `grep -rn "_T312RB_LANDED_VIOLATIONS" tests/` 零命中
- [x] **U7 有游离源键负例**且在旧透传实现下必红；U7b（无源键 → `KeyError`）保留 —— monkeypatch 缩短 `GATE_KEYS` 构造游离源键；变形 M1（改回原样透传）转红；U7b 保留
- [x] **U6 让现场生成的 v2 aggregates 真的走过 `_assert_p15_contract_shape`**，且"谎标成 v1"的分派负例转红 —— 新增 `test_freshly_generated_v2_report_passes_its_own_version_shape`，含「谎标 v1」与「未登记 v9」两个分派负例；变形 M6 转红
- [x] 范围节不再手写文件份数（权威值只在 metadata），且 integration test 在正文中列出 —— 范围节指向 metadata `allowed_paths`，三个测试文件逐个列出；`make preflight` PASS
- [x] `make ci`、`make eval-ci`、`make verify-task`、`make verify-full` 全绿，且 `verify-full` 跑在**分支最后一个提交**上、日志直接绑定该 HEAD（`T312Rb-CR-03`；之后不得再追加记录提交） —— exact HEAD `a10f0a7a1c9e8395d49ebbc68264b9fd12eb129d`、工作区干净、`verify-full` exit=0（`ruff`/`pyright` 全绿、`pytest` 1319 passed、`eval-ci` 584 + 65 passed）。**复审 PASS 后的本次验收回填提交只改文档，不构成新的门禁证据**

## GPT 前置计划审查

### 第一轮（2026-08-06）

- 结论：**`PLAN_REVISION_REQUIRED`**（4 项：2×P1、2×P2）
- 审查基准：本 packet + 《Evaluation-v1.5》§5.1/§2/Holdout 清单 + `eval_contract.py` 相关段 + 用户 2026-08-05 的两项裁决（G2 取 "B+C"；c11 改条件式、c02 判真失败）

**送审重点的裁决（已采纳并写进上方计划）**：

| # | 裁决 | 落点 |
|---|---|---|
| 1 | schema **必须**升到 `p1.5-contract-v2`——同一版本不能同时表示旧报告 13 键和新报告 12 键 | 实现计划第 1 行⑤、第 4 行 |
| 2 | §5.1 第 2 条在 **dev 与 holdout 同步**降为报告项，避免 split 间语义分裂 | 实现计划第 6 行（原为待裁决问题，现为必做项）|
| 3 | **维持 R-b 打包**：用户已有明确裁决，且仅 1 个核心源文件，size exception 可接受 | `size_exception` 保持不变 |

**阻塞项与我的核实结果**（4 项**全部属实**，逐条亲自读代码/查数据确认，非照单接受）：

| Issue | 级别 | 我的核实 | 修订落点 |
|---|---|---|---|
| PG-T312Rb-01 | P1 | **属实，且比原判更糟**。`:1122` 是 `"gate_summary": gates`——直接透传 `:925` 的手写字面 dict，而 `:81` 的注释声称它是投影。只删 `SECTION_GATE_KEYS` 会得到「section 按 6 键合取、`gate_summary` 仍 13 键」的**自相矛盾报告** | 新增 F6b；实现计划第 1 行③④；新增 U7 投影负例；验收标准前两条 |
| PG-T312Rb-02 | P1 | **属实**。`:1146` 硬编码 `"## Gate 汇总（13 键）"`；`:1196` 把共现结果直接写成 `违规 N`——而本 packet 自己的 F4 刚证明了它会误报 | 新增 F6c；实现计划第 2 行；U10 改为锁措辞（含"实现前必红"标注）|
| PG-T312Rb-03 | P2 | **属实，但机制上已被兜住**：`contract_expectations_met` 的第二个合取项 `not retrieved_not_cited` 恰好实现了条件性（三情形推演见 F10b）。**然而这完全不体现在数据行里**、无人锁死，将来任何人放宽那个合取项都会把 c11 悄悄变成真的无条件枚举。审查者要求配对测试是对的 | 新增 F10b；U8 拆为 U8a/U8b/U8c |
| PG-T312Rb-04 | P2 | **属实，两处都是我的错**。`docs/backlog.md:59` 逐字写着 `**P2（阻塞门禁）**`，我却在实现计划写成"关闭 P3"；`status` 也该是 `plan-review`（`workflow_guard.py:21` 列为合法值）| metadata `status`；实现计划第 7 行；验收标准；最终验收栏 |

- 批准范围预告：上述 4 项按最小边界修订后即可复审。**修订不得**扩大改动范围（`src/devkb/agent/` 仍须零改动）、不得引入发生率承诺、不得借"改成真投影"顺手重构 `_conjunction` 或分节结构。

### 第二轮（2026-08-06）

- 结论：**`PLAN_REVISION_REQUIRED`**——`PG-T312Rb-03`/`04` 判为**已按最小边界关闭**；`PG-T312Rb-01` 未再被质疑；**`PG-T312Rb-02` 判为未完全关闭**，另指出 1 处相邻失实注释。
- 审查者同时确认：`4666326` 只改 Task Packet，未增加允许文件、文件预算或业务范围；本轮补充**全部位于既有 `eval_contract.py` 及其测试范围内，不需要扩大范围**。

**三点逐条复核（全部属实，我已逐个打开对应行确认）**：

| # | 审查者指出 | 我的复核 | 修订落点 |
|---|---|---|---|
| a | `:1148-1150` 仍会表述「§5.1 共 11 键 + 两个附加键」，合计 13，与 12 键标题自相矛盾；U10 未覆盖这段 | **属实**。`zero_absence_assertion_on_known_paths` 正是 §5.1 **第 2 条**那一键，降级后该数应为 10。第一轮我只盯着标题字面量，没往下读三行 | F6c 第 2 项；实现计划⑧；U10a 增设 `len(GATE_KEYS) - 2` 机器锁 |
| b | U10 要求 Markdown 与 JSON `final_consistency.note` 两个面都出现该声明，但实现计划只写了 `:1196` | **属实，而且是我的计划与我自己的冻结测试打架**——`:1067-1071` 的 note 今天连这句话都没有 | F6c 第 4 项；实现计划⑩；U10b 参数化跑两个面 |
| c | `:84` 的「第 13 键」注释在 `PG-T312Rb-01` 完成后将成为失实注释 | **属实**；只有 `:84` 是会失实的**现在时键数声明**。（当时我把同段 `:81-83` 笼统写成"史料"——**这句归类不准，第四轮已更正**：只有 `:82`–`:83` 前半是史料，`:81` 与 `:83` 后半是设计声明。结论不变，理由要写准）| F6c 第 5 项；实现计划⑪ |

**这一轮暴露的是同一种毛病**：第一轮我把 PG-T312Rb-02 读成"改两个字面量"，实际它是「**报告里所有陈述键数与判定口径的地方**」。查法上的教训——找硬编码不能只 grep 数字 `13`，还要 grep 与之**加总相等的分解**（`11`）、**同义的措辞面**（JSON note）、以及**同一事实的第二处副本**。

**按新查法重扫后又自查出 2 处前审也没列的**（`:981` 的第二个「第 13 键」、`:907` docstring 的归属错误），故 F6c 由 2 → 5 → **7** 处（**本轮当时的数字；第三轮再加 `:108-109` 后为 8 处——权威值以 F6c 正文为准，本段只记录本轮进展**）。其中 `:907` 的「§5.1 九条落到 12 个具名硬键」**今天就已经是错的**（§5.1 九条实落 11 键），降级后总数虽巧合变成 12，归属仍错——属本任务顺路改正的失实注释，不是新引入的问题。同时显式列出**不该动的各处**并写进验收标准（当时按"9 处"计数，**第四轮已改为按 A–E 五类理由 + `git diff` 边界核对**，不再数个数），避免"重扫"变成扩大改动面。

### 第三轮（2026-08-06）

- 结论：**`PLAN_REVISION_REQUIRED`**——仅 1 处边界修正。`:907` 与 `:981` 两处自查**判定属实**；其余 `PG-T312Rb-02` 修订边界**判定正确**，无需扩大文件范围。
- 唯一阻塞：**`:108` 不应列入"不该动"**。它写着「§5.1 九条 → 具名硬键……」，第 2 条降级后失实。

**我的复核**：**属实，是我上一轮的分类错误**。我按「有没有提到 §5.1 九条」这个**表面特征**把 `:90`/`:101`/`:108`/`:1150` 一起归进"规格条数、不该动"，但 `:108` 与另外三条根本不同类——

| 行 | 断言的是什么 | 降级后 |
|---|---|---|
| `:108-109` | **推导链**：§5.1 九条 **→** 具名硬键（含拆分规则）| **失实**（第 2 条不再产键，链断了）|
| `:90` | **成员关系**否定：L0/L1 **不属** §5.1 九条 | 仍成立 |
| `:101` | **成员关系**否定：fail-fast 来自 §5.2 第 8 条**而非** §5.1 九条 | 仍成立 |
| `:1150` | **规格自身**的条数：§5.1 **没有**十条 | 仍成立 |

判据因此收紧为「**该注释断言的是推导链，还是规格事实/成员关系**」——前者随键集合变化，后者不随。已按此重新审一遍剩余各处，未再发现同类漏网。F6c 由 7 → **8** 处，"不该动"由 9 → **8** 处。

### 第四轮（2026-08-06）

- 结论：**`PLAN_REVISION_REQUIRED`**——2 处记账/措辞修正，**均在本 packet 内部，不涉及代码范围**。`:108-109` 判定已正确移入 F6c；`:90`/`:101`/`:1150`/`:1267` 归类判定正确。

| # | 审查者指出 | 我的复核 | 修订落点 |
|---|---|---|---|
| a | 实现计划第 2 行仍写「共 **7** 处」「前审**两轮**」，虽已加入 `:108-109` 但计数未同步 | **属实**。我在 F6c 正文把 7 改成 8、也补了第 8 项，却漏了实现计划那一行的两个数字——**同一事实在两处各写一份，改了一处忘另一处**，与本任务要修的 `:1146`/`:1148-1150` 是**完全同型**的缺陷 | 实现计划第 2 行改为「共 **8** 处」「**三轮**」|
| b | 不应笼统称 `:81-83` 全为"历史史料"：`:82` 是历史成因，`:81`/`:83` 是**由真投影修复后成立的设计声明**；`:1112` 同类。不列入 F6c 的结论成立，但理由须写准 | **属实**。我在 F6b 的"副产物"里已经把 `:81`/`:83`/`:1112` 正确识别为设计声明，**却在另外三处仍沿用旧的"史料"说法**——又是同一事实两处不同步 | F6c 第 5 项措辞；「不改」说明重构为 **A–E 五类理由表**；实现计划第 2 行末句；验收标准；第二轮表格 c 行加更正 |

**这一轮的教训比前三轮更直接**：两条都不是"没查到"，而是**我已经知道对的答案，却没把它同步到写着旧答案的每一处**。而本任务的实现内容恰恰就是修 `eval_contract.py` 里同一类问题（键数写在 8 个地方）。据此把"不该动"的验收标准由**数个数**改为**可机器核对的边界**（`git diff` 里注释行改动必须恰好落在 F6c 八处内），避免再靠人肉计数。

### 第五轮（2026-08-06）

- 结论：**`PLAN_REVISION_REQUIRED`**——1 处交叉引用错编号，packet 内部，不涉及代码范围。审查者确认：①实现计划第 2 行已同步为「8 处 / 三轮」；②A–E 分类正确；③历史记录里的 7/9 计数已有明确时点说明，**不属于**未同步。
- 唯一阻塞：packet 三处写「由**修⑤**补实」，但真投影是实现计划第 1 行的**修④**，⑤ 是 schema 升级。

**我的复核**：**属实**。实现计划第 1 行的编号是 ①词表收窄 / ②删 `SECTION_GATE_KEYS` / ③删 `gates` 字面项 / ④`:1122` 真投影 / ⑤schema 升 v2 / ⑥renderer 末行——断言"投影"的 `:81`/`:83` 后半/`:1112` 由**④**补实，与 ⑤ 无关。共 4 处（F6b 副产物 1 处、A 类说明 2 处、验收标准 1 处），已全部改为修④。

**顺带按同一方法扫了 packet 里全部圈码交叉引用**，其余**五处**均正确：`第 1 行⑤`（schema，裁决表第 1 行）、`第 1 行③④`（投影，PG-T312Rb-01 落点）、`⑧`（`:1148-1150` 来源说明）、`⑩`（JSON note）、`⑪`（`:84`）。

**连续两轮都栽在同一件事上**：第四轮是计数没同步，第五轮是编号引错——都属于「**同一事实在 packet 里存在多份副本，改一处忘另一处**」。这正是本任务要在 `eval_contract.py` 修的缺陷形态（键数散在 8 处）。区别只在于，代码那边我给出了机器锁（`len(GATE_KEYS)` 派生 + U10a 的 `len(GATE_KEYS) - 2` 断言），而 packet 这边是纯散文、没有任何机器检查——**这条不变量本身就说明了为什么代码侧必须做成派生而不是字面量**。

### 第六轮（2026-08-06）

- 结论：**`PLAN_REVISION_REQUIRED`**——1 处计数与其后紧跟的枚举不符。审查者确认：四处「修⑤」已全部改为修④（F6b 1 处、A 类说明 2 处、验收标准 1 处），第五轮记录里的「修⑤」是历史引文不需改；五个圈码引用均指对。
- 唯一阻塞：第五轮记录写「其余**四处**均正确」，随后实际列出**五处**。

**我的复核**：**属实**，已改为五处。

**这是连续第三轮同型错误**（第四轮计数没同步、第五轮编号引错、第六轮计数与紧邻枚举不符），所以这次不只改那一个字，而是**把 packet 里每一处"数字 + 紧跟枚举"逐条对了一遍**：

| 检查项 | 结果 |
|---|---|
| `size_exception` 的 9 / 1 个核心文件、范围节「9 个」 | 与 metadata `allowed_paths`（9 条）、`max_changed_files=9` 一致 ✓（**本轮当时的数字**；实现期第二次扩容为 10，而范围节那份手写副本没跟上——这就是首审 `T312Rb-CR-02`，第九轮已把该副本删除、改为指向 metadata）|
| F1「11 条 + 12 条 = 23 条」 | 与 `not_found.py:173-185` / `:150-163` 实际条数一致 ✓ |
| F2「未覆盖×10、缺失×1、不存在×1、未包含×1」= 13 | 与报告实测一致 ✓ |
| F5「确定性尾注四处」 | `security.py:55`/`config_layers.py:92`/`nodes.py:560`/`answer.py:63` 逐个打开确认 ✓ |
| F6b「断言投影的注释一共三处」 | `:81`/`:83` 后半/`:1112` ✓ |
| F6c「共 8 处」+ 溯源「2 → +3 → +2 → +1」 | 条目 1–8 逐条可数，加总 = 8 ✓ |
| F6c「按理由分五类」 | A/B/C/D/E ✓ |
| F9「dev 13 题：partial×10 / refusal×2 / policy_refusal×1」 | 与 `contract_dev.jsonl` 实际取值一致 ✓ |
| F14「13 被锁在四处」 | `test_eval_contract.py:236`/`:803-822`/`:932-947`/`:825` ✓ |
| 状态流「由 6 键而非 7 键合取」 | `final_consistency` 现 7 键，减 1 = 6 ✓ |
| U2「13 条里的 12 条形态」 | 10+1+1 = 12 属 `_ABSENCE_MARKERS`，余 1 条 `不存在` ✓ |
| U10a「恰好 2 键非 §5.1」 | `l0_l1_all_pass` + `fail_fast_isolation_ok` ✓ |
| 第五轮「其余五处」 | 本轮修正 ✓ |
| **「尚未证实的假设」第 1 条的「其余 12 题」** | **✗ 无来源**：`12` 是从"13 条违规"顺手减出来的，本轮实为 20 题，且`条`≠`题`。**本轮一并删除**（自查发现，不在前审列出的问题内）|

**结论**：packet 里现在只剩 **1 处**手写计数没有机器锁，就是 F6c 的"8 处"——它由紧邻的编号列表 1–8 自证，无法再漂移。

### 第七轮（2026-08-06）

- 结论：**`PLAN_REVISION_REQUIRED`**——1 处轮次标注错。审查者确认：第五轮「五处」已改对且枚举相符；第六轮计数表其余各项与正文一致；「其余 12 题」已从生效假设中删除、仅存历史说明。
- 唯一阻塞：该历史说明写「本行**第五轮**修订」，实际由第六次修订加入（`16a47a6`），且第六轮表写「本轮一并删除」，自相矛盾。

**我的复核**：**属实**，已改为「本行第六轮修订」。顺带核了 packet 正文里全部**内联轮次自述**（F6b 副产物"第三轮"、F6c 溯源链"第一/二/三轮"、F6c 第 5/6/8 项、"不改"表引言、实现计划第 2 行的"三轮"），**均与提交历史相符**。

**七轮小结（供后续任务参考）**：技术实质在第三轮后未再被质疑；第四至七轮**全部**是 packet 自身的记账一致性——计数没同步、编号引错、计数与紧邻枚举不符、轮次标注错。四次同型复发指向同一个结构性原因：**这份 packet 里同一事实存在多份手写副本，而散文没有任何机器检查**。这恰是本任务要在 `eval_contract.py` 侧根除的形态（键数散在 8 处），也是把它改成 `len(GATE_KEYS)` 派生、并用 U10a 的 `len(GATE_KEYS) - 2` 断言锁住散文的直接理由。**已登记 `WF-P3-04` 候选**：packet 模板宜禁止"数字 + 紧跟枚举"的写法，或要求计数由枚举自证。

### 第八轮（2026-08-06）

- 结论：**`PLAN_APPROVED`（用户直接批准）**。用户 2026-08-06 指示「那就开工」，据此把 `status` 由 `plan-review` 改为 `approved`。
- **批准的是第七轮修订后的计划全文**——即 F6c 八处修改点、A–E 不改分类、实现计划 8 行、U1–U12 冻结测试矩阵、schema v2 连锁、dev/holdout 同步降级。这部分自第三轮起未再被质疑，第四至七轮的四次返工全部落在叙事散文的记账一致性上，**不改变任何实现产物**。
- **实现纪律不因直接批准而放宽**：仍按合同先落冻结测试（U10a/U10b 两条**实现前必红**并留证），`src/devkb/agent/` 零改动，触发「风险和停止条件」任一条即停止上报。

只有结论为 `PLAN_APPROVED` 后，metadata 的 `status` 才能改为 `approved`。

### 第九轮（2026-08-07，首审 `RETURN_TO_DESIGN` 后的设计修订）

- 触发：`$project-scoped-review` 首审结论 **`RETURN_TO_DESIGN`**，3 项 P2（`T312Rb-CR-01/02/03`），逐条见下方「Review Ledger」。
- `status` 由 `approved` → **`return-to-design`**（`workflow_guard.py:19-28` 的合法值之一）。它**不在** `IMPLEMENTATION_STATUSES` 内，故 `make verify-task` / `verify-full` 在重新获批前会主动报「不允许进入实现/验收」——这正是期望行为，也是"重新拿到 `PLAN_APPROVED` 之前不得再写实现"的机器约束。
- **首审对 `src/devkb/eval_contract.py`、`evalsets/v1.5/contract_dev.jsonl`、《Evaluation-v1.5》、`docs/backlog.md` 零 finding**。本轮修订因此**不动生产代码**，也不动已批准的实现计划第 1–8 行；改的是"冻结测试如何被真实执行"这一设计缺口。

**逐条复核（照例不照单接受，每条都在本轮实际跑过命令）**：

| # | 首审指出 | 我的复核 | 修订落点 |
|---|---|---|---|
| CR-01 U3 | 用 10 份重复合成句 + 3 份近似句、路径统一替换成 `README.md`，从未读那份报告 | **属实**。`_T312RB_LANDED_VIOLATIONS`（`test_eval_contract.py:1894-1901`）是手写元组。本轮实际读 `p1.5-dev-contract-20260805T174422+0800.json` 的 13 条真实 `segment`/`path` 重算：**命中恰 1 条**，`phrase=不存在`、`path=…/common/BusinessException.java`，与 F3/F4 一致——**结论本身没错，但在此之前没有任何测试执行过它**，F3 的"实测"只活在 packet 散文里 | U3 行；实现计划第 9 行ⓐ；验收标准 |
| CR-01 U7 | 只比较正常键集合，没有游离键；执行者自己也记了它在旧透传实现下本就是绿的 | **属实**。`gates` 是 `aggregate_contract` 内的字面 dict，外部塞不进去——**这正是原实现"退化成键集合相等"的原因，但退化没被记下来**。等价可执行构造：monkeypatch 缩短 `GATE_KEYS`，让被摘掉的键成为游离源键。本轮实测：缩短后 `gate_summary` 恰为 11 键、被摘键**不泄漏**；透传实现下它必然泄漏 → 该断言实现前必红 | U7 行；实现计划第 9 行ⓑ |
| CR-01 U11 | 报告由无关的 `_green_rows()` 生成，绕过词表的文本从未进入报告 | **属实**（报告确由 `_green_rows()` 生成）。实测该文本 `violations=0` 而 `decidable_segments=1`。**但本轮我附带的另一个判断是错的**——我据此说"原文不可能出现在报告里"并把主判据换成分母，第十轮 `PG-T312Rb-09-01` 指出并经我复核：`assemble_row`（`:1405`）保留 `"answer": answer`、`aggregate_contract`（`:1137`）保留 `"questions": rows`，已落盘报告的 20 题里 `answer.answer_text` 逐字可查。原冻结用语一直可执行，已恢复为主判据、分母降为补充 | U11 行（含第十轮更正）；实现计划第 9 行ⓒ |
| CR-01 U6 | shape helper 只处理已提交的 v1 报告，新生成的 v2 从未传入 | **属实**。`_assert_p15_contract_shape` 只在 `_load_reports()` 遍历里被调用；`test_current_harness_emits_exactly_the_locked_v1_2_shapes` 只断言常量。本轮实测 `aggregate_contract([], [], {...}, split="dev")` 就能产出合规 v2 aggregates（五节齐备、12 键、`all_hard_gates_passed` 为 bool、无禁用总分键），裹进最小信封即可真过 helper。**另补分派负例**：同一份 v2 谎标成 v1 必须失败——否则"按版本分派"可能只是两个分支恰好都放行 | U6 行；实现计划第 10 行 |
| CR-02 | metadata 已是 10 个允许文件且含 integration test，范围正文仍写"两个单测文件"和"9 个" | **属实**。修法不是把 9 改成 10：**份数本来就不该在正文里有第二份手写副本**——这与第四至七轮四次返工、以及本任务要在 `eval_contract.py` 修的"键数散在 8 处"是同一形态。故正文改为指向 metadata 权威值，并把三个测试文件逐个列出 | 范围节两行 |
| CR-03 | 全部门禁日志绑 `9c8f78d`，当前 HEAD 是 `ef6ca73` | **属实，且是结构性的**：把"HEAD + 其门禁结果"写进 packet 必然产生新提交，日志当场与 HEAD 脱钩（packet `:395` 早就记过这个无限 amend 问题，但当时只绕过了 SHA、没绕过日志）。解法见「最终验收」：**最后一次提交之后再跑一次 `make verify-full`，日志随审查交接提供，不再追加记录提交** | 最终验收节 |

**自查出 1 处首审未列的同型缺陷（U1）**：冻结行要求 violation「出现在 `final_consistency.known_path_absence_violations`」，实现只断言了 `score_known_path_absence` 的返回值——与 U11 完全同型（合同要求"进报告"，测试只测了打分函数）。已一并改写，不扩大文件范围。

**按同一查法重扫其余各行，未再发现同型**：U2/U4/U5/U9/U10a/U10b 的约定输入就是它们实际构造的对象；U12 直接读 `evalsets/v1.5/contract_dev.jsonl`。特别核过 **U8a/b/c**——它们用字面 `expected=("partial","refusal")` 而不是读数据行，看起来像替身，但 c11 的真实标签由 **U12 读数据文件锁死**、`partial_or_refusal` → 原子对的解析由 **U9** 锁死，三者构成完整链条，**不属同型缺陷**。

**这一轮与前八轮的区别**：第四至七轮的返工全在 packet 散文的记账一致性，**不改变实现产物**；`CR-01` 的各条子项（首审列出 U3/U6/U7/U11，自查补入 U1）则是**交付的测试没有执行合同约定的输入**——即"冻结测试"这道工序在 ledger 列出的每一处都落空了，而执行者自审第 6 条还逐行打了勾。教训不是"再仔细一点"，而是：**冻结行必须把"输入从哪来"写成可执行的取数方式**（读哪个文件的哪个字段、用什么手段构造负例），否则实现期必然被替身夹具顶替。U1/U3/U6/U7/U11 已按此改写，`WF-P3-04` 一并扩充为"冻结测试矩阵须写明输入来源"。

### 第十轮（2026-08-07，第九轮修订的前审）

- 结论：**`PLAN_REVISION_REQUIRED`**（`PG-T312Rb-09-01`、`PG-T312Rb-09-02`）。审查者同时确认成立的送审点：CR-02 的范围修法正确；U1/U3/U6/U7 的输入均可执行且能排除替身夹具；实现计划第 9–11 行不改生产代码；CR-03 的"最后提交后运行 `verify-full`、日志随交接提供且不再提交"能够形成 exact-HEAD 证据；`d1e1c79` 工作区干净、`make preflight` 通过。

| # | 审查者指出 | 我的复核 | 修订落点 |
|---|---|---|---|
| `PG-T312Rb-09-01` | U11 不应以"分母"替代"原文可见"：`assemble_row` 保留 `"answer": answer`、`aggregate_contract` 保留 `"questions": rows`，绕过文本可在 `questions[*].answer.answer_text` 直接断言 | **属实，是我第九轮的事实错误**。我从"收窄后它不产生逐条明细"推出"原文不可能出现在报告里"——只查了 `final_consistency` 的几个明细字段，**没查报告还原样保留每题的 `answer`**。本轮实读 `:1405`/`:1137` 两处并核已落盘报告：`aggregates.questions` 有 20 题、每题 `answer.answer_text` 逐字可查。**这与 CR-01 是同一种毛病**——那边把"约定输入"换成替身夹具，这边把"约定判据"换成更好断言的替代物，两次都是**削弱了原判据**却写成"等价物" | U11 行恢复原判据并加第十轮更正；实现计划第 9 行ⓒ；验收标准；第九轮记录该行；Review Ledger `CR-01`ⓒ |
| `PG-T312Rb-09-02` | 第九轮仍有计数漂移：验收标准块头写"五条"实列六条、"四个点"与五个子项不符、P3 栏写"CR-01 四条子项"而 ledger 为五条 | **全部属实**；另自查执行者自审的"四行用例"、冻结测试对表引言的"五行"属同型，一并处理。**这是第五次同型复发**（前审第四至七轮四次 + 本次），且发生在一份正文刚写着"份数不该有第二份手写副本"的 packet 里。故这次不逐个改对数字，而是**一律删掉手写计数**，边界改由紧邻枚举或"到节末为止"自证 | 验收标准块头（删计数 + 改边界界定，并把《P1.5任务清单》那条移到块外）；第九轮小结段、执行者自审、对表引言、P3 栏四处删数字 |

**两条的共同点**：我手上都已经有正确材料，却在概括时把它换成了更弱、更好写的东西。`WF-P3-04` 据此再扩充一条：**修订不得以"等价物"替换冻结判据，除非先证明等价**。

### 第十一轮（2026-08-07，第十轮修订的复核）

- 结论：**`PLAN_APPROVED`**。`PG-T312Rb-09-01` 判为已关闭（实现计划、U11 冻结行与验收标准均恢复"原文逐字出现在 `questions[*].answer.answer_text`"为主判据，分母仅作补充）；`PG-T312Rb-09-02` 判为已关闭（原三处及自查两处分别改为节边界、显式枚举或"各行/各条子项"）；`d52a0df` 仅修改本 packet、未扩大范围，exact HEAD 工作区干净、`make preflight` 通过，未直接引入新的阻塞项。
- 据此 `status` 由 `return-to-design` 改回 **`approved`**，按实现计划第 9–11 行重新实现（生产代码零改动）。

## 执行者自审

- [x] 实际文件全部在 allowed_paths —— `make verify-task` 报 `changed files: 10 / 10`，逐个在表内
- [x] 没有未批准依赖、迁移、API、golden/snapshot 变化 —— 零 `pyproject/uv.lock`、零 `alembic/`、零 `evalsets/reports/`。**schema 变化是本任务的批准内容**（`p1.5-contract-v1` → `v2`，前审裁决 1 判定为必然连锁），已按版本分派保证旧报告继续可校验
- [x] 没有大面积格式化、debug、TODO/FIXME、`if testing` —— `ruff format` 只跑了本任务触及的文件
- [x] 没有吞异常、降低断言、skip/xfail —— 见下「删除行审计」。三处既有断言改动（b4e 词表、b4g 聚合、schema 字面量）均为**跟随批准的判据变化**，且 b4g 改后**更强**（原来只断言 gate 为 None，现在逐条断言明细仍落进 section）
- [x] 状态、权限、幂等、失败行为符合合同 —— `src/devkb/agent/` **全目录零改动**（`git diff --stat` 为空）；`PROMPT_VERSION` 未动；D6 调用点与预算未动；D7 无查询构造改动
- [!] 冻结测试真实运行（逐行对表）—— **本条自审失效**（首审 `T312Rb-CR-01`）：U3/U6/U7/U11（以及自查补入的 U1）确实都是绿的，但**没有执行合同约定的输入**（合成夹具 / 无关报告 / 正常状态 / 从未传进 helper / 只测打分函数返回值）。对表时我只核了"函数名对得上、结果是 PASS"，**没核"跑的输入是不是合同写的那个"**——这正是"逐行对表"该拦下的东西。见下「冻结测试对表」的失效标注与第九轮
- [!] 新增用户可见文案均有禁用措辞否定断言；两条「推不出」各有 fail-closed 负例 —— U10b 参数化跑 Markdown/JSON 两个面（**成立**）；U12 直接读数据文件（**成立**）；**U11 不成立**——绕过词表的文本从未进入报告，见 `T312Rb-CR-01`
- [x] `make verify-task PACKET=<本文件>` 通过
- [x] 已创建 candidate commit，工作区干净
- [x] 日志记录 candidate HEAD 和对应命令结果

### 先红证据

实现前跑 `uv run pytest -q tests/unit/test_eval_contract.py -k t312rb` = **12 failed, 6 passed**。
6 条绿是**回归锁**（改动不得破坏它们），不是空转：U1（仓库级否定仍照常命中）、U8a/U8b/U8c（条件性由 `retrieved_not_cited` 承担，F10b 说的"机制已存在"由此机器证实）、U9（非法原子仍抛）、U7（键集合当时恰好相等）。
其中 **U10a/U10b 两条合同要求"实现前必红"的，实测确实红**。

### 冻结测试对表（逐行指到函数名）

> **首审 `T312Rb-CR-01` 后加注**：下表原样保留（PASS 都是真的），但 **PASS ≠ 执行了约定输入**。带 ❌ 的各行（U1 为自查补入）跑的是**替身输入**，即本对表工序当时没有核到的东西。修订后的约定输入写在「冻结测试」矩阵里。

| packet 冻结行 | 测试函数 | 结果 |
|---|---|---|
| 正常：仓库级否定 + 已知路径 | `test_t312rb_u1_repo_level_negation_on_a_known_path_is_still_recorded` | PASS ❌ **只测打分函数返回值，未进聚合报告**（自查）|
| 正常：`.md` 单换行结尾 | `test_t312rb_u5_contract_markdown_ends_with_exactly_one_newline` | PASS（实现前红）|
| 边界：覆盖缺口措辞不再计 | `test_t312rb_u2_coverage_gap_wording_no_longer_counts`（3 参数化）| PASS（实现前 3 红）|
| 边界：13 条重算 = 1 | `test_t312rb_u3_recomputing_the_landed_thirteen_leaves_exactly_one` | PASS（实现前红）❌ **跑的是合成夹具，未读那份报告** |
| 失败 c11 配对① | `test_t312rb_u8a_refusal_is_accepted_when_progress_was_never_retrieved` | PASS |
| 失败 c11 配对② | `test_t312rb_u8b_refusal_still_fails_when_progress_was_retrieved_but_not_cited` | PASS |
| 失败 c11 配对③ | `test_t312rb_u8c_full_still_fails_because_it_is_not_in_the_atom_set` | PASS |
| 越权/非法状态 | `test_t312rb_u9_illegal_expected_mode_atom_still_raises` | PASS |
| 组合：`gate_summary` 12 键 | `test_t312rb_u4_demoted_key_leaves_gate_summary_but_details_stay` | PASS（实现前红）|
| 组合：投影负例 | `test_t312rb_u7_gate_summary_is_a_real_projection_not_a_passthrough` + `…u7b_projection_fails_loud_when_a_gate_key_has_no_source` | PASS（u7b 实现前红）❌ **U7 退化成键集合相等，无游离源键**（u7b 成立）|
| 组合：v1/v2 各按自己版本 | `test_gate_reports_carry_complete_locked_gate_fields`（经 `_assert_p15_contract_shape` 按 `schema_version` 分派）+ `test_current_harness_emits_exactly_the_locked_v1_2_shapes`（锁 v2 字面量 + v1 键集合差恰为降级键）+ `test_schema_version_is_frozen` | PASS ❌ **只有已提交的 v1 走过 helper，v2 从未传入** |
| 组合：标题与来源说明派生 | `test_t312rb_u10a_markdown_key_count_and_provenance_are_derived` | PASS（实现前红）|
| 诚实边界（两个面）| `test_t312rb_u10b_g2_is_reported_as_a_count_not_a_verdict`（2 参数化）| PASS（实现前 2 红）|
| fail-closed 负例① | `test_t312rb_u11_narrowed_scan_cannot_prove_the_absence_of_false_claims` | PASS（实现前红）❌ **报告由无关 `_green_rows()` 生成，绕过文本从未进报告** |
| fail-closed 负例② | `test_t312rb_u12_c11_relaxation_is_marked_and_does_not_lower_the_p16_target` | PASS（实现前红）|

### F6c 八处 + "不该动"边界的机器审计

验收标准写的是**可机器核对的边界**而不是数个数，实际执行的命令与结果：

- 八处全部落地：`:1146` 标题派生、`:1148-1150` 来源说明 §5.1 = 10 键 + 「第 2 条已降为报告项」、`:1196` 计数口径、`:1067-1071` JSON note、`:84` 与 `:981` 两处「第 13 键」→「第 12 键」、`:907` docstring 归属、`:108-109` 推导注释。
- **A 类三处（`:81` / `:83` 后半 / `:1112`）逐字未动**：`git diff | grep -E "^-.*(投影|同一张映射派生)"` **零命中**——它们由修④在代码侧变真，不靠改措辞。
- **B/C/D/E 类逐条核过**：`首版把总判定定成 12 个键的合取`、`不属 §5.1 九条`、`而非 §5.1 九条`、`§5.1 **没有**十条`、`十条结构规则`、`判违规的理由`、`违规信号会既错杀`、`不得静默算作无违规`、`展平后每个键`、`七组评分` —— **10 项全部零删除行**。

### 第二次实现的执行者自审（`PLAN_APPROVED` 第十一轮之后）

- [x] 实际文件全部在 allowed_paths —— `make verify-task` 报 `changed files: 10 / 10`，与首次实现同一批文件，未新增
- [x] **生产代码零改动** —— `git diff 9c8f78d -- src/` 为空；本轮只动 `tests/unit/test_eval_contract.py`、`tests/unit/test_eval_reports_schema.py`、`docs/backlog.md`、`docs/dev-log.md`、本 packet
- [x] 没有吞异常、降低断言、skip/xfail —— 五处改动**全部是加强**：U3 由合成夹具改为读真实报告、U7 加游离源键负例、U11 加原文可见 + 分母、U1 加聚合断言、U6 为新增用例。删除的只有 `_T312RB_LANDED_VIOLATIONS` 这个替身夹具本身
- [x] **逐行核对的是"输入"，不是函数名与 PASS** —— 见下「冻结测试对表（重新实现后）」，每行写明输入从哪来
- [x] 每条重写的断言都做了**变形检验**，且变形直接实施该断言声称防住的那个退化（`WF-P3-03` 的教训）—— 见下「变形检验」
- [x] `make ci` / `make eval-ci` / `make verify-task` 全绿
- [x] exact-head `make verify-full` 在**最后一个提交**上重跑，日志随审查交接提供，之后不再追加记录提交（`CR-03`）

### 变形检验（代替"实现前必红"）

生产代码在 `9c8f78d` 就已正确，本轮拿不到真正的"实现前必红"。等价证据是**直接实施每条断言声称防住的那个退化**，断言必须转红；每个变形跑完立即还原，`git diff --stat` 确认 `src/` 未被留下改动。

| 变形 | 实施的退化 | 目标用例 | 结果 |
|---|---|---|---|
| M1 | `gate_summary` 改回 `gates` 原样透传 | U7 | **RED** ✓ |
| M2 | `NEGATION_PHRASES` 改回并入 `_ABSENCE_MARKERS` | U3 | **RED** ✓ |
| M3 | 报告不再保留每题的 `answer` | U11（原文可见）| **RED** ✓ |
| M4 | `known_path_absence_decidable_segments` 恒为 0 | U11（分母）| **RED** ✓ |
| M5 | `final_consistency` 不再收 violations 明细 | U1 | **RED** ✓ |
| M6 | shape helper 对所有 `v*` 用同一套键集合（去掉版本分派）| U6 | **RED** ✓ |

还原后两份测试文件全绿。**M1/M2/M5/M6 恰好是首审那四条子项原本该拦下的退化**——旧实现下它们四条全是绿的，这就是"约定输入被替身顶替"的代价。

### 冻结测试对表（重新实现后，逐行写明输入从哪来）

| packet 冻结行 | 输入从哪来 | 测试函数 | 结果 |
|---|---|---|---|
| U1 正常 | 手写文本 → `_scored` → `aggregate_contract` 的 `final_consistency` | `test_t312rb_u1_repo_level_negation_on_a_known_path_is_still_recorded` | PASS（M5 转红）|
| U3 边界 | **读** `evalsets/reports/p1.5-dev-contract-20260805T174422+0800.json` 的 `known_path_absence_violations` 全部 13 条 `segment`/`path` | `test_t312rb_u3_recomputing_the_landed_thirteen_leaves_exactly_one` | PASS（M2 转红）|
| U6 组合 | 已提交 v1 报告（`_load_reports()`）+ **现场** `aggregate_contract` 产出的 v2 aggregates | `test_gate_reports_carry_complete_locked_gate_fields` + `test_freshly_generated_v2_report_passes_its_own_version_shape` | PASS（M6 转红）|
| U7 投影负例 | monkeypatch 缩短 `GATE_KEYS`，使 `gates` 里被摘掉的项成为游离源键 | `…u7_gate_summary_is_a_real_projection_not_a_passthrough` + `…u7b_projection_fails_loud_when_a_gate_key_has_no_source` | PASS（M1 转红）|
| U11 fail-closed① | 绕过词表的文本 → `_scored` → `aggregate_contract`，从 `questions[*].answer.answer_text` 取回原文 | `…u11_narrowed_scan_cannot_prove_the_absence_of_false_claims` | PASS（M3/M4 转红）|

U2/U4/U5/U8a-c/U9/U10a/U10b/U12 未改动，输入来源见首次实现的对表。

### 删除行审计（证明"没有降低断言"）

| 删除处 | 内容 | 是否削弱 |
|---|---|---|
| `eval_contract.py` `_ABSENCE_MARKERS` import 与并表 | 词表收窄本体 | 否——批准的判据变更，U1/U2 双向锁 |
| `gates` 字面 dict 的 `zero_absence_…` 项 | 降为报告项本体 | 否——U4 断言明细仍在 section |
| `test_eval_contract.py` `_L1` / `_l2_cases` 的该键构造 | 键已不存在，构造无意义 | 否——其余 12 键的 L1/L2 构造逐条保留 |
| L3 `linked` 11→10、两份字面量列表各删一行 | 同上 | 否——`len` 断言同步收紧，仍是逐字全等 |
| `b4e` 的 `"未找到" in NEGATION_PHRASES` | 改为 `not in` | 否——由单向断言改成**双向**（`不存在` in ∧ `未找到` not in），更强 |
| `b4g` 的 gate 三态断言 | 改为断言明细落 section | 否——原来只看一个 None，现在逐条看 `undecided` 长度与 `violations` 内容 |

结论：`READY_FOR_REVIEW`（**2026-08-07 首审判为 `RETURN_TO_DESIGN`**，本节自审第 6/7 两条已在上方标注失效）

## Review Ledger

首审（`$project-scoped-review`，2026-08-07，base `d5a36b5` → head `ef6ca73`）：**`RETURN_TO_DESIGN`**，**3 项 P2**（`CR-01`、`CR-02`、`CR-03`）、0 项 P0/P1。全部落在"冻结测试与记账"层，**对生产代码零 finding**。

下表按**子项**展开，共 7 行 = `CR-01` 的 5 条（ⓐ–ⓔ，其中首审列出 ⓐ–ⓓ 四条、ⓔ 为本轮自查补入）+ `CR-02` + `CR-03`。

| Issue ID | 严重度 | 合同条款 | 文件/位置 | 证据与实际结果 | 最小修复边界 | 状态 |
|---|---|---|---|---|---|---|
| `T312Rb-CR-01`ⓐ | P2 | 冻结测试 U3 行「本轮已落盘报告的 13 条原文重算」 | `tests/unit/test_eval_contract.py:1892-1947` | 期望：读那份报告的 13 条原文。实际：10 份重复合成句 + 3 份近似句，路径统一换成 `README.md`，报告从未被读取或校验。**本轮复核属实**；实读重算的结果确为 1 条 `不存在`/`BusinessException.java`，与 F3/F4 一致，但此前无任何测试执行过它 | 只补强测试：读报告的真实 `segment`/`path` 逐条重算，删除合成夹具 | **已修复**（第 9 行ⓐ；变形 M2 转红）|
| `T312Rb-CR-01`ⓑ | P2 | 冻结测试 U7 行「往 `gates` 塞 `GATE_KEYS` 外的游离键」 | `tests/unit/test_eval_contract.py:1974-1981`；本 packet「先红证据」段 | 期望：游离键不得泄漏进 `gate_summary`。实际：只比较正常键集合；执行者自己记录了它在旧透传实现下本就是绿的。**属实** | 只补强测试：monkeypatch 缩短 `GATE_KEYS` 构造游离源键 | **已修复**（变形 M1 转红）|
| `T312Rb-CR-01`ⓒ | P2 | 冻结测试 U11 行「该文本仍在报告里可见」 | `tests/unit/test_eval_contract.py:2048-2056` | 期望：绕过词表的文本进报告。实际：报告由无关 `_green_rows()` 生成，该文本从未进入。**属实**。（第九轮我曾判"合同用语不可执行"并改用分母口径，**该判断已被第十轮推翻**：原文经 `questions[*].answer.answer_text` 逐字可查）| 只补强测试：断言原文在报告中逐字可见 + violations 为空 + 分母 ≥1（补充）+ 无合规结论 | **已修复**（变形 M3/M4 转红）|
| `T312Rb-CR-01`ⓓ | P2 | 冻结测试 U6 行「新生成的 v2 报告」 | `tests/unit/test_eval_reports_schema.py:163-177` | 期望：v1 与 v2 各按自己的版本过 shape 断言。实际：helper 只被已提交报告的遍历调用，v2 payload 从未传入。**属实** | 只补强测试：现场 `aggregate_contract` 产 v2 → 过 helper；加"谎标 v1 必须失败"的分派负例 | **已修复**（变形 M6 转红）|
| `T312Rb-CR-01`ⓔ | P2 | 冻结测试 U1 行「出现在 `final_consistency.known_path_absence_violations`」 | `tests/unit/test_eval_contract.py:1910-1916` | **本轮自查补入，首审未列**：与ⓒ同型——合同要求"进报告"，实现只断言了 `score_known_path_absence` 的返回值 | 只补强测试：经 `aggregate_contract` 断言明细逐字落进 section | **已修复**（变形 M5 转红）|
| `T312Rb-CR-02` | P2 | 《开发工作流》§1 任务合同：范围须与 metadata 一致 | 本 packet 范围节（原 `:47-48`）| metadata 已是 10 个允许文件且含 integration test，正文仍写"两个单测文件"与"9 个"。**属实** | 同步为 metadata 权威值，不扩大既有批准范围 | **已修复**（范围节改为指向 metadata，并列出三个测试文件）|
| `T312Rb-CR-03` | P2 | 《Definition of Done》「最终日志对应 exact HEAD」 | 本 packet 最终验收节 | 当前 HEAD `ef6ca73`，全部门禁日志绑 `9c8f78d`。**属实且结构性**：写入 HEAD 与日志本身就会产生新提交 | 形成最终候选 HEAD 后干净重跑 `make verify-full`，日志绑该 HEAD，之后不再追加记录提交 | **已执行**（全部改动收进最后一个提交，在该 tip 上重跑 `verify-full`）|

## 定向修复记录

> 本任务首审结论是 `RETURN_TO_DESIGN` 而非 `TARGETED_FIX`，故走了"设计修订 → 重新前审（第九、十轮）→ 重新实现"。第十一轮前审结论 `PLAN_APPROVED`（2026-08-07），下表为据此完成的重新实现。**生产代码零改动**——`src/devkb/eval_contract.py` 在 `9c8f78d` 之后一个字节未动。

| Issue ID | 修复文件 | 回归测试 | 测试结果 |
|---|---|---|---|
| `CR-01`ⓐ | `tests/unit/test_eval_contract.py` | `test_t312rb_u3_recomputing_the_landed_thirteen_leaves_exactly_one` 改为读 `evalsets/reports/p1.5-dev-contract-20260805T174422+0800.json` 的 `known_path_absence_violations` 逐条重算；合成夹具 `_T312RB_LANDED_VIOLATIONS` 已删除 | PASS。变形 M2（词表改回并入 `_ABSENCE_MARKERS`）转红 |
| `CR-01`ⓑ | 同上 | `…u7_gate_summary_is_a_real_projection_not_a_passthrough` 增 monkeypatch 缩短 `GATE_KEYS` 的游离源键负例 | PASS。变形 M1（`gate_summary` 改回原样透传）转红 |
| `CR-01`ⓒ | 同上 | `…u11_narrowed_scan_cannot_prove_the_absence_of_false_claims` 改为让绕过文本经 `aggregate_contract` 进报告，断言原文逐字见于 `questions[*].answer.answer_text` + violations 为空 + 分母 ≥1 + 无合规结论 | PASS。变形 M3（报告不再保留每题 `answer`）与 M4（分母恒 0）分别转红 |
| `CR-01`ⓓ | `tests/unit/test_eval_reports_schema.py` | 新增 `test_freshly_generated_v2_report_passes_its_own_version_shape`：现场 `aggregate_contract` 产 v2 → 实际传入 `_assert_p15_contract_shape`；含"谎标 v1"与"未登记 v9"两个分派负例 | PASS。变形 M6（helper 对所有 v\* 用同一套键集合）转红 |
| `CR-01`ⓔ | `tests/unit/test_eval_contract.py` | `…u1_repo_level_negation_on_a_known_path_is_still_recorded` 增聚合断言：明细逐字落进 `final_consistency.known_path_absence_violations` | PASS。变形 M5（section 不再收明细）转红 |
| `CR-02` | 本 packet | 无（文档一致性）| 范围节改为指向 metadata 权威值并列出三个测试文件；`make preflight` PASS |
| `CR-03` | 本 packet | 无（流程）| 全部改动收进最后一个提交，在该 tip 上跑 `make verify-full`，日志随交接提供，不再追加记录提交 |

## 最终验收

- Base SHA：`d5a36b5b287bdcc4402ec50bafbcc85759745cf8`
- 首次实现 commit：`9c8f78d9655254d1fe00cf55c785d5dc310be5db`（首审 `RETURN_TO_DESIGN`，见 Review Ledger）
- **下列门禁结果是 `9c8f78d` 时点的历史记录，已不再是本任务的验收证据**（`T312Rb-CR-03`）：`make verify-full` PASS（exact HEAD `9c8f78d`，工作区干净，`changed files: 10 / 10`，ruff/pyright 全绿，`pytest` 1318 passed，`eval-ci` 583 + 65 passed，exit=0）；`make ci` 1318 passed（基线 1304）；`make eval-ci` 583 + 65 passed（基线 569 + 65）。
- **exact-HEAD 证据的执行顺序（`T312Rb-CR-03` 的解法）**：
  1. 重新实现（实现计划第 9–11 行）连同本 packet 的全部记录改动**收进最后一个提交**，工作区清空；
  2. 在**该 tip 上**跑 `make verify-full PACKET=docs/tasks/T31.2R-b-gate-honesty.md`，日志直接绑定该 HEAD；
  3. 日志与 HEAD **随审查交接消息提供，不再追加"记录门禁结果"的提交**——追加即让日志与 HEAD 脱钩，这正是 CR-03 的成因（packet 早在 `:395` 记过同一形态的无限 amend 问题，当时只绕过了 SHA、没绕过日志）；
  4. 故本节**不写 Head SHA 与新日志数字**：它们是随提交变化的量，写进来就必然与自己描述的提交错位。本节只保留不随提交变化的事实。
- P0：0（首审）
- P1：0（首审）
- 当前合同内 P2：**0 项未关闭**——`T312Rb-CR-01`ⓐ–ⓔ 全部由重新实现关闭（逐条见「定向修复记录」，每条附变形检验证据）、`CR-02` 由第九轮修订关闭、`CR-03` 由"全部改动收进最后一个提交后重跑 `verify-full`"关闭
- P3/backlog：**已登记进 `docs/backlog.md` 扩展技术债表**——`WF-P3-04`（packet 不得有手写计数；**并如实指出**这是 2026-07-27 T25.2 同根因提案未实施的第五次复发）、`WF-P3-05`（冻结测试矩阵须写明输入从哪来）、`WF-P3-06`（修订不得以"等价物"替换冻结判据）、`WF-P3-07`（评审历史移出被评审文档）、`WF-P3-08`（计划前审缺严重度下限）。以及 `T312-P3-01`（F15：`evalsets/v1.5/*.jsonl` 不受 `GOLDEN_PREFIXES` 保护，改评测集期望标签门禁不拦）
- 远端 exact-head CI：待推送（阶段级要求见 T32.4，不由本任务门禁承担）
- Reviewer 结论：**`PASS`**（终局复核 2026-08-07）。路径：首审 `RETURN_TO_DESIGN` → 设计修订（第九、十轮）→ 重新前审 `PLAN_APPROVED`（第十一轮）→ 重新实现（仅测试）→ 终局复核 `PASS`。复核确认：`CR-01`ⓐ–ⓔ 全部关闭、M1–M6 六个变形均对相应退化转红（证明新增断言不空转）、`CR-02` 范围一致、`CR-03` 在干净 exact HEAD `a10f0a7` 独立重跑 `verify-full`（1319 / 584 + 65）、`9c8f78d…a10f0a7` 的 `src/devkb/eval_contract.py` diff 为空，未引入 P0/P1/合同内 P2
