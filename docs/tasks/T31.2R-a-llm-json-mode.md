+++
task_id = "T31.2R-a"
status = "draft"
risk = "high"
base_sha = "e7896c4a7eec2e7fb6ec9f1b7592b685c2e0bc3a"
max_changed_files = 8
allowed_paths = [
  "src/devkb/llm.py",
  "src/devkb/agent/nodes.py",
  "tests/unit/test_llm_client.py",
  "tests/unit/test_agent_graph.py",
  "docs/tasks/T31.2R-a-llm-json-mode.md",
  "docs/P1.5任务清单.md",
  "docs/dev-log.md",
  "docs/backlog.md",
]
allow_dependency_changes = false
allow_migration_changes = false
allow_golden_changes = false
size_exception = ""
+++

# Task Packet：T31.2R-a agent 结构化调用点稳定取回 JSON，且重问不再饿死 generate

## 任务目标

四个结构化调用点（plan/evaluate/refine/generate）向供应商声明 JSON 输出模式，并把**空串响应**当作失败；单点重问在会挤掉 generate 预留时不再发生。

**可观察结果（三条都是确定性可验的事实，不含发生率承诺）**：①`json_mode=True` 的请求带 `response_format`、P0 对照路径的请求不带；②`content` 为空串/纯空白/`None` 三者均判为空响应；③非 generate 调用的重问在剩余预算不足以给 generate 留 1 次时不发生。背景实测（**只作留档，不作验收判据**）：同一 evaluate 提示上，不带 `response_format` 空 `content` 7/8，带上后 `ok` 31/32。

## 背景与当前问题

- **当前行为**：`src/devkb/llm.py:93` 只读 `choice.message.content`，且只在 `content is None` 时判空响应；空串 `""` 被当作正常返回，交给 `schema.model_validate_json("")` → `json_invalid` → `nodes.py:699` 记 `invalid_structured_output`。
- **复现证据（2026-08-05 实测，真实 `deepseek-v4-flash`）**：
  - **机制**：同一 evaluate 提示两次调用——第一次 `message.content` **len=0**、`message.reasoning_content` len=731 且内容是**合法 `EvaluateOutput` JSON**、`finish_reason="stop"`、`reasoning_tokens=268`；第二次 `content` len=441（合法 JSON）、`reasoning_content` len=485（中文思维链，**不是 JSON**，直接校验失败）。**模型答对了，是客户端读错了字段**；且回退读 `reasoning_content` 在正常情况下会读到思维链，**不是无条件安全的改法**。
  - **发生率（受控探针，同一提示 N 次并发）**：不带 `response_format` → `empty_content` **7/8**；带 `response_format={"type":"json_object"}` → 第一组 `ok` 7/8，第二组 N=24 **`ok` 24/24**（合计 **31/32**，零 `api_error`，证明 `deepseek-v4-flash` 支持该参数）。
  - **预算链**：`graph.py:65` `can_refine` 预留 `remaining_llm_requests >= 3`（refine + evaluate + generate 各 1 次），**该算术不含 D6 明写的「重问消耗全局预算」**。正常 plan(1)+evaluate(1)+refine(1)+evaluate(1)+generate(1)=5，余 1；每轮 evaluate 各多重问 1 次即 =6，`_can_generate`（`graph.py:70` 要求 `remaining >= 1`）为假 → finalize 无 draft → refusal。
  - **实证后果**：T31.2 首次 dev 运行中 c05 的 trace 节点序列为 `plan→retrieve→evaluate(degraded)→refine→retrieve→evaluate(degraded)→finalize`，**`generate` 一次未执行**，`llm_calls=6`；而第二轮 retrieve **确实召回了** `IngestionRecoveryRunner.java`(E1, 行 10-14)。c08 同型。
  - **影响面**：该次 20 题运行 `invalid_structured_output` **29 次**、`default_applied` 15 次、`coverage_mismatch` **20/20**、`full` **0 个**、必需证据命中 6/35。历史对照（同模型）：`p1-dev-agentic-20260719T230855` 21 题 0 次、`p1-holdout-20260721T223756` 10 题 1 次。**成因不作单一断言**（供应商侧行为随时间变化、P1.5 Prompt 变长都可能）。
- **用户影响**：P1.5 的全部契约能力在真实模型上被大面积降级信号污染，dev Gate 三条 FAIL 中的两条（`contract_expectations_met`、`zero_full_refusal_with_direct_evidence`）由此产生；不修则 T31.2 无法重跑出可信基线，T32.3 holdout 更不可能。

## 范围

- **允许修改模块**：`src/devkb/llm.py`（`LLMClient` Protocol + `OpenAICompatLLM` + `FakeLLM`）、`src/devkb/agent/nodes.py`（`_call_llm` 的调用与重问预算）。
- **允许修改文件**：见 metadata（8 个，含 2 个核心源文件、2 个测试、4 份文档；`docs/backlog.md` 仅在产生 P3 时动）。
- **最大核心文件数**：2。
- **预计 diff**：手写净增约 120–160 行（其中新测试约 90–110 行），源码净增 ≤ 40 行。

## 非目标

- **不实现**：G2 词表/降级、c11 标签修订、`render_contract_markdown` 末尾空行（**全部归 T31.2R-b**，用户 2026-08-05 裁决拆两个任务）；重跑 dev（T31.2 重跑在两个修复任务都验收后进行）。
- **不重构**：不动 `route_after_evaluate` / `route_after_refine` / `route_after_verify` 的判定语义；不动 `finalize`；不动任何 Prompt 文本；不动 `MAX_LLM_REQUESTS=6`、`MAX_RETRIEVAL_ROUNDS=2`、`MAX_REASKS_PER_CALL=1` 任一冻结常量。
- **不修改**：`src/devkb/answer.py`（**P0 对照路径的 `SYSTEM_PROMPT` 要求中文散文、不是 JSON**——见仓库事实 F3，故 JSON 模式**必须**按调用点可选，绝不能无条件加进 `complete`）；任何 schema/迁移/API/依赖；`evalsets/**` 任一文件。
- **不切换模型**：`deepseek-v4-flash` 由 ADR-0004 冻结，换模型须新 ADR + 用户裁决；本任务实测该模型在 JSON 模式下 31/32 正常，无须换。

## 仓库事实调查

- **入口**：`OpenAICompatLLM.complete`（`llm.py:75`）是全仓**唯一**真实供应商调用点。
- **调用链**：`nodes.py:672` `_call_llm` → `llm.complete(...)`（四类结构化调用共用这**一处**）；`answer.py:77` `_complete_with_retry` → `llm.complete(...)`（P0 对照路径，唯一另一处）。
- **数据模型**：无新增持久化；`LLMResult` 形状不变。
- **当前状态流**：不新增 AgentState 通道；`llm_calls`/`llm_retries`/`retry_counts` 语义不变。
- **可复用能力**：`_call_llm` 的重问循环（`nodes.py:661-724`）、`recorder.record_llm_request` 的四种 status、`graph.remaining_llm_requests`。

| # | 事实 | 出处 |
|---|---|---|
| F1 | `complete` 只判 `choice.message.content is None`，空串通过 | `llm.py:92-93` |
| F2 | `LLMClient` Protocol 只有 `complete(*, system, user)` 一个方法；`FakeLLM.complete` 签名与之一致，**14 个测试文件**用 `FakeLLM` 但均不传额外参数 | `llm.py:54-55,107`；`grep -rl FakeLLM tests/` |
| F3 | **P0 对照路径要的是中文散文不是 JSON**：`answer.py:32-37` `SYSTEM_PROMPT` 逐字要求"用中文回答……引用证据时标注 [E编号]"，无任何 JSON 约定 | `answer.py:32-37` |
| F4 | 重问循环的预算判定在 `nodes.py:661-664`：`if calls >= MAX_LLM_REQUESTS: 记 budget_exhausted; break`——**它只防超上限，不为 generate 预留** | `nodes.py:661-664` |
| F5 | `_call_llm` 有 `call_key` 参数，可区分当前是哪一类调用（`plan`/`evaluate`/`refine`/`generate`），故"为 generate 预留"可在该函数内确定性判定，无须新状态 | `nodes.py` `_call_llm` 签名与 `warnings.append(f"{call_key}:...")` 用法 |
| F6 | `deepseek-v4-flash` 接受 `response_format={"type":"json_object"}`，零 `api_error`；实测 `ok` 31/32 | 本 packet「复现证据」受控探针 |
| F7 | D6 逐字："单点解析/传输失败最多重问 1 次，重问消耗全局预算，**预算不足即走冻结默认值或报错**"——即重问本就应当以预算充足为前提，本任务是把"充足"的定义补全，不是新增机制 | `CLAUDE.md` LLM 边界（D6） |
| F8 | 无 `tests/unit/test_llm_client.py`；`llm.py` 现有覆盖只有 `test_pricing.py`（费用计算）与 `test_fail_fast_order.py:168`（monkeypatch 工厂，不进真实 `complete`） | `ls tests/unit/`；`grep -rn OpenAICompatLLM tests/` |

- **尚未证实的假设**：无。

## 关键不变量

- **业务**：四类结构化调用点在供应商侧声明 JSON 模式；P0 对照路径**逐字节不受影响**（不传 JSON 模式）。
- **状态**：零新增 AgentState 字段；`llm_calls` 仍是"实际发往供应商的请求数"，含重问。
- **权限/项目隔离**：不涉及。
- **幂等/重试**：`MAX_REASKS_PER_CALL=1` 不变；重问次数只可能**减少**（预算不足时不发），绝不增加。
- **事务/部分成功**：空串响应与格式错误走**同一条**既有失败路径（记 warning + 重问或默认值），不新增第三种终态。
- **LLM/Tool/RAG**：D6 四调用点不变、单 run ≤6 次不变、检索机制与摄取范围**零改动**。
- **失败行为（fail-closed）**：JSON 模式下仍拿到空串/非法 JSON 时，行为与今天完全一致（`invalid_structured_output` → 重问或冻结默认值），**不得**新增"回退读 `reasoning_content`"（用户 2026-08-05 裁决选 A+空串保底，明确不碰该字段）。
- **向后兼容**：`FakeLLM` 新参数带默认值，14 个既有测试文件**零改动**仍通过；`LLMResult` 形状与 `record_llm_request` 的四种 status 不变。
- **判据可证边界**：
  - 「JSON 模式使失败率降到 0」**推不出**——实测是 31/32，残余非零。故验收断言写成「JSON 模式**已声明**」+「空串**被判为失败**」+「预算不足时**不重问**」三条**可确定性验证**的事实，**不写**任何发生率承诺；发生率只作为背景实测数据留档。
  - 「generate 永不被饿死」**推不出**——若 plan 与 evaluate 各重问一次即已用 4 次，再 refine+evaluate 必然超限。故不变量收窄为：**当 `_call_llm` 的重问会使剩余预算不足以留给 generate 至少 1 次时，该重问不发生**；这是可用确定性测试穷举的命题，而"任何路径下 generate 必执行"不是。

## 实现计划

| 顺序 | 文件 | 必要性 | 最小修改 | 对应测试 |
|---:|---|---|---|---|
| 1 | `src/devkb/llm.py` | 唯一供应商调用点；Protocol/Fake 需同步 | ①`LLMClient.complete` 与 `OpenAICompatLLM.complete`、`FakeLLM.complete` 增加**关键字参数 `json_mode: bool = False`**（默认 False → P0 路径逐字不变）；②`json_mode` 为真时向 SDK 传 `response_format={"type":"json_object"}`；③空响应判定由 `content is None` 改为 `content is None or not content.strip()`，仍抛既有 `LLMError("LLM 返回空响应")` | U1–U6 |
| 2 | `src/devkb/agent/nodes.py` | 四类结构化调用共用的一处；重问预算也在此 | ①`_call_llm` 内 `llm.complete(...)` 传 `json_mode=True`（四类调用全部是结构化输出）；②重问前置预算判定：`attempt > 0`（即这是重问）且 `call_key != "generate"` 且 `calls + 1 > MAX_LLM_REQUESTS - 1` 时，**不发重问**，记既有 `{call_key}:budget_exhausted` 并 break（走冻结默认值），即为 generate 预留 1 次 | U7–U12、G1–G3 |
| 3 | `tests/unit/test_llm_client.py`（新增） | F8：`complete` 今天零直接覆盖 | 以 stub OpenAI client 断言：传/不传 `json_mode` 时 `response_format` 的有无、空串/纯空白/`None` 三种响应均抛 `LLMError`、正常响应逐字返回、`FakeLLM` 接受 `json_mode` 且行为不变 | 该文件自身 |
| 4 | `tests/unit/test_agent_graph.py` | 预算不变量的图级锁 | 加：①evaluate 两轮各失败一次时 `generate` **仍被执行**（对照今天的红）；②重问被预算拦下时记 `budget_exhausted` 且**不**增加 `llm_calls`；③`call_key == "generate"` 时预留规则**不**适用（generate 自己可以用掉最后一次） | 该文件自身 |
| 5 | 4 份文档 | 合同/勾选/叙事/P3 | 见验收 | — |

### 数据流

供应商响应 → `content` 非空白校验 → `LLMResult` → `schema.model_validate_json` →（失败）warning + 预算判定 → 重问或冻结默认值。JSON 模式只改**请求**侧一个字段，不改响应处理形状。

### 状态流

不适用 + 原因：不新增状态、不改路由函数语义。`llm_calls` 的推进点与含义逐字不变。

### 异常与恢复流

- 供应商拒绝 `response_format`（本实测未出现，但须 fail-closed）：`openai.OpenAIError` 走既有 `LLMError` 映射，由调用点按既有 `request_failed` 路径处理，**不静默降级为无 JSON 模式重试**（那会隐藏供应商契约变化）。
- 空串/纯空白响应：抛既有 `LLMError("LLM 返回空响应")` → `nodes.py:676` 记 `{call_key}:request_failed:LLMError` → 既有重问/默认值路径。**注意这是行为变化**：今天该情形记 `invalid_structured_output`，改后记 `request_failed:LLMError`——两者都进 warnings 且都消耗预算，但**语义更准**（是没拿到响应，不是模型输出不合规）。该变化须在 packet 与 dev-log 显式留档，并由 U 层断言固定。

### 风险和停止条件

- **如果需要计划外文件**：停止上报（例如发现须改 `route_after_*` 或 `answer.py`）。
- **如果规格与代码事实冲突**：停止，写入清单偏差记录等裁决。
- **如果超过文件预算（8）**：停止上报。
- **如果核心不变量无法证明**：如实记录，不降低断言。
- **如果实测发现 JSON 模式改变了模型输出的语义质量**（不只是投递字段）：停止上报——那超出"读对字段"的范围，须重新裁决。

## 冻结测试

| 类别 | 输入/前置状态 | 预期结果 | 测试位置 |
|---|---|---|---|
| 正常 | stub client 返回合法 JSON，`json_mode=True` | 请求带 `response_format={"type":"json_object"}`；`LLMResult.text` 逐字等于响应 | U1/U2 |
| 正常 | `json_mode` 不传（P0 路径口径） | 请求**不含** `response_format` 键 | U3 |
| 边界 | 响应 `content=""` / `"   "` / `"\n"` | 三者均抛 `LLMError("LLM 返回空响应")`（今天前两者会静默通过） | U4 |
| 边界 | 响应 `content=None` | 仍抛 `LLMError`（既有行为不回归） | U5 |
| 失败 | SDK 抛 `openai.OpenAIError`（含 `response_format` 不受支持形态） | 映射为 `LLMError`，**不**自动改用无 JSON 模式重试 | U6 |
| 重复/重试 | evaluate 首答非法 + 预算充足 | 重问 1 次；`llm_calls` +2；`MAX_REASKS_PER_CALL` 不被突破 | U7 |
| 重复/重试 | 非 generate 调用首答非法，且重问后剩余预算将 < 1 | **不发重问**，记 `{call_key}:budget_exhausted`，`llm_calls` 只 +1，走冻结默认值 | U8/U9 |
| 重复/重试 | `call_key == "generate"` 且剩余恰好 1 次 | 预留规则**不**适用，generate 可用掉最后一次 | U10 |
| 越权/非法状态 | `FakeLLM` 被传 `json_mode=True` | 接受该参数且脚本行为逐字不变（14 个既有测试文件零改动） | U11 + `make ci` |
| 组合/变形不变量 | 图级：evaluate 两轮各首答非法（复现 c05 形态） | `generate` **被执行**、终态非 `refusal`（今天为 `refusal`——实现前该测试必须先红） | G1 |
| 组合/变形不变量 | 图级：plan+evaluate 都重问后已无预算做第二轮 | 不进 refine，直接 generate 交付 partial，**不**因预算耗尽变 refusal | G2 |
| 诚实边界 | 新增用户可见文案/warning | 本任务**不新增任何用户可见文案**；warning 只复用既有 `budget_exhausted` / `request_failed:LLMError` 两个既有 code。断言：改后 warnings 集合 ⊆ 既有 code 集合（`warnings.py` 的规范表） | U12 |
| 判据 fail-closed 负例 | 「JSON 模式使失败率降到 0」与「generate 永不被饿死」两条**推不出** | 对应负例：①stub 在 `json_mode=True` 下仍返回空串 → 仍走失败路径、**不**因声明了 JSON 模式而放宽（U4 的 `json_mode=True` 变体）；②构造 plan 与 evaluate 各重问一次的场景 → 断言此时**允许**出现无 generate 的终态，即不变量不声称"必执行"（G3） | U4b/G3 |

## 验收标准

- [ ] `json_mode=True` 时请求带 `response_format`，不传时不带（P0 路径逐字不变）
- [ ] 空串/纯空白/`None` 三种响应均判为空响应并抛 `LLMError`
- [ ] 非 generate 调用的重问在会挤掉 generate 预留时不发生，记 `budget_exhausted` 且不增 `llm_calls`
- [ ] 图级：复现 c05 形态（evaluate 两轮各首答非法）时 `generate` 被执行、终态非 `refusal`；该测试在实现前先红
- [ ] `FakeLLM` 接受 `json_mode`，14 个既有测试文件零改动
- [ ] 不新增用户可见文案；warning code 集合未扩张
- [ ] `make ci`、`make eval-ci`、`make verify-task` 全绿（`verify-full` 仍受 `T312-P2-01` 阻塞，归 T31.2R-b）
- [ ] 「推不出」两条各有 fail-closed 负例，且 packet 与 dev-log 均未出现发生率承诺

## GPT 前置计划审查

- 结论：`PENDING`
- 审查基准：
- 阻塞问题：
- 批准范围：

只有结论为 `PLAN_APPROVED` 后，metadata 的 `status` 才能改为 `approved`。

**送审重点（执行者自陈的三处不确定）**：①「为 generate 预留 1 次」的判定放在 `_call_llm` 内按 `call_key` 区分，是否比放在 `graph.can_refine` 里调预留数更合适——后者实测**无法**在预算 6 内给出完备保证（refine 2 + evaluate 2 + generate 1 = 5，plan+evaluate 已用 2 时余 4 < 5）；②空串响应由 `invalid_structured_output` 改记 `request_failed:LLMError` 是语义更准但**是行为变化**，是否接受；③是否需要同时保留一条"JSON 模式下仍空串"的诊断计数，供 T31.2 重跑时观察残余率。

## 执行者自审

- [ ] 实际文件全部在 allowed_paths
- [ ] 没有未批准依赖、迁移、schema、API、golden/snapshot 变化
- [ ] 没有大面积格式化、debug、TODO/FIXME、`if testing`
- [ ] 没有吞异常、降低断言、skip/xfail
- [ ] 状态、权限、幂等、失败行为符合合同
- [ ] 冻结测试真实运行（逐行对表勾掉，指到具体测试函数名）
- [ ] 未新增用户可见文案；两条「推不出」各有 fail-closed 负例
- [ ] `make verify-task PACKET=<本文件>` 通过
- [ ] 已创建 candidate commit，工作区干净
- [ ] 日志记录 candidate HEAD 和对应命令结果

结论：`NOT_READY`

## Review Ledger

| Issue ID | 严重度 | 合同条款 | 文件/位置 | 证据与实际结果 | 最小修复边界 | 状态 |
|---|---|---|---|---|---|---|

## 定向修复记录

| Issue ID | 修复文件 | 回归测试 | 测试结果 |
|---|---|---|---|

## 最终验收

- Base SHA：`e7896c4a7eec2e7fb6ec9f1b7592b685c2e0bc3a`
- Head SHA：
- P0：
- P1：
- 当前合同内 P2：
- P3/backlog：
- `make verify-full`：
- 远端 exact-head CI：
- Reviewer 结论：`PENDING`
