+++
task_id = "T33.1"
status = "review"
risk = "low"
base_sha = "b450d9450bfea163c7ba3f4cb35085526f3511e6"
max_changed_files = 5
allowed_paths = ["benchmarks/p16_function_calling_spike.py", "benchmarks/p16_function_calling_spike.md", "benchmarks/p16_function_calling_spike.json", "docs/tasks/T33.1-function-calling-spike.md", "docs/P1.6任务清单.md"]
allow_dependency_changes = false
allow_migration_changes = false
allow_golden_changes = false
size_exception = ""
+++

# Task Packet：T33.1 function-calling 可行性实测探针（spike）

## 任务目标

用当前冻结的 provider/model 实测 function-calling 是否可用，产出**可直接用于冻结合同的真实观测**，取代目前靠推演写下的 §6 各项。

## 背景与当前问题

三轮前置审查共 20 条 finding 全部成立，自造率 0% → 20% → **62.5%**；第三轮轴三「修复直接引入缺陷」判 ❌。失败形态高度一致：**在规定尚不存在的代码的行为，且没有执行任何东西**（T3 收敛机制、九行状态表、G8 扫描规则、G9 lexer 声称、G1 双写漂移）。凡能跑命令验证的（S1/S8）基本都对。

用户 2026-08-11 裁决**改走 spike：先跑再冻**。本任务是两个 spike 中的第一个，也是 ADR-0011 的**硬闸门**——探针失败则整个 D6 窄修订的可行性前提不成立。

## 范围

- 新增 `benchmarks/p16_function_calling_spike.py`：独立可执行脚本，**不含任何仓库数据**。
- 新增 `benchmarks/p16_function_calling_spike.md`：观测结果留档。
- 更新 `docs/P1.6任务清单.md` 的 T33.1 条目回填结果。

## 非目标

- **不碰 `src/`**：探针直接用 `openai` SDK，不改 `LLMClient`（那是 T33.2）。
- 不查询任何仓库数据、不连数据库、不加载 embedder。
- 不据本探针结果直接冻结规格——结果先回填任务清单，规格修订另行送审。

## 仓库事实调查

| # | 事实 | 证据（已执行） |
|---|---|---|
| F1 | `openai>=2` 在运行依赖内，实测版本 2.45.0 | `pyproject.toml`；审查者第二轮已独立复核 |
| F2 | `LLMClient.complete` 返回 `LLMResult(text, model, usage)`，**无 tool_calls 通道** | `src/devkb/llm.py` |
| F3 | 模型冻结为 `deepseek-v4-flash`（ADR-0004），本项目**从未跑过 function-calling** | ADR-0004；全仓 `grep tools=` 零命中 |
| F4 | 该模型实测存在「内容写进 `reasoning_content` 而 `content` 为空」的间歇行为 | T31.2R-a 记录 |
| F5 | 真实模型调用须 unset 全部代理变量 | `CLAUDE.md` 环境事实；本机实测 9 个 proxy 变量已设置 |

## 关键不变量

- I1：脚本**零仓库数据**——工具与问题均为硬编码的合成内容。
- I2：脚本不导入 `devkb.*` 的任何 `src` 模块（避免误触生产路径）。
- I3：不写数据库、不改 `src/`、不改 `evalsets/`。
- I4：API key 只经环境变量读取，**不打印**。

## 实现计划

1. 写探针脚本，固定一个假工具 `get_weather(city)`，固定问题。
2. **观测项（这就是要拿的数据）**：
   - O1 能否返回结构化 `tool_calls`？字段形态如何？
   - O2 一次响应里 `tool_calls` 的**实际条数**（验证「最多一个」是否是真实约束还是我的假设）。
   - O3 把工具结果喂回去后能否继续，并给出最终文本？
   - O4 `content` 与 `reasoning_content` 在带 tools 时的取值形态（F4 是否复现）。
   - O5 一轮完整交互的**实际请求数与 token 用量**（供 11/5/4 定标）。
   - O6 失败形态：不给工具结果直接再问会怎样；`tool_choice` 不同取值的行为。
3. 三次重复运行，观察**稳定性**（该模型有已知的间歇性问题）。
4. 结果落 `benchmarks/p16_function_calling_spike.md`，含原始响应摘要。

## 冻结测试

本任务是 spike，**不写冻结测试**——它的产出是观测数据，不是行为代码。质量由「观测项 O1–O6 全部有明确结论」保证。

## 验收标准

1. 脚本满足 I1–I4（零仓库数据、不导入 `devkb`、不写库、不打印 key）。
2. O1–O6 六项各有明确结论并落盘，**含原始响应字段摘要**（不是转述）。
3. 三次运行的稳定性已记录（一致 / 不一致及差异）。
4. 结论明确回答：**ADR-0011 的可行性前提是否成立**。
5. 若不成立，packet 与任务清单如实记录并**停止 P1.6，回到 ADR 裁决**——不得改用替代方案。
6. `make ci` 全绿；`make verify-task` PASS。

## GPT 前置计划审查

用户 2026-08-11 裁决改走 spike 模式，本任务为其第一步。审查者第三轮已确认 T33.1 有「不成立即停止回裁决」的停止条件（S7 ✅）。

## 执行者自审

- **I1 ✅ 零仓库数据**：工具为 `get_weather`/`get_population`，问题为「北京和上海的天气和人口」，全文无本仓库或语料信息。
- **I2 ✅ 不导入 `devkb.*`**：脚本只 `import openai`，无 `from devkb...`。
- **I3 ✅ 不写库、不改 `src/`、不改 `evalsets/`**：`git diff --stat` 仅 5 个文件，全在 `allowed_paths`。
- **I4 ✅ key 不打印**：只从环境变量或 `.env` 读取，代码中无任何输出 key 的路径。
- **门禁**：`make ci` 全绿；`make verify-task` PASS（**5 / 5**，全在 `allowed_paths`）；ruff 全过。
- **验收标准 1–4 ✅**：O1–O6 六项各有明确结论并落盘、含原始字段摘要；三次稳定性已记录；结论明确——**ADR-0011 可行性前提成立**。
- **验收标准 5 不适用**：探针未失败，无需停止。

**主动声明**：本 spike 的最大产出不是「能用」，而是**推翻了规格三处已冻结的设计**（详见结果文档「推翻了什么」），其中「协议失败槽位」在协议层不可实现、会直接 400。**按 spike 纪律，本任务不修改规格**——观测先回填任务清单，规格修订另行送前审。

## Review Ledger

| 编号 | 严重度 | 描述 | 处置 |
|---|---|---|---|
| — | — | 待限定审查 | — |

## 定向修复记录

（无）

## 最终验收

**状态：spike 完成、观测齐备，`status = review` 等限定审查——执行者不自我验收。**

| 验收项 | 结果 | 证据 |
|---|---|---|
| 1. I1–I4 | ✅ | 见自审 |
| 2. O1–O6 有明确结论并落盘含原始字段 | ✅ | `benchmarks/p16_function_calling_spike.md` + `.json` |
| 3. 三次稳定性已记录 | ✅ | `tool_calls_n`/name/arguments/`finish_reason`/400 形态**三次完全一致**；仅 `reasoning_len`(178/227/321) 与正文长度(128/124/180) 自然波动 |
| 4. ADR-0011 可行性前提是否成立 | ✅ **成立** | O1/O3/O6-b 均通过 |
| 5. 失败则停止 | 不适用 | 探针未失败 |
| 6. `make ci` + `make verify-task` | ✅ | ci 全绿；verify-task PASS（5/5） |

> **本 spike 证明的是「该模型的 function-calling 可用」，不是「P1.6 的设计可行」。** 恰恰相反——它证明当前规格的工具循环设计有三处必须改，其中一处不可实现。规格修订须另行送前审。
