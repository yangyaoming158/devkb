# P0 任务清单（做一项勾一项）

> 使用规则（详见 CLAUDE.md）：完成判据全部满足才勾选，勾选时在该项后追加一行 `✅ YYYY-MM-DD 结果简记`；与规格冲突/规格缺失 → 停止该项，写入文末"偏差记录"等裁决。
> 依赖关系：仅 **T1 阻塞 T4**（向量维度）；T2/T3 可与 T1 并行；U1 阻塞 T1 的质量小测子项。

## U1 用户任务（人工，与开发并行）

- [x] U1.1 按《Evaluation-v0》格式撰写 dev 检索集 ≥15 问（含 rel_path+anchor 标注），存 `evalsets/v0/retrieval_dev.jsonl`
  - 判据：≥15 条、每条有人工标注锚、真实问题占比 ≥60%、可被 T1 质量小测直接加载
  - ✅ 2026-07-12 Claude 从 mini-mall 真实文档/dev-log 转写 20 问草稿，用户全量确认；q01–q15 入 dev，全部锚点带出处与证据摘录（底稿存档于 retrieval_dev.draft.md）
- [x] U1.2 获取 DeepSeek API Key，填入本地 `.env`（不入库）
  - ✅ 2026-07-12 `.env` 已填入真实 Key；`.gitignore` 已提前创建并排除 .env（连通性最终由 T1.5 smoke 验证）
- [ ] U1.3 补齐 dev 集至 17 问 + 不可答 dev 4 问 + holdout 10 问（可在 T9 前完成）
  - 判据：共 31 问；holdout 单独文件；来源字段（real/llm_assisted）齐全
  - 进度 2026-07-12：dev 检索 15/17、不可答 dev 4/4 ✅、holdout 检索 5/8、不可答 holdout 2/2 ✅（四个 JSONL 已生成）。尚缺：dev 检索 2 问 + holdout 检索 3 问，建议由用户**盲写**（不看现有题目），主题候选：支付超时取消、通知服务职责、审计日志、demo seed 数据、压测阈值

## T1 模型与环境基准（产出 ADR-002 / ADR-004）

- [x] T1.1 环境验证：uv 安装；CUDA 版 PyTorch 装入基准环境；`torch.cuda.is_available()` 为 True；`nvidia-smi` 可见 4060
  - ✅ 2026-07-12 uv 0.11.28；torch 2.13.0+cu130 cuda_available=True，4060/8GB 识别正常；ST 5.6.0、openai 2.45.0。备注：PyPI 经代理仅 ~0.5MB/s，切清华镜像后 ~43MB/s，后续依赖安装固定用 tuna 镜像
- [x] T1.2 语料抽样脚本：从 mini-mall-order 抽 300–500 段（docs/ 文档 + Java 片段，朴素固定长度切分），存 `benchmarks/sample_corpus.jsonl`
  - ✅ 2026-07-12 `benchmarks/sample_corpus.py`（纯标准库、确定性）：350 md 章节块 + 150 Java 干扰片段 = 500 块，含 rel_path/heading 元数据供锚点判中
- [x] T1.3 跑 4 候选 × {GPU fp16, CPU} 矩阵：磁盘大小/加载时间/RAM/VRAM/吞吐(batch 8/32/64)/单查询 P50，原始数据存 `benchmarks/results/*.json`
  - ✅ 2026-07-12 7 个 model×device JSON 落盘；Qwen3 CPU 轮 95 分钟未完成被中止（"CPU 批量不可用"本身作为测量结论记录）。关键发现：8GB 卡需 fp16+截断 1024；Qwen3 batch64 显存溢出致吞吐崩至 1.7 块/s
- [x] T1.4 质量小测（依赖 U1.1）：15 问 Recall@5，精确扫描
  - ✅ 2026-07-12 Qwen3 0.80 / bge-m3 0.60 / bge-base-zh 0.467 / e5 0.067(异常)。教训：首轮因抽样丢锚点块致召回天花板塌陷至 0.40，修复后重测（锚点覆盖校验已固化进 sample_corpus.py）
- [x] T1.5 DeepSeek smoke：连通性；plan 型与生成型 prompt 各 5 次的 P50 延迟；JSON 结构化输出解析成功率；记录模型版本（原判据含"单价"，2026-07-13 修订移交 T9.5，使文字与勾选一致）
  - ✅ 2026-07-12 `benchmarks/results/deepseek_smoke.json`：served=deepseek-v4-flash；plan(JSON mode) P50 1080ms 解析 5/5；generate P50 1150ms。单价未从官网核实，ADR-004 标记待核查；**单价记录职责经审查裁定移交 T9.5**。注意：本机 SOCKS 代理会干扰 httpx，调 DeepSeek/hf-mirror 需 unset 代理直连
- [x] T1.6 产出 `benchmarks/embedding_report.md`（结论表+决策规则套用过程）
  - ✅ 2026-07-12 报告含完整矩阵、规则套用、4 条关键教训、复现命令
- [x] T1.7 写 `docs/adr/0002-embedding-freeze.md`（冻结 model_id + 维度）与 `docs/adr/0004-llm-provider.md`
  - 判据：ADR-002 明确给出唯一 model_id 与 vector 维度数字；决策符合《模型与环境基准方案》第 5 节预冻结规则
  - ✅ 2026-07-12 **冻结 Qwen/Qwen3-Embedding-0.6B，vector(1024)**，附 4 条强制使用约束；ADR-004 记录 DeepSeek smoke 实测。**T4 迁移门禁解除**

## T2 仓库骨架

- [x] T2.1 git init + `.gitignore`（.env/模型缓存/__pycache__/.venv 等）
  - ✅ 2026-07-12 main 分支初始提交 6c17bf8（治理文档+评测集+基准产物）；.env/.venv 确认被忽略；开发分支 `p0`
- [x] T2.2 `uv init`：pyproject（py3.12 pin、包名 devkb、src 布局），uv.lock 提交
  - ✅ 2026-07-12 P0 全量依赖入 pyproject；ml 组（torch/ST）与 dev 组分离，CI 可跳过真模型栈；默认索引=清华镜像，CI 用 env 覆盖回官方源
- [x] T2.3 ruff + pyright + pytest 配置（pyproject 内）；`src/devkb/` 空包 + `cli.py` 输出版本号的最小入口
  - ✅ 2026-07-12 `uv run devkb --version` → devkb 0.1.0；ruff 关闭 RUF001/002/003（中文注释约定）
- [x] T2.4 Makefile（up/lint/test/ci）；`.github/workflows/ci.yml` 跑 ruff+pyright+pytest
  - 判据（2026-07-12 第二轮审查后拆分）：本地 `make ci` 绿；`uv run devkb --version` 可执行。GitHub Actions 首绿判据移至 T10.6
  - ✅ 2026-07-12 本地 `make ci` 全绿（ruff/pyright 0 错、pytest 1 passed）；工作流文件已就位，远程验证见 T10.6

## T3 基础设施骨架

- [x] T3.1 `docker-compose.yml`：pgvector/pgvector:pg16 + 健康检查 + 命名 volume + init 建扩展
  - ✅ 2026-07-12 `make up` 后 devkb-postgres healthy，vector 扩展经 initdb 脚本自动创建（实测确认）
- [x] T3.2 `config.py`：《P0实现规格》§9 全部配置项，pydantic-settings，`.env.example` 同步
  - ✅ 2026-07-12 全配置项含 ADR-0002/0004 默认值；llm_api_key 用 SecretStr（打印自动掩码）；缺失时抛 ConfigError 并指名缺哪个变量
- [x] T3.3 `logging.py`（structlog，dev 彩色/JSON 可切）+ `errors.py`（规格 §9 异常层级）
  - 判据：`docker compose up -d` 后 pg 健康；config 单测通过（缺必填项时报 ConfigError）
  - ✅ 2026-07-12 structlog 含敏感键脱敏 processor；异常层级 9 类照规格；make ci 全绿（pyright 修了 2 处真实类型问题）

## T4 数据层（依赖 T1 的维度冻结）

- [x] T4.1 `models.py`：projects/documents/chunks/agent_runs 四表（约束照规格 §5）
  - ✅ 2026-07-13 四表含第三轮审查新增的 usage JSONB 与 cost 列；EMBEDDING_DIM=1024 常量对齐 ADR-0002
- [x] T4.2 alembic init + migration 001（vector 维度 = ADR-002 数值）；升降级均可执行
  - ✅ 2026-07-13 up→down→up 三连实测通过；migration 内幂等建 vector 扩展（CI service container 无 initdb 挂载，靠此建扩展）
- [x] T4.3 `repositories.py`：Repository 以 project_id 实例化；文档 upsert、chunk 批量写、向量检索、run 读写
  - ✅ 2026-07-13 Project/Document/Chunk/Run 四仓储；修复真实 ORM 陷阱（upsert RETURNING 命中身份映射旧对象返回陈旧属性，populate_existing 解决并有回归测试）
- [x] T4.4 双项目隔离集成测试（种两个项目，检索互不可见）
  - ✅ 2026-07-13 三个隔离断言全过，含最严苛场景：用 B 项目的完美匹配向量（相似度=1）在 A 范围检索，B 不可见。基座=临时数据库+迁移+销毁
- [x] T4.5 CI 静态扫描：sqlalchemy 查询构造仅允许出现在 repositories.py（脚本或测试实现，违者 fail）
  - 判据：`alembic upgrade head && alembic downgrade base && alembic upgrade head` 全成功；隔离测试与静态扫描进 CI 且绿
  - ✅ 2026-07-13 tests/unit/test_layering.py AST 扫描（select/insert/update/delete/text 等仅限 repositories.py）；ci.yml 加 pgvector service container；本地 make ci 全绿 10 passed

## T5 Markdown 分块

- [x] T5.1 `ingest/markdown.py`：markdown-it-py 标题树分块；300–500 token 目标；段落递归切分；代码围栏不切；title_path 面包屑；源起止行号；token 计数注入普通 callable（生产=Qwen tokenizer 懒加载；**不新增 Protocol**，守硬规则）
  - ✅ 2026-07-14 两趟算法：标题切章节（面包屑重置正确）+ 章节内贪心装箱；超目标非原子块按行二分递归切分（切分下限=单行，行内切开会使引用行号失配）；围栏/表格原子；content 为源行逐字切片 + sha256；TokenCounter=`Callable[[str], int]`，未新增 Protocol
- [x] T5.2 `tests/fixtures/corpus_md/` 微语料（含中文/英文/长节/表格/代码围栏各一）+ golden 快照测试
  - 判据：**快照 A**（近似计数器，CI 可跑）锁 chunk 边界/行号/title_path；**独立快照 B**（真实 tokenizer，CI 自动跳过）锁真实 token_count 与边界
  - ✅ 2026-07-14 5 个 fixture（zh/en/long_section/table/fenced）；快照 A 于 `tests/fixtures/golden/`（20 测试，含围栏原子/表格原子/行号不重叠/内容逐字/递归切分等不变量）；快照 B 于 `tests/fixtures/golden_ml/`（真实 Qwen tokenizer，`importorskip("transformers")` CI 自动跳过，本地 6 通过）；重生成机制 `DEVKB_UPDATE_GOLDEN=1`；make ci 全绿 36 passed

## T6 摄取管道

- [x] T6.1 `ingest/pipeline.py`：扫描(.md/.txt，≤5MB，跳隐藏)→编码规范化→content_hash 跳过未变→事务内删旧插新→失败文档隔离(status+parse_error)
  - ✅ 2026-07-14 全程经 Repository（守 D7）；GBK 实测可规范化、纯乱码字节确定性 ParseError；嵌入按清单留给 T7.2 接入，本阶段 embedding 落 NULL
- [x] T6.2 CLI `devkb ingest <dir> --project <slug>` 输出成功/失败/跳过统计表
  - ✅ 2026-07-14 rich 统计表 + 失败原因表；项目 slug 不存在自动创建；DevKbError 顶层捕获非 0 退出；生产 token 计数 = 冻结模型 tokenizer（懒加载）
- [x] T6.3 幂等集成测试：同目录二次摄取新增为 0；含一个构造的坏文件仍不中断批次
  - 判据：三条测试全绿；对 fixtures 语料实跑输出符合预期
  - ✅ 2026-07-14 三测试绿（首摄计数含嵌套目录/GBK txt/隐藏目录排除；二次摄取零新增+单文件变更仅重摄该文档；乱码+超 5MB 两个坏文件 failed 隔离批次继续）；CLI 实跑 fixtures：首跑 5 成功 16 chunks（与 golden B 一致），二跑 5 跳过；make ci 39 passed

## T7 Embedding 接入

- [x] T7.1 `embedding.py`：Embedder Protocol + SentenceTransformer 实现（ADR-002 模型，cuda|cpu 可配）+ FakeEmbedder（确定性哈希向量）
  - ✅ 2026-07-14 Protocol 仅出现在允许的接缝文件；生产实现落实 ADR-0002 四约束（fp16/seq≤1024/batch=32/查询 prompt_name="query"）；FakeEmbedder 同文本 query/doc 向量一致，便于 T8 精确命中断言
- [x] T7.2 批量嵌入接入 pipeline：batch 取基准值，OOM 减半退避，tenacity 重试
  - ✅ 2026-07-14 OOM 减半退避在 Embedder 内（batch=1 仍 OOM 才报错）；tenacity 对 EmbeddingError 重试 1 次；仍失败按单文档隔离（failed+parse_error）不中断批次
- [x] T7.3 集成测试全部用 FakeEmbedder（CI 无 GPU）；真模型冒烟脚本放 benchmarks/ 手动跑
  - 判据：fixtures 语料摄取后 chunks.embedding 非空；CI 不加载真模型
  - ✅ 2026-07-14 集成测试注入 FakeEmbedder 并断言全部 chunk embedding 非空；benchmarks/embedder_smoke.py 实跑（HF_HUB_OFFLINE 走缓存）：加载 5.5s、17 chunks 1.76s、query prompt 生效（同文本 cos 0.89）、top1 语义命中；CLI 实跑 fixtures-t7 项目 16/16 embedding 非空（psql 核验）；make ci 39 passed

## T8 向量检索

- [x] T8.1 `retrieval.py`：查询嵌入 → repo 精确扫描 top-k + 余弦分数
  - ✅ 2026-07-14 RetrievedChunk 含引用渲染全字段；vector_search 联查 documents 带出 rel_path（查询构造仍只在 repositories.py，D7 扫描通过）
- [x] T8.2 集成测试：FakeEmbedder 下已知查询命中期望 chunk（可精确断言）
  - 判据：测试绿；检索结果含 rel_path/行号/title_path/score 完整元数据
  - ✅ 2026-07-14 三测试绿：以 chunk 逐字原文为查询 top1 精确命中（score>0.999、rel_path/title_path 逐字断言）、8 条结果元数据完整性、top_k 生效；make ci 42 passed

## T9 LLM 与固定问答管道

- [ ] T9.1 `llm.py`：LLMClient Protocol + OpenAI 兼容实现（DeepSeek）+ FakeLLM（脚本化输出）；超时与 1 次重问
- [ ] T9.2 `answer.py`：检索→证据编号 E#→生成 prompt（仅依据证据、中文回答保留英文术语、无证据明说）→L0 校验（越界 E# 剔除并记 warnings）→Answer JSON（规格 §6 结构）→agent_runs 落库
- [ ] T9.3 CLI `devkb ask`（rich 渲染引用表+token/延迟，--json 全量）与 `devkb runs list|show`
- [ ] T9.4 e2e：FakeLLM 正常路径断言 Answer 结构；FakeLLM 输出虚假 E# 被剔除且 warnings 记录
  - 判据：e2e 全绿；真实 DeepSeek 手动冒烟 1 次成功（结果记录于 PR/提交说明，不进 CI）
- [ ] T9.5 定价冻结与成本闭环（2026-07-12 第二轮审查补入；2026-07-13 第三轮修订）：~~官网核实单价~~ **单价已核实并记入 ADR-0004**（2026-07-13，v4-flash 三档：缓存命中输入 0.02/未命中输入 1/输出 2 元每百万 tokens）。剩余：计价纯函数**按三档 token 分别计算**（读 agent_runs.usage JSONB）；真实冒烟 run 落库 cost 非空
  - 判据：agent_runs 至少 1 条 cost 非空记录，且 cost 可由该 run 的 usage 三档字段 × ADR-0004 记录单价人工复算一致

## T10 收尾与 P0 验收

- [ ] T10.0 转写 ADR-0001（单 Postgres 与规模假设）、ADR-0003（语料=mini-mall 与解析优先级）、ADR-0007（P0/P1 裁剪清单），内容以《范围冻结与架构决策》为准
  - 判据：docs/adr/ 五份齐全，满足验收第 8 条（2026-07-12 外部审查补入——原清单漏了这三份的产出任务）
- [ ] T10.1 README：一页起步（compose→迁移→ingest→ask），已知限制如实列出（无拒答策略、纯向量、prompt 级诚实性）
- [ ] T10.2 真语料实跑：摄取 mini-mall-order 的 docs/ + 根级 README*，记录文档数/chunk 数/耗时
- [ ] T10.3 人工抽查 5 问并记录到 `evalsets/reports/p0-manual-check.md`（每问：引用路径行号真实、内容对应与否）
- [ ] T10.4 holdout 检索 10 问跑一次基线（脚本级即可），结果存 `evalsets/reports/p0-holdout.md`
- [ ] T10.5 验收总清单逐项核对（见下）
- [ ] T10.6 远程仓库与 CI 验证（原 T2.4 判据移入）：创建 GitHub 仓库（需用户操作或授权）、push main+p0、GitHub Actions 首次全绿

## P0 验收总清单（对应《P0实现规格》§12）

- [ ] 1. `docker compose up -d && uv run alembic upgrade head` 成功
- [ ] 2. ingest 真语料出统计；重跑第二次新增为 0
- [ ] 3. 抽查 5 问全部有真实可对应引用
- [ ] 4. 虚假 E# 被 L0 剔除（e2e 证明）
- [ ] 5. pytest 本地与 CI 全绿；ruff/pyright 零错误
- [ ] 6. 双项目隔离测试通过
- [ ] 7. `devkb runs show` 取回含 token/延迟的历史 run；至少 1 条真实 run 的 cost 非空且可由 usage×已记录单价复算（T9.5）
- [ ] 8. benchmarks/ 有报告；docs/adr/ 有 ADR-001~004、007

## 偏差记录（执行中与规格的冲突，等用户裁决）

| 日期 | 任务项 | 冲突/缺失描述 | 建议 | 裁决 |
|---|---|---|---|---|
| 2026-07-12 | 验收8 / T1.7 | 验收要求 ADR-001~004、007，但清单无任务产出 0001/0003/0007 | 新增 T10.0 转写任务 | ✅ 已修复（GPT-5.6 外部审查发现，用户裁决修改） |
| 2026-07-12 | 规格§5 / ADR-0004 | ADR-0004 承诺按 run 落库 cost，agent_runs 表却无该列 | 规格 §5 增加 cost 可空列，单价核实前为 NULL | ✅ 已修复（同上） |
| 2026-07-12 | T5 / CLAUDE.md 测试纪律 | 分块 token 计数依赖 Qwen tokenizer（ml 组），CI 不装该组致 golden 测试无法运行 | TokenCounter 接缝：CI 用确定性近似计数器 + 本地真 tokenizer 对齐抽查（CI 跳过） | ✅ 已修复（同上） |
| 2026-07-12 | T1.5 / T9 / 验收7 | cost 只有列没有行为闭环：单价无人核实、无非空 cost 验证，P0 可在全 NULL 下过验收；且 T1.5 在单价未核实时已勾选 | 新增 T9.5（定价冻结+复算判据）；T1.5 单价职责移交 T9.5；验收 7 补强 | ✅ 已修复（第二轮审查发现） |
| 2026-07-12 | T2.4 勾选纪律 | 判据含"Actions 绿"但未验证即勾选，违反勾选纪律 | 判据拆分：本地部分保留勾选，Actions 首绿移入新增 T10.6 | ✅ 按审查建议方案 B 处理（如你倾向直接取消勾选请指示） |
| 2026-07-12 | 规格§6 | "TokenCounter 接缝"表述可能诱导新增 Protocol（违反硬规则），且对齐抽查断言未定义 | 改为注入普通 callable；定义快照 A（近似/CI）+ 快照 B（真 tokenizer/本地）双 golden | ✅ 已修复（第二轮审查发现） |
| 2026-07-13 | 规格§5 / T9.5 | DeepSeek 按缓存命中/未命中/输出三档计价，仅存 tokens_in/out 无法复算 cost | agent_runs 增加 usage JSONB（存原始 usage 含分档字段），计价纯函数按三档计算 | ✅ 已修复（第三轮审查发现，官网核实属实） |
| 2026-07-13 | ADR-0004 / config | deepseek-chat 别名 2026-07-24 弃用（官网公告），验收时模型名将失效 | 冻结目标改为显式 deepseek-v4-flash（config/ADR/.env.example/smoke 同步）；单价三档已核实记入 ADR-0004（来源+日期） | ✅ 已修复（同上） |
| 2026-07-13 | T1.5 文字 | 任务定义仍含"记录单价"但保持勾选，文字上判据未满足 | 修订 T1.5 定义为"记录模型版本"，单价移交注明于定义内 | ✅ 已修复（同上） |
